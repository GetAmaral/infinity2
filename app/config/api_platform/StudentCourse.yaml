# API Platform 4 Configuration for StudentCourse
# This file is ALWAYS regenerated by Genmax. DO NOT edit manually.
# @generated by Genmax Code Generator

resources:
  App\Entity\StudentCourse:
    shortName: StudentCourse
    description: "Student course enrollments and participation"

    # DTO configuration - processor is set per-operation to avoid affecting DELETE
    output: App\Dto\StudentCourseOutputDto

    normalizationContext:
      groups: ["student_course:read"]

    denormalizationContext:
      groups: ["student_course:write"]

    order:
      createdAt: DESC

    security: "is_granted('ROLE_ORGANIZATION_ADMIN')"

    operations:
      - class: ApiPlatform\Metadata\GetCollection
        security: "is_granted('ROLE_ORGANIZATION_ADMIN')"
      - class: ApiPlatform\Metadata\Get
        security: "is_granted('ROLE_ORGANIZATION_ADMIN')"
      - class: ApiPlatform\Metadata\Post
        input: App\Dto\StudentCourseInputDto
        processor: App\State\StudentCourseProcessor
        security: "is_granted('ROLE_ORGANIZATION_ADMIN')"
      - class: ApiPlatform\Metadata\Put
        input: App\Dto\StudentCourseInputDto
        processor: App\State\StudentCourseProcessor
        security: "is_granted('ROLE_ORGANIZATION_ADMIN')"
      - class: ApiPlatform\Metadata\Patch
        input: App\Dto\StudentCourseInputDto
        processor: App\State\StudentCourseProcessor
        security: "is_granted('ROLE_ORGANIZATION_ADMIN')"
      - class: ApiPlatform\Metadata\Delete
        security: "is_granted('ROLE_ORGANIZATION_ADMIN')"


    # Property-level configuration
    properties:
      organization:
        description: "Organization"
        example: "/api/organizatia/01234567-89ab-cdef-0123-456789abcdef"
      enrolledAt:
        description: "EnrolledAt"
        example: "sample value"
        # Advanced filters
        filters:
          - type: OrderFilter
      startDate:
        description: "StartDate"
        example: "sample value"
        # Advanced filters
        filters:
          - type: OrderFilter
      lastDate:
        description: "LastDate"
        example: "sample value"
        # Advanced filters
        filters:
          - type: OrderFilter
      progressSeconds:
        description: "ProgressSeconds"
        example: "sample value"
        # Advanced filters
        filters:
          - type: OrderFilter
      progressPercentage:
        description: "ProgressPercentage (decimal value)"
        example: "99.99"
        # Advanced filters
        filters:
          - type: OrderFilter
      completedAt:
        description: "CompletedAt"
        example: "sample value"
        # Advanced filters
        filters:
          - type: OrderFilter
      student:
        description: "Student (text field)"
        example: "Sample longer text value"
        # Advanced filters
        filters:
          - type: OrderFilter
      course:
        description: "Course (text field)"
        example: "Sample longer text value"
        # Advanced filters
        filters:
          - type: OrderFilter
      currentLecture:
        writable: false
        description: "CurrentLecture (text field)"
        example: "Sample longer text value"
        # Advanced filters
        filters:
          - type: OrderFilter
      studentLectures:
        description: "StudentLectures (text field)"
        example: "Sample longer text value"
        # Advanced filters
        filters:
          - type: OrderFilter
      active:
        description: "Whether this record is active"
        example: "true"
        # Advanced filters
        filters:
          - type: OrderFilter
