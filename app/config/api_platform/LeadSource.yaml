# API Platform 4 Configuration for LeadSource
# This file is ALWAYS regenerated by Genmax. DO NOT edit manually.
# @generated by Genmax Code Generator

resources:
  App\Entity\LeadSource:
    shortName: LeadSource
    description: "Lead sources for multi-touch attribution tracking and ROI analysis"

    # DTO configuration - processor is set per-operation to avoid affecting DELETE
    output: App\Dto\LeadSourceOutputDto

    normalizationContext:
      groups: ["leadsource:read"]

    denormalizationContext:
      groups: ["leadsource:write"]

    order:
      category: ASC
      name: ASC

    security: "is_granted('ROLE_USER')"

    operations:
      - class: ApiPlatform\Metadata\GetCollection
        security: "is_granted('ROLE_USER')"
      - class: ApiPlatform\Metadata\Get
        security: "is_granted('ROLE_USER')"
      - class: ApiPlatform\Metadata\Post
        input: App\Dto\LeadSourceInputDto
        processor: App\State\LeadSourceProcessor
        security: "is_granted('ROLE_CRM_ADMIN')"
      - class: ApiPlatform\Metadata\Put
        input: App\Dto\LeadSourceInputDto
        processor: App\State\LeadSourceProcessor
        security: "is_granted('ROLE_CRM_ADMIN')"
      - class: ApiPlatform\Metadata\Patch
        input: App\Dto\LeadSourceInputDto
        processor: App\State\LeadSourceProcessor
        security: "is_granted('ROLE_USER')"
      - class: ApiPlatform\Metadata\Delete
        security: "is_granted('ROLE_CRM_ADMIN')"


    # Property-level configuration
    properties:
      organization:
        description: "Organization"
        example: "/api/organizatia/01234567-89ab-cdef-0123-456789abcdef"
      name:
        description: "The brand name"
        example: "Nike"
        # Advanced filters
        filters:
          - type: SearchFilter
            strategy: partial
          - type: SearchFilter
            strategy: partial
          - type: OrderFilter
      description:
        description: "..."
        example: "..."
        # Advanced filters
        filters:
          - type: SearchFilter
            strategy: partial
          - type: SearchFilter
            strategy: partial
      category:
        description: "Category (text field)"
        example: "Sample text"
        # Advanced filters
        filters:
          - type: SearchFilter
            strategy: exact
          - type: SearchFilter
            strategy: partial
          - type: OrderFilter
      active:
        description: "Whether this record is active"
        example: "true"
        # Advanced filters
        filters:
          - type: OrderFilter
          - type: BooleanFilter
      deals:
        description: "Deals"
        example: "sample value"
