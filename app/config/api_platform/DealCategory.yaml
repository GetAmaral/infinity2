# API Platform 4 Configuration for DealCategory
# This file is ALWAYS regenerated by Genmax. DO NOT edit manually.
# @generated by Genmax Code Generator

resources:
  App\Entity\DealCategory:
    shortName: DealCategory
    description: "Deal categories for reporting and analysis"

    # DTO configuration - processor is set per-operation to avoid affecting DELETE
    output: App\Dto\DealCategoryOutputDto

    normalizationContext:
      groups: ["dealcategory:read"]

    denormalizationContext:
      groups: ["dealcategory:write"]

    order:
      createdAt: DESC

    security: "is_granted('ROLE_CRM_ADMIN')"

    operations:
      - class: ApiPlatform\Metadata\GetCollection
        security: "is_granted('ROLE_CRM_ADMIN')"
      - class: ApiPlatform\Metadata\Get
        security: "is_granted('ROLE_CRM_ADMIN')"
      - class: ApiPlatform\Metadata\Post
        input: App\Dto\DealCategoryInputDto
        processor: App\State\DealCategoryProcessor
        security: "is_granted('ROLE_CRM_ADMIN')"
      - class: ApiPlatform\Metadata\Put
        input: App\Dto\DealCategoryInputDto
        processor: App\State\DealCategoryProcessor
        security: "is_granted('ROLE_CRM_ADMIN')"
      - class: ApiPlatform\Metadata\Patch
        input: App\Dto\DealCategoryInputDto
        processor: App\State\DealCategoryProcessor
        security: "is_granted('ROLE_CRM_ADMIN')"
      - class: ApiPlatform\Metadata\Delete
        security: "is_granted('ROLE_CRM_ADMIN')"


    # Property-level configuration
    properties:
      organization:
        description: "Organization"
        example: "/api/organizatia/01234567-89ab-cdef-0123-456789abcdef"
      name:
        description: "The brand name"
        example: "Nike"
        # Advanced filters
        filters:
          - type: OrderFilter
      color:
        description: "Hex color code for visual identification of the category (e.g., #FF5733)"
        example: "#6f42c1"
        # Advanced filters
        filters:
          - type: OrderFilter
      icon:
        description: "Bootstrap Icon class name for visual representation (e.g., bi-briefcase, bi-star)"
        example: "bi-tag"
        # Advanced filters
        filters:
          - type: OrderFilter
      description:
        description: "..."
        example: "..."
        # Advanced filters
        filters:
          - type: OrderFilter
      default:
        description: "Indicates whether this is the default category automatically assigned to new deals"
        example: "false"
        # Advanced filters
        filters:
          - type: OrderFilter
          - type: BooleanFilter
      displayOrder:
        description: "Numerical order for displaying categories in lists and dropdowns (lower numbers appear first)"
        example: "10"
        # Advanced filters
        filters:
          - type: OrderFilter
      parentCategory:
        description: "Parent category for creating hierarchical category structures"
        example: "\/api\/deal_categories\/0199cadd-1234-75ee-b3b7-66e40a72df84"
        # Advanced filters
        filters:
          - type: OrderFilter
      subcategories:
        writable: false
        description: "Collection of child categories under this parent category"
        example: "[\"\/api\/deal_categories\/0199cadd-1234-75ee-b3b7-66e40a72df84\"]"
      deals:
        description: "Deals"
        example: "sample value"
        # Advanced filters
        filters:
          - type: OrderFilter
      active:
        description: "Indicates whether this category is currently active and available for use"
        example: "true"
        # Advanced filters
        filters:
          - type: OrderFilter
          - type: BooleanFilter
      group:
        description: "Group (text field)"
        example: "Sample longer text value"
        # Advanced filters
        filters:
          - type: OrderFilter
