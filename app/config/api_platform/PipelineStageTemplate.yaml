# API Platform 4 Configuration for PipelineStageTemplate
# This file is ALWAYS regenerated by Genmax. DO NOT edit manually.
# @generated by Genmax Code Generator

resources:
  App\Entity\PipelineStageTemplate:
    shortName: PipelineStageTemplate
    description: "Templates for pipeline stages"

    # DTO configuration - processor is set per-operation to avoid affecting DELETE
    output: App\Dto\PipelineStageTemplateOutputDto

    normalizationContext:
      groups: ["pipelinestagetemplate:read"]

    denormalizationContext:
      groups: ["pipelinestagetemplate:write"]

    order:
      createdAt: DESC

    security: "is_granted('ROLE_ORGANIZATION_ADMIN')"

    operations:
      - class: ApiPlatform\Metadata\GetCollection
        security: "is_granted('ROLE_ORGANIZATION_ADMIN')"
      - class: ApiPlatform\Metadata\Get
        security: "is_granted('ROLE_ORGANIZATION_ADMIN')"
      - class: ApiPlatform\Metadata\Post
        input: App\Dto\PipelineStageTemplateInputDto
        processor: App\State\PipelineStageTemplateProcessor
        security: "is_granted('ROLE_ORGANIZATION_ADMIN')"
      - class: ApiPlatform\Metadata\Put
        input: App\Dto\PipelineStageTemplateInputDto
        processor: App\State\PipelineStageTemplateProcessor
        security: "is_granted('ROLE_ORGANIZATION_ADMIN')"
      - class: ApiPlatform\Metadata\Patch
        input: App\Dto\PipelineStageTemplateInputDto
        processor: App\State\PipelineStageTemplateProcessor
        security: "is_granted('ROLE_ORGANIZATION_ADMIN')"
      - class: ApiPlatform\Metadata\Delete
        security: "is_granted('ROLE_ORGANIZATION_ADMIN')"


    # Property-level configuration
    properties:
      organization:
        description: "Organization"
        example: "/api/organizatia/01234567-89ab-cdef-0123-456789abcdef"
      name:
        description: "The brand name"
        example: "Nike"
      description:
        description: "..."
        example: "..."
      probability:
        description: "The probability percentage (0-100) that a deal in this stage will close successfully. Used for weighted pipeline forecasting."
        example: "75.50"
        # Advanced filters
        filters:
          - type: NumericFilter
      order:
        description: "..."
        example: "..."
      active:
        description: "Indicates whether this pipeline stage template is active and available for creating new pipeline instances"
        example: "true"
        # Advanced filters
        filters:
          - type: BooleanFilter
      color:
        description: "Hexadecimal color code for visual representation of this stage in UI (format: #RRGGBB)"
        example: "\"#4CAF50\""
      rottingDays:
        description: "Number of days after which a deal in this stage is flagged as rotting\/stale (requires attention)"
        example: "30"
        # Advanced filters
        filters:
          - type: NumericFilter
      pipelineTemplate:
        description: "..."
        example: "..."
      tasks:
        description: "..."
        example: "..."
      final:
        description: "Indicates if this is a final\/terminal stage where deals cannot progress further (e.g., Won, Lost, Closed)"
        example: "false"
        # Advanced filters
        filters:
          - type: BooleanFilter
      stageType:
        description: "Categorizes the stage type: \"active\" for in-progress stages, \"won\" for successful closes, \"lost\" for unsuccessful closes"
        example: "\"active\""
      automationRules:
        description: "JSON object defining automation rules (e.g., email notifications, task creation) triggered when a deal enters this stage"
        example: "{\"notify_owner\": true, \"create_tasks\": [\"follow_up_call\"], \"send_email_template\": \"proposal_sent\"}"
      requiredFields:
        description: "JSON array of field names that must be completed before a deal can move from this stage to the next"
        example: "[\"contact_email\", \"budget_amount\", \"decision_maker\"]"
      icon:
        description: "Bootstrap Icons class name for visual representation of this stage (without \"bi-\" prefix or with full class)"
        example: "\"bi-check-circle-fill\""
