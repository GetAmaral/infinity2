# API Platform 4 Configuration for AuditLog
# This file is ALWAYS regenerated by Genmax. DO NOT edit manually.
# @generated by Genmax Code Generator

resources:
  App\Entity\AuditLog:
    shortName: AuditLog
    description: "Audit log for tracking all system changes and user actions"

    normalizationContext:
      groups: ["audit:read"]

    denormalizationContext:
      groups: ["audit:write"]

    order:
      createdAt: DESC

    security: "is_granted('ROLE_SUPER_ADMIN')"

    operations:
      - class: ApiPlatform\Metadata\GetCollection
        security: "is_granted('ROLE_SUPER_ADMIN')"
      - class: ApiPlatform\Metadata\Get
        security: "is_granted('ROLE_SUPER_ADMIN')"



    # Property-level configuration
    properties:
      changes:
        writable: false
        description: "Changes (JSON data)"
        example: "{\"key\": \"value\"}"
      action:
        writable: false
        description: "Action (text field)"
        example: "Sample longer text value"
      checksum:
        writable: false
        description: "Checksum (text field)"
        example: "Sample longer text value"
      entityClass:
        writable: false
        description: "Entity Class (text field)"
        example: "Sample longer text value"
      entityId:
        writable: false
        description: "Entity ID"
        example: "sample value"
      metadata:
        writable: false
        description: "Metadata (JSON data)"
        example: "{\"key\": \"value\"}"
      user:
        writable: false
        description: "User"
        example: "sample value"
