# API Platform 4 Configuration for CalendarType
# This file is ALWAYS regenerated by Genmax. DO NOT edit manually.
# @generated by Genmax Code Generator

resources:
  App\Entity\CalendarType:
    shortName: CalendarType
    description: "Calendar types (Personal, Shared, Public, etc.)"

    normalizationContext:
      groups: ["calendartype:read"]

    denormalizationContext:
      groups: ["calendartype:write"]

    order:
      createdAt: DESC

    security: "is_granted('ROLE_EVENT_ADMIN')"

    operations:
      - class: ApiPlatform\Metadata\GetCollection
        security: "is_granted('ROLE_EVENT_ADMIN')"
      - class: ApiPlatform\Metadata\Get
        security: "is_granted('ROLE_EVENT_ADMIN')"
      - class: ApiPlatform\Metadata\Post
        security: "is_granted('ROLE_EVENT_ADMIN')"
      - class: ApiPlatform\Metadata\Put
        security: "is_granted('ROLE_EVENT_ADMIN')"
      - class: ApiPlatform\Metadata\Patch
        security: "is_granted('ROLE_EVENT_ADMIN')"
      - class: ApiPlatform\Metadata\Delete
        security: "is_granted('ROLE_EVENT_ADMIN')"


    # Property-level configuration
    properties:
      organization:
        description: "Organization"
        example: "/api/organizations/01234567-89ab-cdef-0123-456789abcdef"
      name:
        description: "The name of the calendar type (e.g., Personal, Shared, Public, Team)"
        example: "Personal Calendar"
      description:
        description: "Optional description explaining the purpose and usage of this calendar type"
        example: "This calendar type is used for personal appointments and private events"
      calendars:
        writable: false
        description: "Collection of calendars that use this calendar type"
        example: "[\"\/api\/calendars\/0199c000-0000-0000-0000-000000000001\"]"
      color:
        description: "Hexadecimal color code for visual representation of the calendar type"
        example: "#3788d8"
      icon:
        description: "Bootstrap icon class name for visual representation"
        example: "bi-calendar3"
      active:
        description: "Whether this calendar type is active and available for use"
        example: "true"
      default:
        description: "Whether this is the default calendar type for new calendars"
        example: "false"
      visibility:
        description: "Visibility level of calendars using this type (personal, shared, public, team)"
        example: "personal"
      access_level:
        description: "Default access level for calendars of this type"
        example: "read_write"
      sort_order:
        description: "Order in which this calendar type appears in lists (lower numbers appear first)"
        example: "10"
      allow_sharing:
        description: "Whether calendars of this type can be shared with other users"
        example: "true"
      require_approval:
        description: "Whether creating a calendar of this type requires administrator approval"
        example: "false"
      max_calendars_per_user:
        description: "Maximum number of calendars of this type a user can create (null = no limit)"
        example: "5"
