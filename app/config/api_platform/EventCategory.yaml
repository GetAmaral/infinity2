# API Platform 4 Configuration for EventCategory
# This file is ALWAYS regenerated by Genmax. DO NOT edit manually.
# @generated by Genmax Code Generator

resources:
  App\Entity\EventCategory:
    shortName: EventCategory
    description: "Event categories for calendar organization"

    # DTO configuration - processor is set per-operation to avoid affecting DELETE
    output: App\Dto\EventCategoryOutputDto

    normalizationContext:
      groups: ["eventcategory:read"]

    denormalizationContext:
      groups: ["eventcategory:write"]

    order:
      createdAt: DESC

    security: "is_granted('ROLE_EVENT_ADMIN')"

    operations:
      - class: ApiPlatform\Metadata\GetCollection
        security: "is_granted('ROLE_EVENT_ADMIN')"
      - class: ApiPlatform\Metadata\Get
        security: "is_granted('ROLE_EVENT_ADMIN')"
      - class: ApiPlatform\Metadata\Post
        input: App\Dto\EventCategoryInputDto
        processor: App\State\EventCategoryProcessor
        security: "is_granted('ROLE_EVENT_ADMIN')"
      - class: ApiPlatform\Metadata\Put
        input: App\Dto\EventCategoryInputDto
        processor: App\State\EventCategoryProcessor
        security: "is_granted('ROLE_EVENT_ADMIN')"
      - class: ApiPlatform\Metadata\Patch
        input: App\Dto\EventCategoryInputDto
        processor: App\State\EventCategoryProcessor
        security: "is_granted('ROLE_EVENT_ADMIN')"
      - class: ApiPlatform\Metadata\Delete
        security: "is_granted('ROLE_EVENT_ADMIN')"


    # Property-level configuration
    properties:
      organization:
        description: "Organization"
        example: "/api/organizations/01234567-89ab-cdef-0123-456789abcdef"
      name:
        description: "The brand name"
        example: "Nike"
        # Advanced filters
        filters:
          - type: SearchFilter
            strategy: partial
      color:
        description: "Hexadecimal color code for calendar visualization"
        example: "#3498db"
      description:
        description: "..."
        example: "..."
      icon:
        description: "Bootstrap icon class for visual representation"
        example: "bi-calendar-check"
      default:
        description: "Whether this is the default category for new events"
        example: "false"
        # Advanced filters
        filters:
          - type: BooleanFilter
      eventType:
        description: "Type of events this category is designed for"
        example: "meeting"
        # Advanced filters
        filters:
          - type: OrderFilter
      sortOrder:
        description: "Custom sort order for category display (lower values appear first)"
        example: "10"
        # Advanced filters
        filters:
          - type: OrderFilter
          - type: NumericFilter
      allowMultiple:
        description: "Whether events can be assigned to multiple categories"
        example: "true"
      active:
        description: "Whether this category is active and available for use"
        example: "true"
        # Advanced filters
        filters:
          - type: OrderFilter
          - type: BooleanFilter
      events:
        writable: false
        description: "Collection of events assigned to this category"
        example: "[\"\/api\/events\/0199cadd-64e7-73cf-9c8a-4a4dfe0a8ecf\"]"
