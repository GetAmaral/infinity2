# API Platform 4 Configuration for Calendar
# This file is ALWAYS regenerated by Genmax. DO NOT edit manually.
# @generated by Genmax Code Generator

resources:
  App\Entity\Calendar:
    shortName: Calendar
    description: "Calendars for organizing events and meetings"

    # DTO configuration - processor is set per-operation to avoid affecting DELETE
    output: App\Dto\CalendarOutputDto

    normalizationContext:
      groups: ["calendar:read"]

    denormalizationContext:
      groups: ["calendar:write"]

    order:
      createdAt: DESC

    security: "is_granted('ROLE_EVENT_MANAGER')"

    operations:
      - class: ApiPlatform\Metadata\GetCollection
        security: "is_granted('ROLE_EVENT_MANAGER')"
      - class: ApiPlatform\Metadata\Get
        security: "is_granted('ROLE_EVENT_MANAGER')"
      - class: ApiPlatform\Metadata\Post
        input: App\Dto\CalendarInputDto
        processor: App\State\CalendarProcessor
        security: "is_granted('ROLE_EVENT_MANAGER')"
      - class: ApiPlatform\Metadata\Put
        input: App\Dto\CalendarInputDto
        processor: App\State\CalendarProcessor
        security: "is_granted('ROLE_EVENT_MANAGER')"
      - class: ApiPlatform\Metadata\Patch
        input: App\Dto\CalendarInputDto
        processor: App\State\CalendarProcessor
        security: "is_granted('ROLE_EVENT_MANAGER')"
      - class: ApiPlatform\Metadata\Delete
        security: "is_granted('ROLE_EVENT_MANAGER')"


    # Property-level configuration
    properties:
      organization:
        description: "Organization"
        example: "/api/organizatia/01234567-89ab-cdef-0123-456789abcdef"
      name:
        description: "The brand name"
        example: "Nike"
      description:
        description: "..."
        example: "..."
      user:
        description: "User"
        example: "sample value"
      timeZone:
        description: "TimeZone (text field)"
        example: "Sample longer text value"
      color:
        description: "Color (text field)"
        example: "Sample text"
      primary:
        description: "Primary (yes\/no)"
        example: "true"
      visible:
        description: "Visible (yes\/no)"
        example: "true"
      accessRole:
        description: "AccessRole (text field)"
        example: "Sample longer text value"
      calendarType:
        description: "CalendarType"
        example: "sample value"
      events:
        description: "Events"
        example: "sample value"
      externalLink:
        description: "ExternalLink"
        example: "sample value"
      externalApiKey:
        description: "ExternalApiKey (text field)"
        example: "Sample longer text value"
      workingHours:
        description: "WorkingHours"
        example: "sample value"
      holidays:
        description: "Holidays"
        example: "sample value"
      default:
        description: "Is Default Calendar (yes\/no)"
        example: "true"
      active:
        description: "Is Active (yes\/no)"
        example: "true"
      public:
        description: "Is Public (yes\/no)"
        example: "true"
      icon:
        description: "Icon (text field)"
        example: "Sample longer text value"
      sortOrder:
        description: "Sort Order (numeric value)"
        example: "42"
      externalId:
        description: "External ID (text field)"
        example: "Sample longer text value"
      lastSyncedAt:
        writable: false
        description: "Last Synced At"
        example: "sample value"
      permissions:
        description: "Sharing Permissions (JSON data)"
        example: "{\"key\": \"value\"}"
      settings:
        description: "Calendar Settings (JSON data)"
        example: "{\"key\": \"value\"}"
