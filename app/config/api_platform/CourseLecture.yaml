# API Platform 4 Configuration for CourseLecture
# This file is ALWAYS regenerated by Genmax. DO NOT edit manually.
# @generated by Genmax Code Generator

resources:
  App\Entity\CourseLecture:
    shortName: CourseLecture
    description: "Individual lectures and learning content"

    # DTO configuration - processor is set per-operation to avoid affecting DELETE
    output: App\Dto\CourseLectureOutputDto

    normalizationContext:
      groups: ["course_lecture:read"]

    denormalizationContext:
      groups: ["course_lecture:write"]

    order:
      createdAt: DESC

    security: "is_granted('ROLE_ORGANIZATION_ADMIN')"

    operations:
      - class: ApiPlatform\Metadata\GetCollection
        security: "is_granted('ROLE_ORGANIZATION_ADMIN')"
      - class: ApiPlatform\Metadata\Get
        security: "is_granted('ROLE_ORGANIZATION_ADMIN')"
      - class: ApiPlatform\Metadata\Post
        input: App\Dto\CourseLectureInputDto
        processor: App\State\CourseLectureProcessor
        security: "is_granted('ROLE_ORGANIZATION_ADMIN')"
      - class: ApiPlatform\Metadata\Put
        input: App\Dto\CourseLectureInputDto
        processor: App\State\CourseLectureProcessor
        security: "is_granted('ROLE_ORGANIZATION_ADMIN')"
      - class: ApiPlatform\Metadata\Patch
        input: App\Dto\CourseLectureInputDto
        processor: App\State\CourseLectureProcessor
        security: "is_granted('ROLE_ORGANIZATION_ADMIN')"
      - class: ApiPlatform\Metadata\Delete
        security: "is_granted('ROLE_ORGANIZATION_ADMIN')"


    # Property-level configuration
    properties:
      name:
        description: "The brand name"
        example: "Nike"
        # Advanced filters
        filters:
          - type: OrderFilter
      description:
        description: "..."
        example: "..."
        # Advanced filters
        filters:
          - type: OrderFilter
      videoFileName:
        writable: false
        description: "VideoFileName (text field)"
        example: "Sample longer text value"
        # Advanced filters
        filters:
          - type: OrderFilter
      videoPath:
        writable: false
        description: "VideoPath (text field)"
        example: "Sample longer text value"
        # Advanced filters
        filters:
          - type: OrderFilter
      processingStatus:
        writable: false
        description: "ProcessingStatus (text field)"
        example: "Sample text"
        # Advanced filters
        filters:
          - type: OrderFilter
      processingStep:
        writable: false
        description: "ProcessingStep (text field)"
        example: "Sample longer text value"
        # Advanced filters
        filters:
          - type: OrderFilter
      processingPercentage:
        writable: false
        description: "ProcessingPercentage (numeric value)"
        example: "42"
        # Advanced filters
        filters:
          - type: OrderFilter
      processingError:
        writable: false
        description: "ProcessingError (long text)"
        example: "This is a longer text field with more content"
        # Advanced filters
        filters:
          - type: OrderFilter
      processedAt:
        writable: false
        description: "ProcessedAt"
        example: "sample value"
        # Advanced filters
        filters:
          - type: OrderFilter
      viewOrder:
        description: "ViewOrder (numeric value)"
        example: "42"
        # Advanced filters
        filters:
          - type: OrderFilter
      lengthSeconds:
        description: "LengthSeconds (numeric value)"
        example: "42"
        # Advanced filters
        filters:
          - type: OrderFilter
      courseModule:
        description: "CourseModule (text field)"
        example: "Sample longer text value"
        # Advanced filters
        filters:
          - type: OrderFilter
      studentLectures:
        description: "StudentLectures (text field)"
        example: "Sample longer text value"
        # Advanced filters
        filters:
          - type: OrderFilter
      studentCoursesOnThisLecture:
        description: "StudentCoursesOnThisLecture (text field)"
        example: "Sample longer text value"
        # Advanced filters
        filters:
          - type: OrderFilter
      organization:
        writable: false
        description: "Organization"
        example: "/api/organizatia/01234567-89ab-cdef-0123-456789abcdef"
      active:
        description: "Active"
        example: true
      published:
        description: "Published"
        example: true
      free:
        description: "Free"
        example: true
      publishedAt:
        writable: false
        description: "Published At"
        example: "2025-01-15T10:30:00Z"
      videoUrl:
        description: "Video URL"
        example: "Sample video url"
      videoType:
        description: "Video Type"
        example: "Sample video type"
      videoResolution:
        writable: false
        description: "Video Resolution"
        example: "Sample video resolution"
      videoSizeBytes:
        writable: false
        description: "Video Size (Bytes)"
      transcript:
        description: "Transcript"
        example: "Sample transcript"
      subtitleUrl:
        description: "Subtitle URL"
        example: "Sample subtitle url"
      subtitleLanguage:
        description: "Subtitle Language"
        example: "Sample subtitle language"
      attachments:
        description: "Attachments"
        example: "{\"key\": \"value\"}"
      externalLinks:
        description: "External Links"
        example: "{\"key\": \"value\"}"
      learningObjectives:
        description: "Learning Objectives"
        example: "{\"key\": \"value\"}"
      prerequisites:
        description: "Prerequisites"
        example: "Sample prerequisites"
      viewCount:
        writable: false
        description: "View Count"
        example: 42
      completionCount:
        writable: false
        description: "Completion Count"
        example: 42
      averageWatchPercentage:
        writable: false
        description: "Average Watch %"
        example: 99.99
      rating:
        writable: false
        description: "Rating"
        example: 99.99
      ratingCount:
        writable: false
        description: "Rating Count"
        example: 42
      durationSeconds:
        description: "Duration (seconds)"
        example: 42
      difficultyLevel:
        description: "Difficulty Level"
        example: "Sample difficulty level"
      tags:
        description: "Tags"
        example: "{\"key\": \"value\"}"
      skillsCovered:
        description: "Skills Covered"
        example: "{\"key\": \"value\"}"
      pointsValue:
        description: "Points Value"
        example: 42
      badges:
        writable: false
        description: "Badges"
        example: "{\"key\": \"value\"}"
