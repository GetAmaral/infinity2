# API Platform 4 Configuration for Contact
# This file is ALWAYS regenerated by Genmax. DO NOT edit manually.
# @generated by Genmax Code Generator

resources:
  App\Entity\Contact:
    shortName: Contact
    description: "Customer contacts with full profile and interaction history"

    # DTO configuration - processor is set per-operation to avoid affecting DELETE
    output: App\Dto\ContactOutputDto

    normalizationContext:
      groups: ["contact:read"]

    denormalizationContext:
      groups: ["contact:write"]

    order:
      createdAt: DESC

    security: "is_granted('ROLE_SALES_MANAGER')"

    operations:
      - class: ApiPlatform\Metadata\GetCollection
        security: "is_granted('ROLE_SALES_MANAGER')"
      - class: ApiPlatform\Metadata\Get
        security: "is_granted('ROLE_SALES_MANAGER')"
      - class: ApiPlatform\Metadata\Post
        input: App\Dto\ContactInputDto
        processor: App\State\ContactProcessor
        security: "is_granted('ROLE_SALES_MANAGER')"
      - class: ApiPlatform\Metadata\Put
        input: App\Dto\ContactInputDto
        processor: App\State\ContactProcessor
        security: "is_granted('ROLE_SALES_MANAGER')"
      - class: ApiPlatform\Metadata\Patch
        input: App\Dto\ContactInputDto
        processor: App\State\ContactProcessor
        security: "is_granted('ROLE_SALES_MANAGER')"
      - class: ApiPlatform\Metadata\Delete
        security: "is_granted('ROLE_SALES_MANAGER')"


    # Property-level configuration
    properties:
      organization:
        description: "Organization"
        example: "/api/organizatia/01234567-89ab-cdef-0123-456789abcdef"
      name:
        description: "The brand name"
        example: "Nike"
        # Advanced filters
        filters:
          - type: OrderFilter
      firstName:
        description: "First Name (text field)"
        example: "Sample longer text value"
        # Advanced filters
        filters:
          - type: OrderFilter
      lastName:
        description: "Last Name (text field)"
        example: "Sample longer text value"
        # Advanced filters
        filters:
          - type: OrderFilter
      accountManager:
        description: "AccountManager"
        example: "sample value"
        # Advanced filters
        filters:
          - type: OrderFilter
      website:
        description: "Official website URL..."
        example: "https:\/\/www.nike.com"
      accountTeam:
        description: "AccountTeam"
        example: "sample value"
        # Advanced filters
        filters:
          - type: OrderFilter
      address:
        description: "Address (text field)"
        example: "Sample longer text value"
        # Advanced filters
        filters:
          - type: OrderFilter
      billingAddress:
        description: "BillingAddress (text field)"
        example: "Sample longer text value"
        # Advanced filters
        filters:
          - type: OrderFilter
      billingCity:
        description: "BillingCity"
        example: "sample value"
        # Advanced filters
        filters:
          - type: OrderFilter
      birthDate:
        description: "BirthDate (date)"
        example: "2025-01-15"
        # Advanced filters
        filters:
          - type: OrderFilter
      campaigns:
        description: "Campaigns"
        example: "sample value"
        # Advanced filters
        filters:
          - type: OrderFilter
      city:
        description: "City"
        example: "sample value"
        # Advanced filters
        filters:
          - type: OrderFilter
      company:
        description: "Company"
        example: "sample value"
        # Advanced filters
        filters:
          - type: OrderFilter
      mobilePhone:
        description: "Mobile Phone (text field)"
        example: "Sample text"
      deals:
        description: "Deals"
        example: "sample value"
        # Advanced filters
        filters:
          - type: OrderFilter
      title:
        description: "Job Title (text field)"
        example: "Sample longer text value"
      linkedinUrl:
        description: "LinkedIn URL (text field)"
        example: "Sample longer text value"
      emailOptOut:
        description: "Email Opt-Out (yes\/no)"
        example: "true"
      department:
        description: "Department (text field)"
        example: "Sample longer text value"
        # Advanced filters
        filters:
          - type: OrderFilter
      leadSource:
        description: "Lead Source (text field)"
        example: "Sample longer text value"
        # Advanced filters
        filters:
          - type: OrderFilter
      doNotCall:
        description: "Do Not Call (yes\/no)"
        example: "true"
      preferredContactMethod:
        description: "Preferred Contact Method (text field)"
        example: "Sample longer text value"
      lastContactDate:
        description: "Last Contact Date (date and time)"
        example: "2025-01-15T10:30:00Z"
        # Advanced filters
        filters:
          - type: OrderFilter
      document:
        description: "Document (text field)"
        example: "Sample text"
        # Advanced filters
        filters:
          - type: OrderFilter
      email:
        description: "Email address"
        example: "user@example.com"
        # Advanced filters
        filters:
          - type: OrderFilter
      eventAttendances:
        description: "EventAttendances"
        example: "sample value"
        # Advanced filters
        filters:
          - type: OrderFilter
      firstTalkDate:
        description: "FirstTalkDate (date and time)"
        example: "2025-01-15T10:30:00Z"
        # Advanced filters
        filters:
          - type: OrderFilter
      gender:
        description: "Gender (numeric value)"
        example: "42"
        # Advanced filters
        filters:
          - type: OrderFilter
      geo:
        description: "Geo (text field)"
        example: "Sample longer text value"
        # Advanced filters
        filters:
          - type: OrderFilter
      lastTalkDate:
        description: "LastTalkDate (date and time)"
        example: "2025-01-15T10:30:00Z"
        # Advanced filters
        filters:
          - type: OrderFilter
      neighborhood:
        description: "Neighborhood (text field)"
        example: "Sample longer text value"
        # Advanced filters
        filters:
          - type: OrderFilter
      nickname:
        description: "Nickname (text field)"
        example: "Sample longer text value"
        # Advanced filters
        filters:
          - type: OrderFilter
      notes:
        description: "Additional notes"
        example: "This is a longer text field with more content"
        # Advanced filters
        filters:
          - type: OrderFilter
      origin:
        description: "Origin (text field)"
        example: "Sample longer text value"
        # Advanced filters
        filters:
          - type: OrderFilter
      phone:
        description: "Phone number"
        example: "+1234567890"
        # Advanced filters
        filters:
          - type: OrderFilter
      postalCode:
        description: "PostalCode (text field)"
        example: "Sample longer text value"
        # Advanced filters
        filters:
          - type: OrderFilter
      primaryDeals:
        description: "PrimaryDeals"
        example: "sample value"
        # Advanced filters
        filters:
          - type: OrderFilter
      profilePictureUrl:
        description: "ProfilePictureUrl (text field)"
        example: "Sample longer text value"
        # Advanced filters
        filters:
          - type: OrderFilter
      ranking:
        description: "Ranking (numeric value)"
        example: "42"
        # Advanced filters
        filters:
          - type: OrderFilter
      score:
        description: "Score (numeric value)"
        example: "42"
        # Advanced filters
        filters:
          - type: OrderFilter
      socialMedias:
        description: "SocialMedias"
        example: "sample value"
        # Advanced filters
        filters:
          - type: OrderFilter
      status:
        description: "Current status"
        example: "42"
        # Advanced filters
        filters:
          - type: OrderFilter
      talks:
        description: "Talks"
        example: "sample value"
        # Advanced filters
        filters:
          - type: OrderFilter
      tasks:
        description: "..."
        example: "..."
        # Advanced filters
        filters:
          - type: OrderFilter
