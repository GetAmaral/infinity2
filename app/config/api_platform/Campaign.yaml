# API Platform 4 Configuration for Campaign
# This file is ALWAYS regenerated by Genmax. DO NOT edit manually.
# @generated by Genmax Code Generator

resources:
  App\Entity\Campaign:
    shortName: Campaign
    description: "Marketing campaigns for lead generation and customer engagement"

    # DTO configuration - processor is set per-operation to avoid affecting DELETE
    output: App\Dto\CampaignOutputDto

    normalizationContext:
      groups: ["campaign:read"]

    denormalizationContext:
      groups: ["campaign:write"]

    order:
      createdAt: DESC

    security: "is_granted('ROLE_MARKETING_ADMIN')"

    operations:
      - class: ApiPlatform\Metadata\GetCollection
        security: "is_granted('ROLE_MARKETING_ADMIN')"
      - class: ApiPlatform\Metadata\Get
        security: "is_granted('ROLE_MARKETING_ADMIN')"
      - class: ApiPlatform\Metadata\Post
        input: App\Dto\CampaignInputDto
        processor: App\State\CampaignProcessor
        security: "is_granted('ROLE_MARKETING_ADMIN')"
      - class: ApiPlatform\Metadata\Put
        input: App\Dto\CampaignInputDto
        processor: App\State\CampaignProcessor
        security: "is_granted('ROLE_MARKETING_ADMIN')"
      - class: ApiPlatform\Metadata\Patch
        input: App\Dto\CampaignInputDto
        processor: App\State\CampaignProcessor
        security: "is_granted('ROLE_MARKETING_ADMIN')"
      - class: ApiPlatform\Metadata\Delete
        security: "is_granted('ROLE_MARKETING_ADMIN')"


    # Property-level configuration
    properties:
      organization:
        description: "Organization"
        example: "/api/organizatia/01234567-89ab-cdef-0123-456789abcdef"
      name:
        description: "The brand name"
        example: "Nike"
        # Advanced filters
        filters:
          - type: OrderFilter
      description:
        description: "..."
        example: "..."
        # Advanced filters
        filters:
          - type: OrderFilter
      actualBudget:
        description: "Actual Budget (decimal value)"
        example: "99.99"
        # Advanced filters
        filters:
          - type: OrderFilter
      active:
        description: "Whether this record is active"
        example: "true"
      actualCost:
        description: "Actual Cost (decimal value)"
        example: "99.99"
      actualReturn:
        description: "Actual Return (decimal value)"
        example: "99.99"
        # Advanced filters
        filters:
          - type: OrderFilter
      actualRevenue:
        description: "Actual Revenue (decimal value)"
        example: "99.99"
      archived:
        description: "Archived (yes\/no)"
        example: "true"
      budgetedCost:
        description: "Budgeted Cost (decimal value)"
        example: "99.99"
      campaignStatus:
        description: "Campaign Status (numeric value)"
        example: "42"
        # Advanced filters
        filters:
          - type: OrderFilter
      campaignType:
        description: "Campaign Type (text field)"
        example: "Sample longer text value"
      codeName:
        description: "Code Name (text field)"
        example: "Sample longer text value"
      companies:
        description: "Companies"
        example: "sample value"
        # Advanced filters
        filters:
          - type: OrderFilter
      contacts:
        description: "Contacts"
        example: "sample value"
        # Advanced filters
        filters:
          - type: OrderFilter
      deals:
        description: "Deals"
        example: "sample value"
        # Advanced filters
        filters:
          - type: OrderFilter
      draft:
        description: "Draft (yes\/no)"
        example: "true"
      duration:
        description: "Duration (numeric value)"
        example: "42"
        # Advanced filters
        filters:
          - type: OrderFilter
      emailAddress:
        description: "Email Address (text field)"
        example: "Sample longer text value"
      endDate:
        description: "End Date (date and time)"
        example: "2025-01-15T10:30:00Z"
        # Advanced filters
        filters:
          - type: OrderFilter
      expectedResponse:
        description: "Expected Response (%) (numeric value)"
        example: "42"
      expectedRevenue:
        description: "Expected Revenue (decimal value)"
        example: "99.99"
      manager:
        description: "Manager"
        example: "sample value"
        # Advanced filters
        filters:
          - type: OrderFilter
      message:
        description: "Message (long text)"
        example: "This is a longer text field with more content"
      childCampaigns:
        writable: false
        description: "Child Campaigns"
        example: "/api/campaigns/01234567-89ab-cdef-0123-456789abcdef"
      numberOfContacts:
        description: "Number of Contacts (numeric value)"
        example: "42"
      numberOfConverted:
        description: "Number of Converted (numeric value)"
        example: "42"
      numberOfLeads:
        description: "Number of Leads (numeric value)"
        example: "42"
      numberOfResponses:
        description: "Number of Responses (numeric value)"
        example: "42"
      objective:
        description: "Objective (long text)"
        example: "This is a longer text field with more content"
      otherCost:
        description: "Other Cost (decimal value)"
        example: "99.99"
      owner:
        description: "Owner"
        example: "sample value"
      parentCampaign:
        description: "Parent Campaign"
        example: "sample value"
      plannedBudget:
        description: "Planned Budget (decimal value)"
        example: "99.99"
        # Advanced filters
        filters:
          - type: OrderFilter
      plannedDuration:
        description: "Planned Duration (numeric value)"
        example: "42"
        # Advanced filters
        filters:
          - type: OrderFilter
      plannedEndDate:
        description: "Planned End Date (date and time)"
        example: "2025-01-15T10:30:00Z"
        # Advanced filters
        filters:
          - type: OrderFilter
      plannedReturn:
        description: "Planned Return (decimal value)"
        example: "99.99"
        # Advanced filters
        filters:
          - type: OrderFilter
      plannedStartDate:
        description: "Planned Start Date (date and time)"
        example: "2025-01-15T10:30:00Z"
        # Advanced filters
        filters:
          - type: OrderFilter
      roi:
        description: "ROI (decimal value)"
        example: "99.99"
        # Advanced filters
        filters:
          - type: OrderFilter
      socialMedias:
        description: "Social Medias"
        example: "sample value"
        # Advanced filters
        filters:
          - type: OrderFilter
      startDate:
        description: "Start Date (date and time)"
        example: "2025-01-15T10:30:00Z"
        # Advanced filters
        filters:
          - type: OrderFilter
      status:
        description: "Current status"
        example: "Sample longer text value"
      talks:
        description: "Talks"
        example: "sample value"
        # Advanced filters
        filters:
          - type: OrderFilter
      targetAudience:
        description: "Target Audience (long text)"
        example: "This is a longer text field with more content"
      team:
        description: "Team"
        example: "sample value"
        # Advanced filters
        filters:
          - type: OrderFilter
      template:
        description: "Is Template (yes\/no)"
        example: "true"
