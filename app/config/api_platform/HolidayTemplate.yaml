# API Platform 4 Configuration for HolidayTemplate
# This file is ALWAYS regenerated by Genmax. DO NOT edit manually.
# @generated by Genmax Code Generator

resources:
  App\Entity\HolidayTemplate:
    shortName: HolidayTemplate
    description: "Templates for holidays (National, Regional, Company, etc.)"

    # DTO configuration - processor is set per-operation to avoid affecting DELETE
    output: App\Dto\HolidayTemplateOutputDto

    normalizationContext:
      groups: ["holidaytemplate:read"]

    denormalizationContext:
      groups: ["holidaytemplate:write"]

    order:
      createdAt: DESC

    security: "is_granted('ROLE_SUPER_ADMIN')"

    operations:
      - class: ApiPlatform\Metadata\GetCollection
        security: "is_granted('ROLE_SUPER_ADMIN')"
      - class: ApiPlatform\Metadata\Get
        security: "is_granted('ROLE_SUPER_ADMIN')"
      - class: ApiPlatform\Metadata\Post
        input: App\Dto\HolidayTemplateInputDto
        processor: App\State\HolidayTemplateProcessor
        security: "is_granted('ROLE_SUPER_ADMIN')"
      - class: ApiPlatform\Metadata\Put
        input: App\Dto\HolidayTemplateInputDto
        processor: App\State\HolidayTemplateProcessor
        security: "is_granted('ROLE_SUPER_ADMIN')"
      - class: ApiPlatform\Metadata\Patch
        input: App\Dto\HolidayTemplateInputDto
        processor: App\State\HolidayTemplateProcessor
        security: "is_granted('ROLE_SUPER_ADMIN')"
      - class: ApiPlatform\Metadata\Delete
        security: "is_granted('ROLE_SUPER_ADMIN')"


    # Property-level configuration
    properties:
      name:
        description: "The brand name"
        example: "Nike"
        # Advanced filters
        filters:
          - type: OrderFilter
      blocksScheduling:
        description: "Blocks Scheduling (yes\/no)"
        example: "true"
        # Advanced filters
        filters:
          - type: OrderFilter
      city:
        description: "City"
        example: "sample value"
        # Advanced filters
        filters:
          - type: OrderFilter
      country:
        description: "Country"
        example: "sample value"
        # Advanced filters
        filters:
          - type: OrderFilter
      recurrenceFrequency:
        description: "RecurrenceFrequency (numeric value)"
        example: "42"
        # Advanced filters
        filters:
          - type: OrderFilter
      recurrenceInterval:
        description: "RecurrenceInterval (numeric value)"
        example: "42"
        # Advanced filters
        filters:
          - type: OrderFilter
      sentAt:
        description: "Sent At (date)"
        example: "2025-01-15"
        # Advanced filters
        filters:
          - type: OrderFilter
