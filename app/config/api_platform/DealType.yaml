# API Platform 4 Configuration for DealType
# This file is ALWAYS regenerated by Genmax. DO NOT edit manually.
# @generated by Genmax Code Generator

resources:
  App\Entity\DealType:
    shortName: DealType
    description: "Deal types for categorizing opportunities"

    normalizationContext:
      groups: ["dealtype:read"]

    denormalizationContext:
      groups: ["dealtype:write"]

    order:
      createdAt: DESC

    security: "is_granted('ROLE_CRM_ADMIN')"

    operations:
      - class: ApiPlatform\Metadata\GetCollection
        security: "is_granted('ROLE_CRM_ADMIN')"
      - class: ApiPlatform\Metadata\Get
        security: "is_granted('ROLE_CRM_ADMIN')"
      - class: ApiPlatform\Metadata\Post
        security: "is_granted('ROLE_CRM_ADMIN')"
      - class: ApiPlatform\Metadata\Put
        security: "is_granted('ROLE_CRM_ADMIN')"
      - class: ApiPlatform\Metadata\Patch
        security: "is_granted('ROLE_CRM_ADMIN')"
      - class: ApiPlatform\Metadata\Delete
        security: "is_granted('ROLE_CRM_ADMIN')"


    # Property-level configuration
    properties:
      organization:
        description: "Organization"
        example: "/api/organizations/01234567-89ab-cdef-0123-456789abcdef"
      name:
        description: "The brand name"
        example: "Nike"
      description:
        description: "..."
        example: "..."
      category:
        description: "High-level business category for grouping and reporting on deal types"
        example: "New Business"
      color:
        description: "Hex color code used for visual identification of this deal type in dashboards and reports"
        example: "#6366f1"
      deals:
        writable: false
        description: "Collection of deals associated with this deal type"
        example: "[]"
      icon:
        description: "Bootstrap icon class name for visual representation in the UI"
        example: "bi-briefcase"
      default:
        description: "Indicates if this is the default deal type to be pre-selected when creating new deals"
        example: "false"
      active:
        description: "Indicates if this deal type is active and available for use in the system"
        example: "true"
      sortOrder:
        description: "Numeric value determining the display order of deal types in lists and selection fields"
        example: "10"
      expectedDuration:
        description: "Expected sales cycle duration in days for deals of this type, used for forecasting"
        example: "45"
      winProbability:
        description: "Historical or expected win probability percentage for deals of this type, used for weighted forecasting"
        example: "35.50"
