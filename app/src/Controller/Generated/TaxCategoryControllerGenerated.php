<?php

declare(strict_types=1);

namespace App\Controller\Generated;

use App\Controller\Base\BaseApiController;
use App\Entity\TaxCategory;
use App\Repository\TaxCategoryRepository;
use App\Security\Voter\TaxCategoryVoter;
use App\Form\TaxCategoryFormType;
use App\Service\ListPreferencesService;
use Doctrine\ORM\EntityManagerInterface;
use Symfony\Component\HttpFoundation\Request;
use Symfony\Component\HttpFoundation\Response;
use Symfony\Component\HttpFoundation\JsonResponse;
use Symfony\Component\Security\Csrf\CsrfToken;
use Symfony\Component\Security\Csrf\CsrfTokenManagerInterface;
use Symfony\Component\Security\Core\Exception\InvalidCsrfTokenException;
use Symfony\Contracts\Translation\TranslatorInterface;

/**
 * Generated Base Controller for TaxCategory
 *
 * ⚠️ WARNING: This file is ALWAYS regenerated by Genmax
 * DO NOT EDIT THIS FILE - Edit TaxCategoryController instead
 *
 * @generated
 * @codeCoverageIgnore
 */
abstract class TaxCategoryControllerGenerated extends BaseApiController
{
    public function __construct(
        protected readonly EntityManagerInterface $entityManager,
        protected readonly TaxCategoryRepository $repository,
        protected readonly ListPreferencesService $listPreferencesService,
        protected readonly TranslatorInterface $translator,
        protected readonly CsrfTokenManagerInterface $csrfTokenManager,
    ) {}

    // ====================================
    // ABSTRACT METHOD IMPLEMENTATIONS
    // ====================================

    /**
     * Get the repository for this entity
     */
    protected function getRepository(): TaxCategoryRepository
    {
        return $this->repository;
    }

    /**
     * Get the plural name for this entity
     */
    protected function getEntityPluralName(): string
    {
        return 'ries';
    }

    /**
     * Convert entity to array for API responses
     *
     * ⚠️ IMPORTANT: Enum Properties Stored as Strings
     * -------------------------------------------------
     * Properties with enum-backed types (like InputType) are stored as STRINGS
     * in the database and getters return strings directly, NOT enum objects.
     *
     * ❌ WRONG:   $entity->getType()->value   (Error: "Attempt to read property 'value' on string")
     * ✅ CORRECT: $entity->getType()          (Already a string - 'fully_completed')
     *
     * If you need the enum object for business logic, use:
     *    InputType::from($entity->getType())
     */
    protected function entityToArray(object $entity): array
    {
        if (!$entity instanceof TaxCategory) {
            throw new \InvalidArgumentException('Expected TaxCategory instance');
        }

        return [
            'id' => $entity->getId()->toString(),
            'organization' => ($organizationRel = $entity->getOrganization()) ? [
                'id' => $organizationRel->getId()->toString(),
                'display' => (string) $organizationRel,
            ] : null,
            'name' => $entity->getName(),
            'description' => $entity->getDescription(),
            'taxRate' => $entity->getTaxRate(),
            'products' => ($productsRel = $entity->getProducts()) ? [
                'id' => $productsRel->getId()->toString(),
                'display' => (string) $productsRel,
            ] : null,
        ];
    }

    // ====================================
    // INDEX ACTION - List all ries
    // ====================================

    /**
     * List ries
     */
    protected function indexAction(): Response
    {
        $this->denyAccessUnlessGranted(TaxCategoryVoter::LIST);

        $preferences = $this->listPreferencesService->getEntityPreferences('ries');
        $savedView = $preferences['view'] ?? 'grid';

        return $this->render('taxcategory/index.html.twig', [
            'entities' => [],  // Loaded via API
            'entity_name' => 'taxCategory',
            'entity_name_plural' => 'ries',
            'page_icon' => 'bi-percent',
            'default_view' => $savedView,

            // List configuration from GeneratorProperty
            'enable_search' => true,
            'enable_filters' => false,
            'enable_sorting' => true,
            'enable_create_button' => true,

            // Property metadata for client-side rendering (as JSON strings)
            'list_fields' => '[{"name":"name","label":"Name","type":"string","sortable":true,"searchable":true,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getName"},{"name":"description","label":"Description","type":"text","sortable":true,"searchable":true,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getDescription"},{"name":"taxRate","label":"TaxRate","type":"decimal","sortable":true,"searchable":false,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getTaxRate"},{"name":"products","label":"Products","type":"","sortable":true,"searchable":false,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getProducts"}]',
            'searchable_fields' => '[{"name":"name","label":"Name","type":"string"},{"name":"description","label":"Description","type":"text"}]',
            'filterable_fields' => '[]',
            'sortable_fields' => '[{"name":"name","label":"Name"},{"name":"description","label":"Description"},{"name":"taxRate","label":"TaxRate"},{"name":"products","label":"Products"}]',
        ]);
    }

    // ====================================
    // API SEARCH - Fetch ries for list view
    // ====================================

    /**
     * API endpoint for searching ries
     */
    protected function apiSearchAction(Request $request): JsonResponse
    {
        $this->denyAccessUnlessGranted(TaxCategoryVoter::LIST);

        // This method uses the BaseApiController's handleSearchRequest
        // which integrates with API Platform's GetCollection operation
        return $this->handleSearchRequest($request);
    }

    // ====================================
    // NEW FORM ACTION - Display create form
    // ====================================

    /**
     * Display new taxCategory form
     */
    protected function newFormAction(Request $request): Response
    {
        $this->denyAccessUnlessGranted(TaxCategoryVoter::CREATE);

        $taxCategory = new TaxCategory();

        // Initialize with custom logic if needed
        $this->initializeNewEntity($taxCategory);

        $form = $this->createForm(TaxCategoryFormType::class, $taxCategory);

        return $this->render('taxcategory/_form_modal.html.twig', [
            'form' => $form,
            'taxCategory' => $taxCategory,
            'is_edit' => false,
        ]);
    }

    // ====================================
    // CREATE ACTION - Process create form submission
    // ====================================

    /**
     * Process new taxCategory form submission
     */
    protected function createAction(Request $request): Response
    {
        $this->denyAccessUnlessGranted(TaxCategoryVoter::CREATE);

        $taxCategory = new TaxCategory();

        // Initialize with custom logic if needed
        $this->initializeNewEntity($taxCategory);

        $form = $this->createForm(TaxCategoryFormType::class, $taxCategory);
        $form->handleRequest($request);

        if ($form->isSubmitted() && $form->isValid()) {
            try {
                // Before create hook
                $this->beforeCreate($taxCategory);

                $this->entityManager->persist($taxCategory);
                $this->entityManager->flush();

                // After create hook
                $this->afterCreate($taxCategory);

                $this->addFlash('success', $this->translator->trans(
                    'taxcategory.flash.created_successfully',
                    ['%name%' => (string) $taxCategory],
                    'taxcategory'
                ));

                return $this->redirectToRoute('taxcategory_index', [], Response::HTTP_SEE_OTHER);

            } catch (\Exception $e) {
                $this->addFlash('error', $this->translator->trans(
                    'taxcategory.flash.create_failed',
                    ['%error%' => $e->getMessage()],
                    'taxcategory'
                ));
            }
        }

        // Re-render with errors
        return $this->render('taxcategory/_form_modal.html.twig', [
            'form' => $form,
            'taxCategory' => $taxCategory,
            'is_edit' => false,
        ], new Response('', $form->isSubmitted() ? Response::HTTP_UNPROCESSABLE_ENTITY : Response::HTTP_OK));
    }

    // ====================================
    // EDIT FORM ACTION - Display edit form
    // ====================================

    /**
     * Display edit taxCategory form
     */
    protected function editFormAction(TaxCategory $taxCategory, Request $request): Response
    {
        $this->denyAccessUnlessGranted(TaxCategoryVoter::EDIT, $taxCategory);

        $form = $this->createForm(TaxCategoryFormType::class, $taxCategory);

        return $this->render('taxcategory/_form_modal.html.twig', [
            'form' => $form,
            'taxCategory' => $taxCategory,
            'is_edit' => true,
        ]);
    }

    // ====================================
    // UPDATE ACTION - Process edit form submission
    // ====================================

    /**
     * Process edit taxCategory form submission
     */
    protected function updateAction(TaxCategory $taxCategory, Request $request): Response
    {
        $this->denyAccessUnlessGranted(TaxCategoryVoter::EDIT, $taxCategory);

        $form = $this->createForm(TaxCategoryFormType::class, $taxCategory);
        $form->handleRequest($request);

        if ($form->isSubmitted() && $form->isValid()) {
            try {
                // Before update hook
                $this->beforeUpdate($taxCategory);

                $this->entityManager->flush();

                // After update hook
                $this->afterUpdate($taxCategory);

                $this->addFlash('success', $this->translator->trans(
                    'taxcategory.flash.updated_successfully',
                    ['%name%' => (string) $taxCategory],
                    'taxcategory'
                ));

                return $this->redirectToRoute('taxcategory_index', [], Response::HTTP_SEE_OTHER);

            } catch (\Exception $e) {
                $this->addFlash('error', $this->translator->trans(
                    'taxcategory.flash.update_failed',
                    ['%error%' => $e->getMessage()],
                    'taxcategory'
                ));
            }
        }

        // Re-render with errors
        return $this->render('taxcategory/_form_modal.html.twig', [
            'form' => $form,
            'taxCategory' => $taxCategory,
            'is_edit' => true,
        ], new Response('', $form->isSubmitted() ? Response::HTTP_UNPROCESSABLE_ENTITY : Response::HTTP_OK));
    }

    // ====================================
    // DELETE ACTION - Delete taxCategory
    // ====================================

    /**
     * Delete taxCategory
     */
    protected function deleteAction(TaxCategory $taxCategory, Request $request): Response
    {
        $this->denyAccessUnlessGranted(TaxCategoryVoter::DELETE, $taxCategory);

        // Verify CSRF token
        $token = $request->request->get('_token');
        if (!$this->csrfTokenManager->isTokenValid(new CsrfToken('delete_taxCategory_' . $taxCategory->getId()->toString(), $token))) {
            throw new InvalidCsrfTokenException('Invalid CSRF token for delete operation.');
        }

        try {
            // Before delete hook
            $this->beforeDelete($taxCategory);

            $name = (string) $taxCategory;

            $this->entityManager->remove($taxCategory);
            $this->entityManager->flush();

            // After delete hook
            $this->afterDelete();

            $this->addFlash('success', $this->translator->trans(
                'taxcategory.flash.deleted_successfully',
                ['%name%' => $name],
                'taxcategory'
            ));

        } catch (\Exception $e) {
            $this->addFlash('error', $this->translator->trans(
                'taxcategory.flash.delete_failed',
                ['%error%' => $e->getMessage()],
                'taxcategory'
            ));
        }

        return $this->redirectToRoute('taxcategory_index', [], Response::HTTP_SEE_OTHER);
    }

    // ====================================
    // SHOW ACTION - Display taxCategory details
    // ====================================

    /**
     * Display taxCategory details
     */
    protected function showAction(TaxCategory $taxCategory): Response
    {
        $this->denyAccessUnlessGranted(TaxCategoryVoter::VIEW, $taxCategory);

        return $this->render('taxcategory/show.html.twig', [
            'taxCategory' => $taxCategory,
        ]);
    }

    // ====================================
    // LIFECYCLE HOOKS
    // ====================================
    // Override these in TaxCategoryController to add custom logic

    /**
     * Initialize new entity before creating form
     *
     * Note: Organization and Owner are set automatically by TenantEntityProcessor
     * Only use this for custom initialization logic
     */
    protected function initializeNewEntity(TaxCategory $taxCategory): void
    {
        // Organization and Owner are set automatically by TenantEntityProcessor
        // Add your custom initialization here
    }

    /**
     * Execute logic before creating entity
     */
    protected function beforeCreate(TaxCategory $taxCategory): void
    {
        // Override to add logic before create
    }

    /**
     * Execute logic after creating entity
     */
    protected function afterCreate(TaxCategory $taxCategory): void
    {
        // Override to add logic after create (e.g., send notification, trigger event)
    }

    /**
     * Execute logic before updating entity
     */
    protected function beforeUpdate(TaxCategory $taxCategory): void
    {
        // Override to add logic before update
    }

    /**
     * Execute logic after updating entity
     */
    protected function afterUpdate(TaxCategory $taxCategory): void
    {
        // Override to add logic after update (e.g., send notification, trigger event)
    }

    /**
     * Execute logic before deleting entity
     */
    protected function beforeDelete(TaxCategory $taxCategory): void
    {
        // Override to add logic before delete (e.g., check dependencies, archive)
    }

    /**
     * Execute logic after deleting entity
     */
    protected function afterDelete(): void
    {
        // Override to add logic after delete (e.g., cleanup, send notification)
    }
}
