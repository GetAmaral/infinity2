<?php

declare(strict_types=1);

namespace App\Controller\Generated;

use App\Controller\Base\BaseApiController;
use App\Entity\TaskType;
use App\Repository\TaskTypeRepository;
use App\Security\Voter\TaskTypeVoter;
use App\Form\TaskTypeFormType;
use App\Service\ListPreferencesService;
use Doctrine\ORM\EntityManagerInterface;
use Symfony\Component\HttpFoundation\Request;
use Symfony\Component\HttpFoundation\Response;
use Symfony\Component\HttpFoundation\JsonResponse;
use Symfony\Component\Security\Csrf\CsrfToken;
use Symfony\Component\Security\Csrf\CsrfTokenManagerInterface;
use Symfony\Component\Security\Core\Exception\InvalidCsrfTokenException;
use Symfony\Contracts\Translation\TranslatorInterface;

/**
 * Generated Base Controller for TaskType
 *
 * ⚠️ WARNING: This file is ALWAYS regenerated by Genmax
 * DO NOT EDIT THIS FILE - Edit TaskTypeController instead
 *
 * @generated
 * @codeCoverageIgnore
 */
abstract class TaskTypeControllerGenerated extends BaseApiController
{
    public function __construct(
        protected readonly EntityManagerInterface $entityManager,
        protected readonly TaskTypeRepository $repository,
        protected readonly ListPreferencesService $listPreferencesService,
        protected readonly TranslatorInterface $translator,
        protected readonly CsrfTokenManagerInterface $csrfTokenManager,
    ) {}

    // ====================================
    // ABSTRACT METHOD IMPLEMENTATIONS
    // ====================================

    /**
     * Get the repository for this entity
     */
    protected function getRepository(): TaskTypeRepository
    {
        return $this->repository;
    }

    /**
     * Get the plural name for this entity
     */
    protected function getEntityPluralName(): string
    {
        return 'taskTypes';
    }

    /**
     * Convert entity to array for API responses
     */
    protected function entityToArray(object $entity): array
    {
        if (!$entity instanceof TaskType) {
            throw new \InvalidArgumentException('Expected TaskType instance');
        }

        return [
            'id' => $entity->getId()->toString(),
            'organization' => ($organizationRel = $entity->getOrganization()) ? [
                'id' => $organizationRel->getId()->toString(),
                'display' => (string) $organizationRel,
            ] : null,
            'name' => $entity->getName(),
            'taskFunction' => $entity->getTaskFunction(),
            'taskTemplates' => ($taskTemplatesRel = $entity->getTaskTemplates()) ? [
                'id' => $taskTemplatesRel->getId()->toString(),
                'display' => (string) $taskTemplatesRel,
            ] : null,
            'tasks' => ($tasksRel = $entity->getTasks()) ? [
                'id' => $tasksRel->getId()->toString(),
                'display' => (string) $tasksRel,
            ] : null,
        ];
    }

    // ====================================
    // INDEX ACTION - List all TaskTypes
    // ====================================

    /**
     * List TaskTypes
     */
    protected function indexAction(): Response
    {
        $this->denyAccessUnlessGranted(TaskTypeVoter::LIST);

        $preferences = $this->listPreferencesService->getEntityPreferences('taskTypes');
        $savedView = $preferences['view'] ?? 'grid';

        return $this->render('tasktype/index.html.twig', [
            'entities' => [],  // Loaded via API
            'entity_name' => 'taskType',
            'entity_name_plural' => 'taskTypes',
            'page_icon' => 'bi-list-check',
            'default_view' => $savedView,

            // List configuration from GeneratorProperty
            'enable_search' => true,
            'enable_filters' => false,
            'enable_sorting' => true,
            'enable_create_button' => true,

            // Property metadata for client-side rendering (as JSON strings)
            'list_fields' => '[{"name":"name","label":"Name","type":"string","sortable":true,"searchable":true,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getName"},{"name":"taskFunction","label":"TaskFunction","type":"integer","sortable":true,"searchable":false,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getTaskFunction"},{"name":"taskTemplates","label":"TaskTemplates","type":"","sortable":true,"searchable":false,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getTaskTemplates"},{"name":"tasks","label":"Tasks","type":"","sortable":true,"searchable":false,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getTasks"}]',
            'searchable_fields' => '[{"name":"name","label":"Name","type":"string"}]',
            'filterable_fields' => '[]',
            'sortable_fields' => '[{"name":"name","label":"Name"},{"name":"taskFunction","label":"TaskFunction"},{"name":"taskTemplates","label":"TaskTemplates"},{"name":"tasks","label":"Tasks"}]',
        ]);
    }

    // ====================================
    // API SEARCH - Fetch TaskTypes for list view
    // ====================================

    /**
     * API endpoint for searching TaskTypes
     */
    protected function apiSearchAction(Request $request): JsonResponse
    {
        $this->denyAccessUnlessGranted(TaskTypeVoter::LIST);

        // This method uses the BaseApiController's handleSearchRequest
        // which integrates with API Platform's GetCollection operation
        return $this->handleSearchRequest($request);
    }

    // ====================================
    // NEW FORM ACTION - Display create form
    // ====================================

    /**
     * Display new taskType form
     */
    protected function newFormAction(Request $request): Response
    {
        $this->denyAccessUnlessGranted(TaskTypeVoter::CREATE);

        $taskType = new TaskType();

        // Initialize with custom logic if needed
        $this->initializeNewEntity($taskType);

        $form = $this->createForm(TaskTypeFormType::class, $taskType);

        return $this->render('tasktype/_form_modal.html.twig', [
            'form' => $form,
            'taskType' => $taskType,
            'is_edit' => false,
        ]);
    }

    // ====================================
    // CREATE ACTION - Process create form submission
    // ====================================

    /**
     * Process new taskType form submission
     */
    protected function createAction(Request $request): Response
    {
        $this->denyAccessUnlessGranted(TaskTypeVoter::CREATE);

        $taskType = new TaskType();

        // Initialize with custom logic if needed
        $this->initializeNewEntity($taskType);

        $form = $this->createForm(TaskTypeFormType::class, $taskType);
        $form->handleRequest($request);

        if ($form->isSubmitted() && $form->isValid()) {
            try {
                // Before create hook
                $this->beforeCreate($taskType);

                $this->entityManager->persist($taskType);
                $this->entityManager->flush();

                // After create hook
                $this->afterCreate($taskType);

                $this->addFlash('success', $this->translator->trans(
                    'tasktype.flash.created_successfully',
                    ['%name%' => (string) $taskType],
                    'tasktype'
                ));

                return $this->redirectToRoute('tasktype_index', [], Response::HTTP_SEE_OTHER);

            } catch (\Exception $e) {
                $this->addFlash('error', $this->translator->trans(
                    'tasktype.flash.create_failed',
                    ['%error%' => $e->getMessage()],
                    'tasktype'
                ));
            }
        }

        // Re-render with errors
        return $this->render('tasktype/_form_modal.html.twig', [
            'form' => $form,
            'taskType' => $taskType,
            'is_edit' => false,
        ], new Response('', $form->isSubmitted() ? Response::HTTP_UNPROCESSABLE_ENTITY : Response::HTTP_OK));
    }

    // ====================================
    // EDIT FORM ACTION - Display edit form
    // ====================================

    /**
     * Display edit taskType form
     */
    protected function editFormAction(TaskType $taskType, Request $request): Response
    {
        $this->denyAccessUnlessGranted(TaskTypeVoter::EDIT, $taskType);

        $form = $this->createForm(TaskTypeFormType::class, $taskType);

        return $this->render('tasktype/_form_modal.html.twig', [
            'form' => $form,
            'taskType' => $taskType,
            'is_edit' => true,
        ]);
    }

    // ====================================
    // UPDATE ACTION - Process edit form submission
    // ====================================

    /**
     * Process edit taskType form submission
     */
    protected function updateAction(TaskType $taskType, Request $request): Response
    {
        $this->denyAccessUnlessGranted(TaskTypeVoter::EDIT, $taskType);

        $form = $this->createForm(TaskTypeFormType::class, $taskType);
        $form->handleRequest($request);

        if ($form->isSubmitted() && $form->isValid()) {
            try {
                // Before update hook
                $this->beforeUpdate($taskType);

                $this->entityManager->flush();

                // After update hook
                $this->afterUpdate($taskType);

                $this->addFlash('success', $this->translator->trans(
                    'tasktype.flash.updated_successfully',
                    ['%name%' => (string) $taskType],
                    'tasktype'
                ));

                return $this->redirectToRoute('tasktype_index', [], Response::HTTP_SEE_OTHER);

            } catch (\Exception $e) {
                $this->addFlash('error', $this->translator->trans(
                    'tasktype.flash.update_failed',
                    ['%error%' => $e->getMessage()],
                    'tasktype'
                ));
            }
        }

        // Re-render with errors
        return $this->render('tasktype/_form_modal.html.twig', [
            'form' => $form,
            'taskType' => $taskType,
            'is_edit' => true,
        ], new Response('', $form->isSubmitted() ? Response::HTTP_UNPROCESSABLE_ENTITY : Response::HTTP_OK));
    }

    // ====================================
    // DELETE ACTION - Delete taskType
    // ====================================

    /**
     * Delete taskType
     */
    protected function deleteAction(TaskType $taskType, Request $request): Response
    {
        $this->denyAccessUnlessGranted(TaskTypeVoter::DELETE, $taskType);

        // Verify CSRF token
        $token = $request->request->get('_token');
        if (!$this->csrfTokenManager->isTokenValid(new CsrfToken('delete_taskType_' . $taskType->getId()->toString(), $token))) {
            throw new InvalidCsrfTokenException('Invalid CSRF token for delete operation.');
        }

        try {
            // Before delete hook
            $this->beforeDelete($taskType);

            $name = (string) $taskType;

            $this->entityManager->remove($taskType);
            $this->entityManager->flush();

            // After delete hook
            $this->afterDelete();

            $this->addFlash('success', $this->translator->trans(
                'tasktype.flash.deleted_successfully',
                ['%name%' => $name],
                'tasktype'
            ));

        } catch (\Exception $e) {
            $this->addFlash('error', $this->translator->trans(
                'tasktype.flash.delete_failed',
                ['%error%' => $e->getMessage()],
                'tasktype'
            ));
        }

        return $this->redirectToRoute('tasktype_index', [], Response::HTTP_SEE_OTHER);
    }

    // ====================================
    // SHOW ACTION - Display taskType details
    // ====================================

    /**
     * Display taskType details
     */
    protected function showAction(TaskType $taskType): Response
    {
        $this->denyAccessUnlessGranted(TaskTypeVoter::VIEW, $taskType);

        return $this->render('tasktype/show.html.twig', [
            'taskType' => $taskType,
        ]);
    }

    // ====================================
    // LIFECYCLE HOOKS
    // ====================================
    // Override these in TaskTypeController to add custom logic

    /**
     * Initialize new entity before creating form
     *
     * Note: Organization and Owner are set automatically by TenantEntityProcessor
     * Only use this for custom initialization logic
     */
    protected function initializeNewEntity(TaskType $taskType): void
    {
        // Organization and Owner are set automatically by TenantEntityProcessor
        // Add your custom initialization here
    }

    /**
     * Execute logic before creating entity
     */
    protected function beforeCreate(TaskType $taskType): void
    {
        // Override to add logic before create
    }

    /**
     * Execute logic after creating entity
     */
    protected function afterCreate(TaskType $taskType): void
    {
        // Override to add logic after create (e.g., send notification, trigger event)
    }

    /**
     * Execute logic before updating entity
     */
    protected function beforeUpdate(TaskType $taskType): void
    {
        // Override to add logic before update
    }

    /**
     * Execute logic after updating entity
     */
    protected function afterUpdate(TaskType $taskType): void
    {
        // Override to add logic after update (e.g., send notification, trigger event)
    }

    /**
     * Execute logic before deleting entity
     */
    protected function beforeDelete(TaskType $taskType): void
    {
        // Override to add logic before delete (e.g., check dependencies, archive)
    }

    /**
     * Execute logic after deleting entity
     */
    protected function afterDelete(): void
    {
        // Override to add logic after delete (e.g., cleanup, send notification)
    }
}
