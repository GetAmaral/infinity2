<?php

declare(strict_types=1);

namespace App\Controller\Generated;

use App\Controller\Base\BaseApiController;
use App\Entity\Role;
use App\Repository\RoleRepository;
use App\Security\Voter\RoleVoter;
use App\Form\RoleFormType;
use App\Service\ListPreferencesService;
use Doctrine\ORM\EntityManagerInterface;
use Symfony\Component\HttpFoundation\Request;
use Symfony\Component\HttpFoundation\Response;
use Symfony\Component\HttpFoundation\JsonResponse;
use Symfony\Component\Security\Csrf\CsrfToken;
use Symfony\Component\Security\Csrf\CsrfTokenManagerInterface;
use Symfony\Component\Security\Core\Exception\InvalidCsrfTokenException;
use Symfony\Contracts\Translation\TranslatorInterface;

/**
 * Generated Base Controller for Role
 *
 * ⚠️ WARNING: This file is ALWAYS regenerated by Genmax
 * DO NOT EDIT THIS FILE - Edit RoleController instead
 *
 * @generated
 * @codeCoverageIgnore
 */
abstract class RoleControllerGenerated extends BaseApiController
{
    public function __construct(
        protected readonly EntityManagerInterface $entityManager,
        protected readonly RoleRepository $repository,
        protected readonly ListPreferencesService $listPreferencesService,
        protected readonly TranslatorInterface $translator,
        protected readonly CsrfTokenManagerInterface $csrfTokenManager,
    ) {}

    // ====================================
    // ABSTRACT METHOD IMPLEMENTATIONS
    // ====================================

    /**
     * Get the repository for this entity
     */
    protected function getRepository(): RoleRepository
    {
        return $this->repository;
    }

    /**
     * Get the plural name for this entity
     */
    protected function getEntityPluralName(): string
    {
        return 'roles';
    }

    /**
     * Convert entity to array for API responses
     *
     * ⚠️ IMPORTANT: Enum Properties Stored as Strings
     * -------------------------------------------------
     * Properties with enum-backed types (like InputType) are stored as STRINGS
     * in the database and getters return strings directly, NOT enum objects.
     *
     * ❌ WRONG:   $entity->getType()->value   (Error: "Attempt to read property 'value' on string")
     * ✅ CORRECT: $entity->getType()          (Already a string - 'fully_completed')
     *
     * If you need the enum object for business logic, use:
     *    InputType::from($entity->getType())
     */
    protected function entityToArray(object $entity): array
    {
        if (!$entity instanceof Role) {
            throw new \InvalidArgumentException('Expected Role instance');
        }

        return [
            'id' => $entity->getId()->toString(),
            'name' => $entity->getName(),
            'permissions' => $entity->getPermissions(),
            'users' => ($usersRel = $entity->getUsers()) ? [
                'id' => $usersRel->getId()->toString(),
                'display' => (string) $usersRel,
            ] : null,
            'description' => $entity->getDescription(),
            'priority' => $entity->getPriority(),
            'systemRole' => $entity->getSystemRole(),
        ];
    }

    // ====================================
    // INDEX ACTION - List all Roles
    // ====================================

    /**
     * List Roles
     */
    protected function indexAction(): Response
    {
        $this->denyAccessUnlessGranted(RoleVoter::LIST);

        $preferences = $this->listPreferencesService->getEntityPreferences('roles');
        $savedView = $preferences['view'] ?? 'grid';

        return $this->render('role/index.html.twig', [
            'entities' => [],  // Loaded via API
            'entity_name' => 'role',
            'entity_name_plural' => 'roles',
            'page_icon' => 'bi-shield-lock',
            'default_view' => $savedView,

            // List configuration from GeneratorProperty
            'enable_search' => true,
            'enable_filters' => false,
            'enable_sorting' => true,
            'enable_create_button' => true,

            // Property metadata for client-side rendering (as JSON strings)
            'list_fields' => '[{"name":"name","label":"Name","type":"string","sortable":true,"searchable":true,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getName"},{"name":"description","label":"Description","type":"string","sortable":true,"searchable":true,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getDescription"},{"name":"priority","label":"Priority","type":"integer","sortable":true,"searchable":false,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getPriority"},{"name":"systemRole","label":"SystemRole","type":"boolean","sortable":true,"searchable":false,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getSystemRole"}]',
            'searchable_fields' => '[{"name":"name","label":"Name","type":"string"},{"name":"description","label":"Description","type":"string"}]',
            'filterable_fields' => '[]',
            'sortable_fields' => '[{"name":"name","label":"Name"},{"name":"description","label":"Description"},{"name":"priority","label":"Priority"},{"name":"systemRole","label":"SystemRole"}]',
        ]);
    }

    // ====================================
    // API SEARCH - Fetch Roles for list view
    // ====================================

    /**
     * API endpoint for searching Roles
     */
    protected function apiSearchAction(Request $request): JsonResponse
    {
        $this->denyAccessUnlessGranted(RoleVoter::LIST);

        // This method uses the BaseApiController's handleSearchRequest
        // which integrates with API Platform's GetCollection operation
        return $this->handleSearchRequest($request);
    }

    // ====================================
    // NEW FORM ACTION - Display create form
    // ====================================

    /**
     * Display new role form
     */
    protected function newFormAction(Request $request): Response
    {
        $this->denyAccessUnlessGranted(RoleVoter::CREATE);

        $role = new Role();

        // Initialize with custom logic if needed
        $this->initializeNewEntity($role);

        $form = $this->createForm(RoleFormType::class, $role);

        return $this->render('role/_form_modal.html.twig', [
            'form' => $form,
            'role' => $role,
            'is_edit' => false,
        ]);
    }

    // ====================================
    // CREATE ACTION - Process create form submission
    // ====================================

    /**
     * Process new role form submission
     */
    protected function createAction(Request $request): Response
    {
        $this->denyAccessUnlessGranted(RoleVoter::CREATE);

        $role = new Role();

        // Initialize with custom logic if needed
        $this->initializeNewEntity($role);

        $form = $this->createForm(RoleFormType::class, $role);
        $form->handleRequest($request);

        if ($form->isSubmitted() && $form->isValid()) {
            try {
                // Before create hook
                $this->beforeCreate($role);

                $this->entityManager->persist($role);
                $this->entityManager->flush();

                // After create hook
                $this->afterCreate($role);

                $this->addFlash('success', $this->translator->trans(
                    'role.flash.created_successfully',
                    ['%name%' => (string) $role],
                    'role'
                ));

                return $this->redirectToRoute('role_index', [], Response::HTTP_SEE_OTHER);

            } catch (\Exception $e) {
                $this->addFlash('error', $this->translator->trans(
                    'role.flash.create_failed',
                    ['%error%' => $e->getMessage()],
                    'role'
                ));
            }
        }

        // Re-render with errors
        return $this->render('role/_form_modal.html.twig', [
            'form' => $form,
            'role' => $role,
            'is_edit' => false,
        ], new Response('', $form->isSubmitted() ? Response::HTTP_UNPROCESSABLE_ENTITY : Response::HTTP_OK));
    }

    // ====================================
    // EDIT FORM ACTION - Display edit form
    // ====================================

    /**
     * Display edit role form
     */
    protected function editFormAction(Role $role, Request $request): Response
    {
        $this->denyAccessUnlessGranted(RoleVoter::EDIT, $role);

        $form = $this->createForm(RoleFormType::class, $role);

        return $this->render('role/_form_modal.html.twig', [
            'form' => $form,
            'role' => $role,
            'is_edit' => true,
        ]);
    }

    // ====================================
    // UPDATE ACTION - Process edit form submission
    // ====================================

    /**
     * Process edit role form submission
     */
    protected function updateAction(Role $role, Request $request): Response
    {
        $this->denyAccessUnlessGranted(RoleVoter::EDIT, $role);

        $form = $this->createForm(RoleFormType::class, $role);
        $form->handleRequest($request);

        if ($form->isSubmitted() && $form->isValid()) {
            try {
                // Before update hook
                $this->beforeUpdate($role);

                $this->entityManager->flush();

                // After update hook
                $this->afterUpdate($role);

                $this->addFlash('success', $this->translator->trans(
                    'role.flash.updated_successfully',
                    ['%name%' => (string) $role],
                    'role'
                ));

                return $this->redirectToRoute('role_index', [], Response::HTTP_SEE_OTHER);

            } catch (\Exception $e) {
                $this->addFlash('error', $this->translator->trans(
                    'role.flash.update_failed',
                    ['%error%' => $e->getMessage()],
                    'role'
                ));
            }
        }

        // Re-render with errors
        return $this->render('role/_form_modal.html.twig', [
            'form' => $form,
            'role' => $role,
            'is_edit' => true,
        ], new Response('', $form->isSubmitted() ? Response::HTTP_UNPROCESSABLE_ENTITY : Response::HTTP_OK));
    }

    // ====================================
    // DELETE ACTION - Delete role
    // ====================================

    /**
     * Delete role
     */
    protected function deleteAction(Role $role, Request $request): Response
    {
        $this->denyAccessUnlessGranted(RoleVoter::DELETE, $role);

        // Verify CSRF token
        $token = $request->request->get('_token');
        if (!$this->csrfTokenManager->isTokenValid(new CsrfToken('delete_role_' . $role->getId()->toString(), $token))) {
            throw new InvalidCsrfTokenException('Invalid CSRF token for delete operation.');
        }

        try {
            // Before delete hook
            $this->beforeDelete($role);

            $name = (string) $role;

            $this->entityManager->remove($role);
            $this->entityManager->flush();

            // After delete hook
            $this->afterDelete();

            $this->addFlash('success', $this->translator->trans(
                'role.flash.deleted_successfully',
                ['%name%' => $name],
                'role'
            ));

        } catch (\Exception $e) {
            $this->addFlash('error', $this->translator->trans(
                'role.flash.delete_failed',
                ['%error%' => $e->getMessage()],
                'role'
            ));
        }

        return $this->redirectToRoute('role_index', [], Response::HTTP_SEE_OTHER);
    }

    // ====================================
    // SHOW ACTION - Display role details
    // ====================================

    /**
     * Display role details
     */
    protected function showAction(Role $role): Response
    {
        $this->denyAccessUnlessGranted(RoleVoter::VIEW, $role);

        return $this->render('role/show.html.twig', [
            'role' => $role,
        ]);
    }

    // ====================================
    // LIFECYCLE HOOKS
    // ====================================
    // Override these in RoleController to add custom logic

    /**
     * Initialize new entity before creating form
     *
     * Note: Organization and Owner are set automatically by TenantEntityProcessor
     * Only use this for custom initialization logic
     */
    protected function initializeNewEntity(Role $role): void
    {
        // Organization and Owner are set automatically by TenantEntityProcessor
        // Add your custom initialization here
    }

    /**
     * Execute logic before creating entity
     */
    protected function beforeCreate(Role $role): void
    {
        // Override to add logic before create
    }

    /**
     * Execute logic after creating entity
     */
    protected function afterCreate(Role $role): void
    {
        // Override to add logic after create (e.g., send notification, trigger event)
    }

    /**
     * Execute logic before updating entity
     */
    protected function beforeUpdate(Role $role): void
    {
        // Override to add logic before update
    }

    /**
     * Execute logic after updating entity
     */
    protected function afterUpdate(Role $role): void
    {
        // Override to add logic after update (e.g., send notification, trigger event)
    }

    /**
     * Execute logic before deleting entity
     */
    protected function beforeDelete(Role $role): void
    {
        // Override to add logic before delete (e.g., check dependencies, archive)
    }

    /**
     * Execute logic after deleting entity
     */
    protected function afterDelete(): void
    {
        // Override to add logic after delete (e.g., cleanup, send notification)
    }
}
