<?php

declare(strict_types=1);

namespace App\Controller\Generated;

use App\Controller\Base\BaseApiController;
use App\Entity\WorkingHour;
use App\Repository\WorkingHourRepository;
use App\Security\Voter\WorkingHourVoter;
use App\Form\WorkingHourFormType;
use App\Service\ListPreferencesService;
use Doctrine\ORM\EntityManagerInterface;
use Symfony\Component\HttpFoundation\Request;
use Symfony\Component\HttpFoundation\Response;
use Symfony\Component\HttpFoundation\JsonResponse;
use Symfony\Component\Security\Csrf\CsrfToken;
use Symfony\Component\Security\Csrf\CsrfTokenManagerInterface;
use Symfony\Component\Security\Core\Exception\InvalidCsrfTokenException;
use Symfony\Contracts\Translation\TranslatorInterface;

/**
 * Generated Base Controller for WorkingHour
 *
 * ⚠️ WARNING: This file is ALWAYS regenerated by Genmax
 * DO NOT EDIT THIS FILE - Edit WorkingHourController instead
 *
 * @generated
 * @codeCoverageIgnore
 */
abstract class WorkingHourControllerGenerated extends BaseApiController
{
    public function __construct(
        protected readonly EntityManagerInterface $entityManager,
        protected readonly WorkingHourRepository $repository,
        protected readonly ListPreferencesService $listPreferencesService,
        protected readonly TranslatorInterface $translator,
        protected readonly CsrfTokenManagerInterface $csrfTokenManager,
    ) {}

    // ====================================
    // ABSTRACT METHOD IMPLEMENTATIONS
    // ====================================

    /**
     * Get the repository for this entity
     */
    protected function getRepository(): WorkingHourRepository
    {
        return $this->repository;
    }

    /**
     * Get the plural name for this entity
     */
    protected function getEntityPluralName(): string
    {
        return 'workingHours';
    }

    /**
     * Convert entity to array for API responses
     *
     * ⚠️ IMPORTANT: Enum Properties Stored as Strings
     * -------------------------------------------------
     * Properties with enum-backed types (like InputType) are stored as STRINGS
     * in the database and getters return strings directly, NOT enum objects.
     *
     * ❌ WRONG:   $entity->getType()->value   (Error: "Attempt to read property 'value' on string")
     * ✅ CORRECT: $entity->getType()          (Already a string - 'fully_completed')
     *
     * If you need the enum object for business logic, use:
     *    InputType::from($entity->getType())
     */
    protected function entityToArray(object $entity): array
    {
        if (!$entity instanceof WorkingHour) {
            throw new \InvalidArgumentException('Expected WorkingHour instance');
        }

        return [
            'id' => $entity->getId()->toString(),
            'organization' => ($organizationRel = $entity->getOrganization()) ? [
                'id' => $organizationRel->getId()->toString(),
                'display' => (string) $organizationRel,
            ] : null,
            'description' => $entity->getDescription(),
            'calendar' => ($calendarRel = $entity->getCalendar()) ? [
                'id' => $calendarRel->getId()->toString(),
                'display' => (string) $calendarRel,
            ] : null,
            'dayOfWeek' => $entity->getDayOfWeek(),
            'endTime' => $entity->getEndTime(),
            'event' => ($eventRel = $entity->getEvent()) ? [
                'id' => $eventRel->getId()->toString(),
                'display' => (string) $eventRel,
            ] : null,
            'minimalMinutesEventDuration' => $entity->getMinimalMinutesEventDuration(),
            'notes' => $entity->getNotes(),
            'startTime' => $entity->getStartTime(),
            'timeZone' => ($timeZoneRel = $entity->getTimeZone()) ? [
                'id' => $timeZoneRel->getId()->toString(),
                'display' => (string) $timeZoneRel,
            ] : null,
        ];
    }

    // ====================================
    // INDEX ACTION - List all WorkingHours
    // ====================================

    /**
     * List WorkingHours
     */
    protected function indexAction(): Response
    {
        $this->denyAccessUnlessGranted(WorkingHourVoter::LIST);

        $preferences = $this->listPreferencesService->getEntityPreferences('workingHours');
        $savedView = $preferences['view'] ?? 'grid';

        return $this->render('workinghour/index.html.twig', [
            'entities' => [],  // Loaded via API
            'entity_name' => 'workingHour',
            'entity_name_plural' => 'workingHours',
            'page_icon' => 'bi-clock',
            'default_view' => $savedView,

            // List configuration from GeneratorProperty
            'enable_search' => true,
            'enable_filters' => false,
            'enable_sorting' => true,
            'enable_create_button' => true,

            // Property metadata for client-side rendering (as JSON strings)
            'list_fields' => '[{"name":"description","label":"Description","type":"string","sortable":true,"searchable":true,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getDescription"},{"name":"calendar","label":"Calendar","type":"","sortable":true,"searchable":false,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getCalendar"},{"name":"dayOfWeek","label":"DayOfWeek","type":"integer","sortable":true,"searchable":false,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getDayOfWeek"},{"name":"endTime","label":"EndTime","type":"time","sortable":true,"searchable":false,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getEndTime"},{"name":"event","label":"Event","type":"","sortable":true,"searchable":false,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getEvent"},{"name":"minimalMinutesEventDuration","label":"MinimalMinutesEventDuration","type":"integer","sortable":true,"searchable":false,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getMinimalMinutesEventDuration"},{"name":"notes","label":"Notes","type":"text","sortable":true,"searchable":true,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getNotes"},{"name":"startTime","label":"StartTime","type":"time","sortable":true,"searchable":false,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getStartTime"},{"name":"timeZone","label":"TimeZone","type":"","sortable":true,"searchable":false,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getTimeZone"}]',
            'searchable_fields' => '[{"name":"description","label":"Description","type":"string"},{"name":"notes","label":"Notes","type":"text"}]',
            'filterable_fields' => '[]',
            'sortable_fields' => '[{"name":"description","label":"Description"},{"name":"calendar","label":"Calendar"},{"name":"dayOfWeek","label":"DayOfWeek"},{"name":"endTime","label":"EndTime"},{"name":"event","label":"Event"},{"name":"minimalMinutesEventDuration","label":"MinimalMinutesEventDuration"},{"name":"notes","label":"Notes"},{"name":"startTime","label":"StartTime"},{"name":"timeZone","label":"TimeZone"}]',
        ]);
    }

    // ====================================
    // API SEARCH - Fetch WorkingHours for list view
    // ====================================

    /**
     * API endpoint for searching WorkingHours
     */
    protected function apiSearchAction(Request $request): JsonResponse
    {
        $this->denyAccessUnlessGranted(WorkingHourVoter::LIST);

        // This method uses the BaseApiController's handleSearchRequest
        // which integrates with API Platform's GetCollection operation
        return $this->handleSearchRequest($request);
    }

    // ====================================
    // NEW FORM ACTION - Display create form
    // ====================================

    /**
     * Display new workingHour form
     */
    protected function newFormAction(Request $request): Response
    {
        $this->denyAccessUnlessGranted(WorkingHourVoter::CREATE);

        $workingHour = new WorkingHour();

        // Initialize with custom logic if needed
        $this->initializeNewEntity($workingHour);

        $form = $this->createForm(WorkingHourFormType::class, $workingHour);

        return $this->render('workinghour/_form_modal.html.twig', [
            'form' => $form,
            'workingHour' => $workingHour,
            'is_edit' => false,
        ]);
    }

    // ====================================
    // CREATE ACTION - Process create form submission
    // ====================================

    /**
     * Process new workingHour form submission
     */
    protected function createAction(Request $request): Response
    {
        $this->denyAccessUnlessGranted(WorkingHourVoter::CREATE);

        $workingHour = new WorkingHour();

        // Initialize with custom logic if needed
        $this->initializeNewEntity($workingHour);

        $form = $this->createForm(WorkingHourFormType::class, $workingHour);
        $form->handleRequest($request);

        if ($form->isSubmitted() && $form->isValid()) {
            try {
                // Before create hook
                $this->beforeCreate($workingHour);

                $this->entityManager->persist($workingHour);
                $this->entityManager->flush();

                // After create hook
                $this->afterCreate($workingHour);

                $this->addFlash('success', $this->translator->trans(
                    'workinghour.flash.created_successfully',
                    ['%name%' => (string) $workingHour],
                    'workinghour'
                ));

                return $this->redirectToRoute('workinghour_index', [], Response::HTTP_SEE_OTHER);

            } catch (\Exception $e) {
                $this->addFlash('error', $this->translator->trans(
                    'workinghour.flash.create_failed',
                    ['%error%' => $e->getMessage()],
                    'workinghour'
                ));
            }
        }

        // Re-render with errors
        return $this->render('workinghour/_form_modal.html.twig', [
            'form' => $form,
            'workingHour' => $workingHour,
            'is_edit' => false,
        ], new Response('', $form->isSubmitted() ? Response::HTTP_UNPROCESSABLE_ENTITY : Response::HTTP_OK));
    }

    // ====================================
    // EDIT FORM ACTION - Display edit form
    // ====================================

    /**
     * Display edit workingHour form
     */
    protected function editFormAction(WorkingHour $workingHour, Request $request): Response
    {
        $this->denyAccessUnlessGranted(WorkingHourVoter::EDIT, $workingHour);

        $form = $this->createForm(WorkingHourFormType::class, $workingHour);

        return $this->render('workinghour/_form_modal.html.twig', [
            'form' => $form,
            'workingHour' => $workingHour,
            'is_edit' => true,
        ]);
    }

    // ====================================
    // UPDATE ACTION - Process edit form submission
    // ====================================

    /**
     * Process edit workingHour form submission
     */
    protected function updateAction(WorkingHour $workingHour, Request $request): Response
    {
        $this->denyAccessUnlessGranted(WorkingHourVoter::EDIT, $workingHour);

        $form = $this->createForm(WorkingHourFormType::class, $workingHour);
        $form->handleRequest($request);

        if ($form->isSubmitted() && $form->isValid()) {
            try {
                // Before update hook
                $this->beforeUpdate($workingHour);

                $this->entityManager->flush();

                // After update hook
                $this->afterUpdate($workingHour);

                $this->addFlash('success', $this->translator->trans(
                    'workinghour.flash.updated_successfully',
                    ['%name%' => (string) $workingHour],
                    'workinghour'
                ));

                return $this->redirectToRoute('workinghour_index', [], Response::HTTP_SEE_OTHER);

            } catch (\Exception $e) {
                $this->addFlash('error', $this->translator->trans(
                    'workinghour.flash.update_failed',
                    ['%error%' => $e->getMessage()],
                    'workinghour'
                ));
            }
        }

        // Re-render with errors
        return $this->render('workinghour/_form_modal.html.twig', [
            'form' => $form,
            'workingHour' => $workingHour,
            'is_edit' => true,
        ], new Response('', $form->isSubmitted() ? Response::HTTP_UNPROCESSABLE_ENTITY : Response::HTTP_OK));
    }

    // ====================================
    // DELETE ACTION - Delete workingHour
    // ====================================

    /**
     * Delete workingHour
     */
    protected function deleteAction(WorkingHour $workingHour, Request $request): Response
    {
        $this->denyAccessUnlessGranted(WorkingHourVoter::DELETE, $workingHour);

        // Verify CSRF token
        $token = $request->request->get('_token');
        if (!$this->csrfTokenManager->isTokenValid(new CsrfToken('delete_workingHour_' . $workingHour->getId()->toString(), $token))) {
            throw new InvalidCsrfTokenException('Invalid CSRF token for delete operation.');
        }

        try {
            // Before delete hook
            $this->beforeDelete($workingHour);

            $name = (string) $workingHour;

            $this->entityManager->remove($workingHour);
            $this->entityManager->flush();

            // After delete hook
            $this->afterDelete();

            $this->addFlash('success', $this->translator->trans(
                'workinghour.flash.deleted_successfully',
                ['%name%' => $name],
                'workinghour'
            ));

        } catch (\Exception $e) {
            $this->addFlash('error', $this->translator->trans(
                'workinghour.flash.delete_failed',
                ['%error%' => $e->getMessage()],
                'workinghour'
            ));
        }

        return $this->redirectToRoute('workinghour_index', [], Response::HTTP_SEE_OTHER);
    }

    // ====================================
    // SHOW ACTION - Display workingHour details
    // ====================================

    /**
     * Display workingHour details
     */
    protected function showAction(WorkingHour $workingHour): Response
    {
        $this->denyAccessUnlessGranted(WorkingHourVoter::VIEW, $workingHour);

        return $this->render('workinghour/show.html.twig', [
            'workingHour' => $workingHour,
        ]);
    }

    // ====================================
    // LIFECYCLE HOOKS
    // ====================================
    // Override these in WorkingHourController to add custom logic

    /**
     * Initialize new entity before creating form
     *
     * Note: Organization and Owner are set automatically by TenantEntityProcessor
     * Only use this for custom initialization logic
     */
    protected function initializeNewEntity(WorkingHour $workingHour): void
    {
        // Organization and Owner are set automatically by TenantEntityProcessor
        // Add your custom initialization here
    }

    /**
     * Execute logic before creating entity
     */
    protected function beforeCreate(WorkingHour $workingHour): void
    {
        // Override to add logic before create
    }

    /**
     * Execute logic after creating entity
     */
    protected function afterCreate(WorkingHour $workingHour): void
    {
        // Override to add logic after create (e.g., send notification, trigger event)
    }

    /**
     * Execute logic before updating entity
     */
    protected function beforeUpdate(WorkingHour $workingHour): void
    {
        // Override to add logic before update
    }

    /**
     * Execute logic after updating entity
     */
    protected function afterUpdate(WorkingHour $workingHour): void
    {
        // Override to add logic after update (e.g., send notification, trigger event)
    }

    /**
     * Execute logic before deleting entity
     */
    protected function beforeDelete(WorkingHour $workingHour): void
    {
        // Override to add logic before delete (e.g., check dependencies, archive)
    }

    /**
     * Execute logic after deleting entity
     */
    protected function afterDelete(): void
    {
        // Override to add logic after delete (e.g., cleanup, send notification)
    }
}
