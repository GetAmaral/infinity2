<?php

declare(strict_types=1);

namespace App\Controller\Generated;

use App\Controller\Base\BaseApiController;
use App\Entity\LeadSource;
use App\MultiTenant\TenantContext;
use App\Repository\LeadSourceRepository;
use App\Security\Voter\LeadSourceVoter;
use App\Form\LeadSourceType;
use App\Service\ListPreferencesService;
use Doctrine\ORM\EntityManagerInterface;
use Symfony\Component\HttpFoundation\Request;
use Symfony\Component\HttpFoundation\Response;
use Symfony\Component\HttpFoundation\JsonResponse;
use Symfony\Component\Security\Csrf\CsrfToken;
use Symfony\Component\Security\Csrf\CsrfTokenManagerInterface;
use Symfony\Component\Security\Core\Exception\InvalidCsrfTokenException;
use Symfony\Contracts\Translation\TranslatorInterface;

/**
 * Generated Base Controller for LeadSource
 *
 * ⚠️ WARNING: This file is ALWAYS regenerated by Genmax
 * DO NOT EDIT THIS FILE - Edit LeadSourceController instead
 *
 * @generated
 * @codeCoverageIgnore
 */
abstract class LeadSourceControllerGenerated extends BaseApiController
{
    public function __construct(
        protected readonly EntityManagerInterface $entityManager,
        protected readonly LeadSourceRepository $repository,
        protected readonly ListPreferencesService $listPreferencesService,
        protected readonly TranslatorInterface $translator,
        protected readonly CsrfTokenManagerInterface $csrfTokenManager,
        protected readonly TenantContext $tenantContext,
    ) {}

    // ====================================
    // ABSTRACT METHOD IMPLEMENTATIONS
    // ====================================

    /**
     * Get the repository for this entity
     */
    protected function getRepository(): LeadSourceRepository
    {
        return $this->repository;
    }

    /**
     * Get the plural name for this entity
     */
    protected function getEntityPluralName(): string
    {
        return 'leadSources';
    }

    /**
     * Convert entity to array for API responses
     *
     * ⚠️ IMPORTANT: Enum Properties Stored as Strings
     * -------------------------------------------------
     * Properties with enum-backed types (like InputType) are stored as STRINGS
     * in the database and getters return strings directly, NOT enum objects.
     *
     * ❌ WRONG:   $entity->getType()->value   (Error: "Attempt to read property 'value' on string")
     * ✅ CORRECT: $entity->getType()          (Already a string - 'fully_completed')
     *
     * If you need the enum object for business logic, use:
     *    InputType::from($entity->getType())
     */
    protected function entityToArray(object $entity): array
    {
        if (!$entity instanceof LeadSource) {
            throw new \InvalidArgumentException('Expected LeadSource instance');
        }

        return [
            'id' => $entity->getId()->toString(),
            'organization' => ($organizationRel = $entity->getOrganization()) ? [
                'id' => $organizationRel->getId()->toString(),
                'display' => (string) $organizationRel,
            ] : null,
            'name' => $entity->getName(),
            'description' => $entity->getDescription(),
            'category' => $entity->getCategory(),
            'active' => $entity->getActive(),
            'deals' => ($dealsRel = $entity->getDeals()) ? array_map(
                fn($item) => [
                    'id' => $item->getId()->toString(),
                    'display' => (string) $item,
                ],
                $dealsRel->toArray()
            ) : [],
        ];
    }

    // ====================================
    // INDEX ACTION - List all LeadSources
    // ====================================

    /**
     * List LeadSources
     */
    protected function indexAction(): Response
    {
        $this->denyAccessUnlessGranted(LeadSourceVoter::LIST);

        $preferences = $this->listPreferencesService->getEntityPreferences('leadSources');
        $savedView = $preferences['view'] ?? 'grid';

        return $this->render('leadsource/index.html.twig', [
            'entities' => [],  // Loaded via API
            'entity_name' => 'leadsource',
            'entity_name_plural' => 'leadSources',
            'page_icon' => 'bi-funnel',
            'default_view' => $savedView,

            // List configuration from GeneratorProperty
            'enable_search' => true,
            'enable_filters' => false,
            'enable_sorting' => true,
            'enable_create_button' => true,
            'create_permission' => LeadSourceVoter::CREATE,

            // Property metadata for Twig templates (as PHP arrays)
            'listProperties' => json_decode('[{"name":"name","label":"Name","type":"string","sortable":true,"searchable":true,"filterable":false,"filterStrategy":"partial","filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getName","isRelationship":false},{"name":"description","label":"Description","type":"text","sortable":true,"searchable":true,"filterable":false,"filterStrategy":"partial","filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getDescription","isRelationship":false},{"name":"category","label":"Category","type":"string","sortable":true,"searchable":true,"filterable":false,"filterStrategy":"exact","filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getCategory","isRelationship":false},{"name":"active","label":"Active","type":"boolean","sortable":true,"searchable":false,"filterable":false,"filterStrategy":null,"filterBoolean":true,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getActive","isRelationship":false},{"name":"deals","label":"Deals","type":"","sortable":true,"searchable":false,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getDeals","isRelationship":true}]', true),
            'searchableFields' => json_decode('[{"name":"name","label":"Name","type":"string"},{"name":"description","label":"Description","type":"text"},{"name":"category","label":"Category","type":"string"}]', true),
            'filterableFields' => json_decode('[]', true),
            'sortableFields' => json_decode('[{"name":"name","label":"Name"},{"name":"description","label":"Description"},{"name":"category","label":"Category"},{"name":"active","label":"Active"},{"name":"deals","label":"Deals"}]', true),

            // Property metadata for client-side rendering (as JSON strings)
            'list_fields' => '[{"name":"name","label":"Name","type":"string","sortable":true,"searchable":true,"filterable":false,"filterStrategy":"partial","filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getName","isRelationship":false},{"name":"description","label":"Description","type":"text","sortable":true,"searchable":true,"filterable":false,"filterStrategy":"partial","filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getDescription","isRelationship":false},{"name":"category","label":"Category","type":"string","sortable":true,"searchable":true,"filterable":false,"filterStrategy":"exact","filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getCategory","isRelationship":false},{"name":"active","label":"Active","type":"boolean","sortable":true,"searchable":false,"filterable":false,"filterStrategy":null,"filterBoolean":true,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getActive","isRelationship":false},{"name":"deals","label":"Deals","type":"","sortable":true,"searchable":false,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getDeals","isRelationship":true}]',
            'searchable_fields' => '[{"name":"name","label":"Name","type":"string"},{"name":"description","label":"Description","type":"text"},{"name":"category","label":"Category","type":"string"}]',
            'filterable_fields' => '[]',
            'sortable_fields' => '[{"name":"name","label":"Name"},{"name":"description","label":"Description"},{"name":"category","label":"Category"},{"name":"active","label":"Active"},{"name":"deals","label":"Deals"}]',
        ]);
    }

    // ====================================
    // API SEARCH - Fetch LeadSources for list view
    // ====================================

    /**
     * API endpoint for searching LeadSources
     */
    protected function apiSearchAction(Request $request): JsonResponse
    {
        $this->denyAccessUnlessGranted(LeadSourceVoter::LIST);

        // Delegate to parent BaseApiController which handles
        // search, filtering, sorting, and pagination
        return parent::apiSearchAction($request);
    }

    // ====================================
    // NEW FORM ACTION - Display create form
    // ====================================

    /**
     * Display new leadSource form
     */
    protected function newFormAction(Request $request): Response
    {
        $this->denyAccessUnlessGranted(LeadSourceVoter::CREATE);

        $leadSource = new LeadSource();

        // Initialize with custom logic if needed
        $this->initializeNewEntity($leadSource);

        $form = $this->createForm(LeadSourceType::class, $leadSource);

        return $this->render('leadsource/_form_modal.html.twig', [
            'form' => $form,
            'leadSource' => $leadSource,
            'is_edit' => false,
        ]);
    }

    // ====================================
    // CREATE ACTION - Process create form submission
    // ====================================

    /**
     * Process new leadSource form submission
     */
    protected function createAction(Request $request): Response
    {
        $this->denyAccessUnlessGranted(LeadSourceVoter::CREATE);

        $leadSource = new LeadSource();

        // Initialize with custom logic if needed
        $this->initializeNewEntity($leadSource);

        $form = $this->createForm(LeadSourceType::class, $leadSource);
        $form->handleRequest($request);

        if ($form->isSubmitted()) {
            // Re-set organization after form handling (form excludes this field)
            $organization = $this->tenantContext->getOrganizationForNewEntity();
            if ($organization) {
                $leadSource->setOrganization($organization);
                error_log('✅ LeadSourceController: Organization re-set after form handling to ' . $organization->getName());
            }

            if ($form->isValid()) {
                try {
                    // Before create hook
                    $this->beforeCreate($leadSource);

                    $this->entityManager->persist($leadSource);
                    $this->entityManager->flush();

                    // After create hook
                    $this->afterCreate($leadSource);

                    $this->addFlash('success', $this->translator->trans(
                        'leadsource.flash.created_successfully',
                        ['%name%' => (string) $leadSource],
                        'leadsource'
                    ));

                    // If this is a modal/AJAX request (from "+" button), return Turbo Stream with event dispatch
                    // Check both GET and POST for modal parameter
                    if ($request->headers->get('X-Requested-With') === 'turbo-frame' ||
                        $request->get('modal') === '1') {

                        // Get display text for the entity
                        $displayText = (string) $leadSource;

                        $response = $this->render('_entity_created_success_stream.html.twig', [
                            'entityType' => 'LeadSource',
                            'entityId' => $leadSource->getId()->toRfc4122(),
                            'displayText' => $displayText,
                        ]);

                        // Set Turbo Stream content type so Turbo processes it without navigating
                        $response->headers->set('Content-Type', 'text/vnd.turbo-stream.html');

                        return $response;
                    }

                    return $this->redirectToRoute('leadsource_index', [], Response::HTTP_SEE_OTHER);

                } catch (\Exception $e) {
                    $this->addFlash('error', $this->translator->trans(
                        'leadsource.flash.create_failed',
                        ['%error%' => $e->getMessage()],
                        'leadsource'
                    ));
                }
            }
        }

        // Re-render with errors
        return $this->render('leadsource/_form_modal.html.twig', [
            'form' => $form,
            'leadSource' => $leadSource,
            'is_edit' => false,
        ], new Response('', $form->isSubmitted() ? Response::HTTP_UNPROCESSABLE_ENTITY : Response::HTTP_OK));
    }

    // ====================================
    // EDIT FORM ACTION - Display edit form
    // ====================================

    /**
     * Display edit leadSource form
     */
    protected function editFormAction(LeadSource $leadSource, Request $request): Response
    {
        $this->denyAccessUnlessGranted(LeadSourceVoter::EDIT, $leadSource);

        $form = $this->createForm(LeadSourceType::class, $leadSource);

        return $this->render('leadsource/_form_modal.html.twig', [
            'form' => $form,
            'leadSource' => $leadSource,
            'is_edit' => true,
        ]);
    }

    // ====================================
    // UPDATE ACTION - Process edit form submission
    // ====================================

    /**
     * Process edit leadSource form submission
     */
    protected function updateAction(LeadSource $leadSource, Request $request): Response
    {
        $this->denyAccessUnlessGranted(LeadSourceVoter::EDIT, $leadSource);

        // Store original organization to preserve it
        $originalOrganization = $leadSource->getOrganization();

        $form = $this->createForm(LeadSourceType::class, $leadSource);
        $form->handleRequest($request);

        if ($form->isSubmitted()) {
            // Restore organization after form handling (form excludes this field)
            if ($originalOrganization) {
                $leadSource->setOrganization($originalOrganization);
            }

            if ($form->isValid()) {
                try {
                    // Before update hook
                    $this->beforeUpdate($leadSource);

                    $this->entityManager->flush();

                    // After update hook
                    $this->afterUpdate($leadSource);

                    $this->addFlash('success', $this->translator->trans(
                        'leadsource.flash.updated_successfully',
                        ['%name%' => (string) $leadSource],
                        'leadsource'
                    ));

                    return $this->redirectToRoute('leadsource_index', [], Response::HTTP_SEE_OTHER);

                } catch (\Exception $e) {
                    $this->addFlash('error', $this->translator->trans(
                        'leadsource.flash.update_failed',
                        ['%error%' => $e->getMessage()],
                        'leadsource'
                    ));
                }
            }
        }

        // Re-render with errors
        return $this->render('leadsource/_form_modal.html.twig', [
            'form' => $form,
            'leadSource' => $leadSource,
            'is_edit' => true,
        ], new Response('', $form->isSubmitted() ? Response::HTTP_UNPROCESSABLE_ENTITY : Response::HTTP_OK));
    }

    // ====================================
    // DELETE ACTION - Delete leadSource
    // ====================================

    /**
     * Delete leadSource
     */
    protected function deleteAction(LeadSource $leadSource, Request $request): Response
    {
        $this->denyAccessUnlessGranted(LeadSourceVoter::DELETE, $leadSource);

        // Verify CSRF token
        $token = $request->request->get('_token');
        if (!$this->csrfTokenManager->isTokenValid(new CsrfToken('delete_leadSource_' . $leadSource->getId()->toString(), $token))) {
            throw new InvalidCsrfTokenException('Invalid CSRF token for delete operation.');
        }

        try {
            // Before delete hook
            $this->beforeDelete($leadSource);

            $name = (string) $leadSource;

            $this->entityManager->remove($leadSource);
            $this->entityManager->flush();

            // After delete hook
            $this->afterDelete();

            $this->addFlash('success', $this->translator->trans(
                'leadsource.flash.deleted_successfully',
                ['%name%' => $name],
                'leadsource'
            ));

        } catch (\Exception $e) {
            $this->addFlash('error', $this->translator->trans(
                'leadsource.flash.delete_failed',
                ['%error%' => $e->getMessage()],
                'leadsource'
            ));
        }

        return $this->redirectToRoute('leadsource_index', [], Response::HTTP_SEE_OTHER);
    }

    // ====================================
    // SHOW ACTION - Display leadSource details
    // ====================================

    /**
     * Display leadSource details
     */
    protected function showAction(LeadSource $leadSource): Response
    {
        $this->denyAccessUnlessGranted(LeadSourceVoter::VIEW, $leadSource);

        // Build show properties configuration for view
        $showProperties = $this->buildShowProperties($leadSource);

        return $this->render('leadsource/show.html.twig', [
            'leadSource' => $leadSource,
            'showProperties' => $showProperties,
        ]);
    }

    /**
     * Build show properties configuration
     * Override this method in LeadSourceController to customize displayed properties
     */
    protected function buildShowProperties(LeadSource $leadSource): array
    {
        return [];
    }

    // ====================================
    // LIFECYCLE HOOKS
    // ====================================
    // Override these in LeadSourceController to add custom logic

    /**
     * Initialize new entity before creating form
     *
     * Sets organization from multi-tenant context.
     * Multi-tenant system handles: subdomain OR user's organization fallback.
     *
     * This runs BEFORE form validation, ensuring required organization field is set.
     */
    protected function initializeNewEntity(LeadSource $leadSource): void
    {
        // Auto-set organization from multi-tenant context
        $organization = $this->tenantContext->getOrganizationForNewEntity();
        if ($organization) {
            $leadSource->setOrganization($organization);
            error_log('✅ LeadSourceController: Organization set to ' . $organization->getName());
        } else {
            error_log('❌ LeadSourceController: No organization available from TenantContext');
        }

        // Add your custom initialization here
    }

    /**
     * Execute logic before creating entity
     */
    protected function beforeCreate(LeadSource $leadSource): void
    {
        // Override to add logic before create
    }

    /**
     * Execute logic after creating entity
     */
    protected function afterCreate(LeadSource $leadSource): void
    {
        // Override to add logic after create (e.g., send notification, trigger event)
    }

    /**
     * Execute logic before updating entity
     */
    protected function beforeUpdate(LeadSource $leadSource): void
    {
        // Override to add logic before update
    }

    /**
     * Execute logic after updating entity
     */
    protected function afterUpdate(LeadSource $leadSource): void
    {
        // Override to add logic after update (e.g., send notification, trigger event)
    }

    /**
     * Execute logic before deleting entity
     */
    protected function beforeDelete(LeadSource $leadSource): void
    {
        // Override to add logic before delete (e.g., check dependencies, archive)
    }

    /**
     * Execute logic after deleting entity
     */
    protected function afterDelete(): void
    {
        // Override to add logic after delete (e.g., cleanup, send notification)
    }
}
