<?php

declare(strict_types=1);

namespace App\Controller\Generated;

use App\Controller\Base\BaseApiController;
use App\Entity\ProductCategory;
use App\Repository\ProductCategoryRepository;
use App\Security\Voter\ProductCategoryVoter;
use App\Form\ProductCategoryFormType;
use App\Service\ListPreferencesService;
use Doctrine\ORM\EntityManagerInterface;
use Symfony\Component\HttpFoundation\Request;
use Symfony\Component\HttpFoundation\Response;
use Symfony\Component\HttpFoundation\JsonResponse;
use Symfony\Component\Security\Csrf\CsrfToken;
use Symfony\Component\Security\Csrf\CsrfTokenManagerInterface;
use Symfony\Component\Security\Core\Exception\InvalidCsrfTokenException;
use Symfony\Contracts\Translation\TranslatorInterface;

/**
 * Generated Base Controller for ProductCategory
 *
 * ⚠️ WARNING: This file is ALWAYS regenerated by Genmax
 * DO NOT EDIT THIS FILE - Edit ProductCategoryController instead
 *
 * @generated
 * @codeCoverageIgnore
 */
abstract class ProductCategoryControllerGenerated extends BaseApiController
{
    public function __construct(
        protected readonly EntityManagerInterface $entityManager,
        protected readonly ProductCategoryRepository $repository,
        protected readonly ListPreferencesService $listPreferencesService,
        protected readonly TranslatorInterface $translator,
        protected readonly CsrfTokenManagerInterface $csrfTokenManager,
    ) {}

    // ====================================
    // ABSTRACT METHOD IMPLEMENTATIONS
    // ====================================

    /**
     * Get the repository for this entity
     */
    protected function getRepository(): ProductCategoryRepository
    {
        return $this->repository;
    }

    /**
     * Get the plural name for this entity
     */
    protected function getEntityPluralName(): string
    {
        return 'ries';
    }

    /**
     * Convert entity to array for API responses
     */
    protected function entityToArray(object $entity): array
    {
        if (!$entity instanceof ProductCategory) {
            throw new \InvalidArgumentException('Expected ProductCategory instance');
        }

        return [
            'id' => $entity->getId()->toString(),
            'organization' => ($organizationRel = $entity->getOrganization()) ? [
                'id' => $organizationRel->getId()->toString(),
                'display' => (string) $organizationRel,
            ] : null,
            'name' => $entity->getName(),
            'description' => $entity->getDescription(),
            'parentCategory' => ($parentCategoryRel = $entity->getParentCategory()) ? [
                'id' => $parentCategoryRel->getId()->toString(),
                'display' => (string) $parentCategoryRel,
            ] : null,
            'subcategories' => ($subcategoriesRel = $entity->getSubcategories()) ? [
                'id' => $subcategoriesRel->getId()->toString(),
                'display' => (string) $subcategoriesRel,
            ] : null,
            'products' => ($productsRel = $entity->getProducts()) ? [
                'id' => $productsRel->getId()->toString(),
                'display' => (string) $productsRel,
            ] : null,
        ];
    }

    // ====================================
    // INDEX ACTION - List all ries
    // ====================================

    /**
     * List ries
     */
    protected function indexAction(): Response
    {
        $this->denyAccessUnlessGranted(ProductCategoryVoter::LIST);

        $preferences = $this->listPreferencesService->getEntityPreferences('ries');
        $savedView = $preferences['view'] ?? 'grid';

        return $this->render('productcategory/index.html.twig', [
            'entities' => [],  // Loaded via API
            'entity_name' => 'productCategory',
            'entity_name_plural' => 'ries',
            'page_icon' => 'bi-boxes',
            'default_view' => $savedView,

            // List configuration from GeneratorProperty
            'enable_search' => true,
            'enable_filters' => false,
            'enable_sorting' => true,
            'enable_create_button' => true,

            // Property metadata for client-side rendering (as JSON strings)
            'list_fields' => '[{"name":"name","label":"Name","type":"string","sortable":true,"searchable":true,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getName"},{"name":"description","label":"Description","type":"text","sortable":true,"searchable":true,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getDescription"},{"name":"parentCategory","label":"ParentCategory","type":"","sortable":true,"searchable":false,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getParentCategory"},{"name":"subcategories","label":"Subcategories","type":"","sortable":true,"searchable":false,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getSubcategories"},{"name":"products","label":"Products","type":"","sortable":true,"searchable":false,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getProducts"}]',
            'searchable_fields' => '[{"name":"name","label":"Name","type":"string"},{"name":"description","label":"Description","type":"text"}]',
            'filterable_fields' => '[]',
            'sortable_fields' => '[{"name":"name","label":"Name"},{"name":"description","label":"Description"},{"name":"parentCategory","label":"ParentCategory"},{"name":"subcategories","label":"Subcategories"},{"name":"products","label":"Products"}]',
        ]);
    }

    // ====================================
    // API SEARCH - Fetch ries for list view
    // ====================================

    /**
     * API endpoint for searching ries
     */
    protected function apiSearchAction(Request $request): JsonResponse
    {
        $this->denyAccessUnlessGranted(ProductCategoryVoter::LIST);

        // This method uses the BaseApiController's handleSearchRequest
        // which integrates with API Platform's GetCollection operation
        return $this->handleSearchRequest($request);
    }

    // ====================================
    // NEW FORM ACTION - Display create form
    // ====================================

    /**
     * Display new productCategory form
     */
    protected function newFormAction(Request $request): Response
    {
        $this->denyAccessUnlessGranted(ProductCategoryVoter::CREATE);

        $productCategory = new ProductCategory();

        // Initialize with custom logic if needed
        $this->initializeNewEntity($productCategory);

        $form = $this->createForm(ProductCategoryFormType::class, $productCategory);

        return $this->render('productcategory/_form_modal.html.twig', [
            'form' => $form,
            'productCategory' => $productCategory,
            'is_edit' => false,
        ]);
    }

    // ====================================
    // CREATE ACTION - Process create form submission
    // ====================================

    /**
     * Process new productCategory form submission
     */
    protected function createAction(Request $request): Response
    {
        $this->denyAccessUnlessGranted(ProductCategoryVoter::CREATE);

        $productCategory = new ProductCategory();

        // Initialize with custom logic if needed
        $this->initializeNewEntity($productCategory);

        $form = $this->createForm(ProductCategoryFormType::class, $productCategory);
        $form->handleRequest($request);

        if ($form->isSubmitted() && $form->isValid()) {
            try {
                // Before create hook
                $this->beforeCreate($productCategory);

                $this->entityManager->persist($productCategory);
                $this->entityManager->flush();

                // After create hook
                $this->afterCreate($productCategory);

                $this->addFlash('success', $this->translator->trans(
                    'productcategory.flash.created_successfully',
                    ['%name%' => (string) $productCategory],
                    'productcategory'
                ));

                return $this->redirectToRoute('productcategory_index', [], Response::HTTP_SEE_OTHER);

            } catch (\Exception $e) {
                $this->addFlash('error', $this->translator->trans(
                    'productcategory.flash.create_failed',
                    ['%error%' => $e->getMessage()],
                    'productcategory'
                ));
            }
        }

        // Re-render with errors
        return $this->render('productcategory/_form_modal.html.twig', [
            'form' => $form,
            'productCategory' => $productCategory,
            'is_edit' => false,
        ], new Response('', $form->isSubmitted() ? Response::HTTP_UNPROCESSABLE_ENTITY : Response::HTTP_OK));
    }

    // ====================================
    // EDIT FORM ACTION - Display edit form
    // ====================================

    /**
     * Display edit productCategory form
     */
    protected function editFormAction(ProductCategory $productCategory, Request $request): Response
    {
        $this->denyAccessUnlessGranted(ProductCategoryVoter::EDIT, $productCategory);

        $form = $this->createForm(ProductCategoryFormType::class, $productCategory);

        return $this->render('productcategory/_form_modal.html.twig', [
            'form' => $form,
            'productCategory' => $productCategory,
            'is_edit' => true,
        ]);
    }

    // ====================================
    // UPDATE ACTION - Process edit form submission
    // ====================================

    /**
     * Process edit productCategory form submission
     */
    protected function updateAction(ProductCategory $productCategory, Request $request): Response
    {
        $this->denyAccessUnlessGranted(ProductCategoryVoter::EDIT, $productCategory);

        $form = $this->createForm(ProductCategoryFormType::class, $productCategory);
        $form->handleRequest($request);

        if ($form->isSubmitted() && $form->isValid()) {
            try {
                // Before update hook
                $this->beforeUpdate($productCategory);

                $this->entityManager->flush();

                // After update hook
                $this->afterUpdate($productCategory);

                $this->addFlash('success', $this->translator->trans(
                    'productcategory.flash.updated_successfully',
                    ['%name%' => (string) $productCategory],
                    'productcategory'
                ));

                return $this->redirectToRoute('productcategory_index', [], Response::HTTP_SEE_OTHER);

            } catch (\Exception $e) {
                $this->addFlash('error', $this->translator->trans(
                    'productcategory.flash.update_failed',
                    ['%error%' => $e->getMessage()],
                    'productcategory'
                ));
            }
        }

        // Re-render with errors
        return $this->render('productcategory/_form_modal.html.twig', [
            'form' => $form,
            'productCategory' => $productCategory,
            'is_edit' => true,
        ], new Response('', $form->isSubmitted() ? Response::HTTP_UNPROCESSABLE_ENTITY : Response::HTTP_OK));
    }

    // ====================================
    // DELETE ACTION - Delete productCategory
    // ====================================

    /**
     * Delete productCategory
     */
    protected function deleteAction(ProductCategory $productCategory, Request $request): Response
    {
        $this->denyAccessUnlessGranted(ProductCategoryVoter::DELETE, $productCategory);

        // Verify CSRF token
        $token = $request->request->get('_token');
        if (!$this->csrfTokenManager->isTokenValid(new CsrfToken('delete_productCategory_' . $productCategory->getId()->toString(), $token))) {
            throw new InvalidCsrfTokenException('Invalid CSRF token for delete operation.');
        }

        try {
            // Before delete hook
            $this->beforeDelete($productCategory);

            $name = (string) $productCategory;

            $this->entityManager->remove($productCategory);
            $this->entityManager->flush();

            // After delete hook
            $this->afterDelete();

            $this->addFlash('success', $this->translator->trans(
                'productcategory.flash.deleted_successfully',
                ['%name%' => $name],
                'productcategory'
            ));

        } catch (\Exception $e) {
            $this->addFlash('error', $this->translator->trans(
                'productcategory.flash.delete_failed',
                ['%error%' => $e->getMessage()],
                'productcategory'
            ));
        }

        return $this->redirectToRoute('productcategory_index', [], Response::HTTP_SEE_OTHER);
    }

    // ====================================
    // SHOW ACTION - Display productCategory details
    // ====================================

    /**
     * Display productCategory details
     */
    protected function showAction(ProductCategory $productCategory): Response
    {
        $this->denyAccessUnlessGranted(ProductCategoryVoter::VIEW, $productCategory);

        return $this->render('productcategory/show.html.twig', [
            'productCategory' => $productCategory,
        ]);
    }

    // ====================================
    // LIFECYCLE HOOKS
    // ====================================
    // Override these in ProductCategoryController to add custom logic

    /**
     * Initialize new entity before creating form
     *
     * Note: Organization and Owner are set automatically by TenantEntityProcessor
     * Only use this for custom initialization logic
     */
    protected function initializeNewEntity(ProductCategory $productCategory): void
    {
        // Organization and Owner are set automatically by TenantEntityProcessor
        // Add your custom initialization here
    }

    /**
     * Execute logic before creating entity
     */
    protected function beforeCreate(ProductCategory $productCategory): void
    {
        // Override to add logic before create
    }

    /**
     * Execute logic after creating entity
     */
    protected function afterCreate(ProductCategory $productCategory): void
    {
        // Override to add logic after create (e.g., send notification, trigger event)
    }

    /**
     * Execute logic before updating entity
     */
    protected function beforeUpdate(ProductCategory $productCategory): void
    {
        // Override to add logic before update
    }

    /**
     * Execute logic after updating entity
     */
    protected function afterUpdate(ProductCategory $productCategory): void
    {
        // Override to add logic after update (e.g., send notification, trigger event)
    }

    /**
     * Execute logic before deleting entity
     */
    protected function beforeDelete(ProductCategory $productCategory): void
    {
        // Override to add logic before delete (e.g., check dependencies, archive)
    }

    /**
     * Execute logic after deleting entity
     */
    protected function afterDelete(): void
    {
        // Override to add logic after delete (e.g., cleanup, send notification)
    }
}
