<?php

declare(strict_types=1);

namespace App\Controller\Generated;

use App\Controller\Base\BaseApiController;
use App\Entity\TalkType;
use App\Repository\TalkTypeRepository;
use App\Security\Voter\TalkTypeVoter;
use App\Form\TalkTypeType;
use App\Service\ListPreferencesService;
use Doctrine\ORM\EntityManagerInterface;
use Symfony\Component\HttpFoundation\Request;
use Symfony\Component\HttpFoundation\Response;
use Symfony\Component\HttpFoundation\JsonResponse;
use Symfony\Component\Security\Csrf\CsrfToken;
use Symfony\Component\Security\Csrf\CsrfTokenManagerInterface;
use Symfony\Component\Security\Core\Exception\InvalidCsrfTokenException;
use Symfony\Contracts\Translation\TranslatorInterface;

/**
 * Generated Base Controller for TalkType
 *
 * ⚠️ WARNING: This file is ALWAYS regenerated by Genmax
 * DO NOT EDIT THIS FILE - Edit TalkTypeController instead
 *
 * @generated
 * @codeCoverageIgnore
 */
abstract class TalkTypeControllerGenerated extends BaseApiController
{
    public function __construct(
        protected readonly EntityManagerInterface $entityManager,
        protected readonly TalkTypeRepository $repository,
        protected readonly ListPreferencesService $listPreferencesService,
        protected readonly TranslatorInterface $translator,
        protected readonly CsrfTokenManagerInterface $csrfTokenManager,
    ) {}

    // ====================================
    // ABSTRACT METHOD IMPLEMENTATIONS
    // ====================================

    /**
     * Get the repository for this entity
     */
    protected function getRepository(): TalkTypeRepository
    {
        return $this->repository;
    }

    /**
     * Get the plural name for this entity
     */
    protected function getEntityPluralName(): string
    {
        return 'talkTypes';
    }

    /**
     * Convert entity to array for API responses
     *
     * ⚠️ IMPORTANT: Enum Properties Stored as Strings
     * -------------------------------------------------
     * Properties with enum-backed types (like InputType) are stored as STRINGS
     * in the database and getters return strings directly, NOT enum objects.
     *
     * ❌ WRONG:   $entity->getType()->value   (Error: "Attempt to read property 'value' on string")
     * ✅ CORRECT: $entity->getType()          (Already a string - 'fully_completed')
     *
     * If you need the enum object for business logic, use:
     *    InputType::from($entity->getType())
     */
    protected function entityToArray(object $entity): array
    {
        if (!$entity instanceof TalkType) {
            throw new \InvalidArgumentException('Expected TalkType instance');
        }

        return [
            'id' => $entity->getId()->toString(),
            'organization' => ($organizationRel = $entity->getOrganization()) ? [
                'id' => $organizationRel->getId()->toString(),
                'display' => (string) $organizationRel,
            ] : null,
            'name' => $entity->getName(),
            'description' => $entity->getDescription(),
            'iconUrl' => $entity->getIconUrl(),
            'talks' => ($talksRel = $entity->getTalks()) ? array_map(
                fn($item) => [
                    'id' => $item->getId()->toString(),
                    'display' => (string) $item,
                ],
                $talksRel->toArray()
            ) : [],
        ];
    }

    // ====================================
    // INDEX ACTION - List all TalkTypes
    // ====================================

    /**
     * List TalkTypes
     */
    protected function indexAction(): Response
    {
        $this->denyAccessUnlessGranted(TalkTypeVoter::LIST);

        $preferences = $this->listPreferencesService->getEntityPreferences('talkTypes');
        $savedView = $preferences['view'] ?? 'grid';

        return $this->render('talktype/index.html.twig', [
            'entities' => [],  // Loaded via API
            'entity_name' => 'talktype',
            'entity_name_plural' => 'talkTypes',
            'page_icon' => 'bi-chat-left',
            'default_view' => $savedView,

            // List configuration from GeneratorProperty
            'enable_search' => true,
            'enable_filters' => false,
            'enable_sorting' => true,
            'enable_create_button' => true,
            'create_permission' => TalkTypeVoter::CREATE,

            // Property metadata for Twig templates (as PHP arrays)
            'listProperties' => json_decode('[{"name":"name","label":"Name","type":"string","sortable":true,"searchable":true,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getName","isRelationship":false},{"name":"description","label":"Description","type":"text","sortable":true,"searchable":true,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getDescription","isRelationship":false},{"name":"iconUrl","label":"IconUrl","type":"string","sortable":true,"searchable":true,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getIconUrl","isRelationship":false},{"name":"talks","label":"Talks","type":"","sortable":true,"searchable":false,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getTalks","isRelationship":true}]', true),
            'searchableFields' => json_decode('[{"name":"name","label":"Name","type":"string"},{"name":"description","label":"Description","type":"text"},{"name":"iconUrl","label":"IconUrl","type":"string"}]', true),
            'filterableFields' => json_decode('[]', true),
            'sortableFields' => json_decode('[{"name":"name","label":"Name"},{"name":"description","label":"Description"},{"name":"iconUrl","label":"IconUrl"},{"name":"talks","label":"Talks"}]', true),

            // Property metadata for client-side rendering (as JSON strings)
            'list_fields' => '[{"name":"name","label":"Name","type":"string","sortable":true,"searchable":true,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getName","isRelationship":false},{"name":"description","label":"Description","type":"text","sortable":true,"searchable":true,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getDescription","isRelationship":false},{"name":"iconUrl","label":"IconUrl","type":"string","sortable":true,"searchable":true,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getIconUrl","isRelationship":false},{"name":"talks","label":"Talks","type":"","sortable":true,"searchable":false,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getTalks","isRelationship":true}]',
            'searchable_fields' => '[{"name":"name","label":"Name","type":"string"},{"name":"description","label":"Description","type":"text"},{"name":"iconUrl","label":"IconUrl","type":"string"}]',
            'filterable_fields' => '[]',
            'sortable_fields' => '[{"name":"name","label":"Name"},{"name":"description","label":"Description"},{"name":"iconUrl","label":"IconUrl"},{"name":"talks","label":"Talks"}]',
        ]);
    }

    // ====================================
    // API SEARCH - Fetch TalkTypes for list view
    // ====================================

    /**
     * API endpoint for searching TalkTypes
     */
    protected function apiSearchAction(Request $request): JsonResponse
    {
        $this->denyAccessUnlessGranted(TalkTypeVoter::LIST);

        // Delegate to parent BaseApiController which handles
        // search, filtering, sorting, and pagination
        return parent::apiSearchAction($request);
    }

    // ====================================
    // NEW FORM ACTION - Display create form
    // ====================================

    /**
     * Display new talkType form
     */
    protected function newFormAction(Request $request): Response
    {
        $this->denyAccessUnlessGranted(TalkTypeVoter::CREATE);

        $talkType = new TalkType();

        // Initialize with custom logic if needed
        $this->initializeNewEntity($talkType);

        $form = $this->createForm(TalkTypeType::class, $talkType);

        return $this->render('talktype/_form_modal.html.twig', [
            'form' => $form,
            'talkType' => $talkType,
            'is_edit' => false,
        ]);
    }

    // ====================================
    // CREATE ACTION - Process create form submission
    // ====================================

    /**
     * Process new talkType form submission
     */
    protected function createAction(Request $request): Response
    {
        $this->denyAccessUnlessGranted(TalkTypeVoter::CREATE);

        $talkType = new TalkType();

        // Initialize with custom logic if needed
        $this->initializeNewEntity($talkType);

        $form = $this->createForm(TalkTypeType::class, $talkType);
        $form->handleRequest($request);

        if ($form->isSubmitted() && $form->isValid()) {
            try {
                // Before create hook
                $this->beforeCreate($talkType);

                $this->entityManager->persist($talkType);
                $this->entityManager->flush();

                // After create hook
                $this->afterCreate($talkType);

                $this->addFlash('success', $this->translator->trans(
                    'talktype.flash.created_successfully',
                    ['%name%' => (string) $talkType],
                    'talktype'
                ));

                return $this->redirectToRoute('talktype_index', [], Response::HTTP_SEE_OTHER);

            } catch (\Exception $e) {
                $this->addFlash('error', $this->translator->trans(
                    'talktype.flash.create_failed',
                    ['%error%' => $e->getMessage()],
                    'talktype'
                ));
            }
        }

        // Re-render with errors
        return $this->render('talktype/_form_modal.html.twig', [
            'form' => $form,
            'talkType' => $talkType,
            'is_edit' => false,
        ], new Response('', $form->isSubmitted() ? Response::HTTP_UNPROCESSABLE_ENTITY : Response::HTTP_OK));
    }

    // ====================================
    // EDIT FORM ACTION - Display edit form
    // ====================================

    /**
     * Display edit talkType form
     */
    protected function editFormAction(TalkType $talkType, Request $request): Response
    {
        $this->denyAccessUnlessGranted(TalkTypeVoter::EDIT, $talkType);

        $form = $this->createForm(TalkTypeType::class, $talkType);

        return $this->render('talktype/_form_modal.html.twig', [
            'form' => $form,
            'talkType' => $talkType,
            'is_edit' => true,
        ]);
    }

    // ====================================
    // UPDATE ACTION - Process edit form submission
    // ====================================

    /**
     * Process edit talkType form submission
     */
    protected function updateAction(TalkType $talkType, Request $request): Response
    {
        $this->denyAccessUnlessGranted(TalkTypeVoter::EDIT, $talkType);

        $form = $this->createForm(TalkTypeType::class, $talkType);
        $form->handleRequest($request);

        if ($form->isSubmitted() && $form->isValid()) {
            try {
                // Before update hook
                $this->beforeUpdate($talkType);

                $this->entityManager->flush();

                // After update hook
                $this->afterUpdate($talkType);

                $this->addFlash('success', $this->translator->trans(
                    'talktype.flash.updated_successfully',
                    ['%name%' => (string) $talkType],
                    'talktype'
                ));

                return $this->redirectToRoute('talktype_index', [], Response::HTTP_SEE_OTHER);

            } catch (\Exception $e) {
                $this->addFlash('error', $this->translator->trans(
                    'talktype.flash.update_failed',
                    ['%error%' => $e->getMessage()],
                    'talktype'
                ));
            }
        }

        // Re-render with errors
        return $this->render('talktype/_form_modal.html.twig', [
            'form' => $form,
            'talkType' => $talkType,
            'is_edit' => true,
        ], new Response('', $form->isSubmitted() ? Response::HTTP_UNPROCESSABLE_ENTITY : Response::HTTP_OK));
    }

    // ====================================
    // DELETE ACTION - Delete talkType
    // ====================================

    /**
     * Delete talkType
     */
    protected function deleteAction(TalkType $talkType, Request $request): Response
    {
        $this->denyAccessUnlessGranted(TalkTypeVoter::DELETE, $talkType);

        // Verify CSRF token
        $token = $request->request->get('_token');
        if (!$this->csrfTokenManager->isTokenValid(new CsrfToken('delete_talkType_' . $talkType->getId()->toString(), $token))) {
            throw new InvalidCsrfTokenException('Invalid CSRF token for delete operation.');
        }

        try {
            // Before delete hook
            $this->beforeDelete($talkType);

            $name = (string) $talkType;

            $this->entityManager->remove($talkType);
            $this->entityManager->flush();

            // After delete hook
            $this->afterDelete();

            $this->addFlash('success', $this->translator->trans(
                'talktype.flash.deleted_successfully',
                ['%name%' => $name],
                'talktype'
            ));

        } catch (\Exception $e) {
            $this->addFlash('error', $this->translator->trans(
                'talktype.flash.delete_failed',
                ['%error%' => $e->getMessage()],
                'talktype'
            ));
        }

        return $this->redirectToRoute('talktype_index', [], Response::HTTP_SEE_OTHER);
    }

    // ====================================
    // SHOW ACTION - Display talkType details
    // ====================================

    /**
     * Display talkType details
     */
    protected function showAction(TalkType $talkType): Response
    {
        $this->denyAccessUnlessGranted(TalkTypeVoter::VIEW, $talkType);

        return $this->render('talktype/show.html.twig', [
            'talkType' => $talkType,
        ]);
    }

    // ====================================
    // LIFECYCLE HOOKS
    // ====================================
    // Override these in TalkTypeController to add custom logic

    /**
     * Initialize new entity before creating form
     *
     * Note: Organization and Owner are set automatically by TenantEntityProcessor
     * Only use this for custom initialization logic
     */
    protected function initializeNewEntity(TalkType $talkType): void
    {
        // Organization and Owner are set automatically by TenantEntityProcessor
        // Add your custom initialization here
    }

    /**
     * Execute logic before creating entity
     */
    protected function beforeCreate(TalkType $talkType): void
    {
        // Override to add logic before create
    }

    /**
     * Execute logic after creating entity
     */
    protected function afterCreate(TalkType $talkType): void
    {
        // Override to add logic after create (e.g., send notification, trigger event)
    }

    /**
     * Execute logic before updating entity
     */
    protected function beforeUpdate(TalkType $talkType): void
    {
        // Override to add logic before update
    }

    /**
     * Execute logic after updating entity
     */
    protected function afterUpdate(TalkType $talkType): void
    {
        // Override to add logic after update (e.g., send notification, trigger event)
    }

    /**
     * Execute logic before deleting entity
     */
    protected function beforeDelete(TalkType $talkType): void
    {
        // Override to add logic before delete (e.g., check dependencies, archive)
    }

    /**
     * Execute logic after deleting entity
     */
    protected function afterDelete(): void
    {
        // Override to add logic after delete (e.g., cleanup, send notification)
    }
}
