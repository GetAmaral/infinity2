<?php

declare(strict_types=1);

namespace App\Controller\Generated;

use App\Entity\Brand;
use App\Form\BrandType;
use App\Repository\BrandRepository;
use App\Service\OrganizationContext;
use Symfony\Bundle\FrameworkBundle\Controller\AbstractController;
use Symfony\Component\HttpFoundation\Request;
use Symfony\Component\HttpFoundation\Response;
use Symfony\Component\Routing\Attribute\Route;
use Symfony\Component\Security\Http\Attribute\IsGranted;

/**
 * Brand Controller (Generated Base Class)
 *
 * This class is ALWAYS regenerated from CSV. DO NOT edit this file.
 * Add custom actions to BrandController.php instead.
 *
 * @generated by Luminai Code Generator
 */
#[Route('/brand')]
abstract class BrandControllerGenerated extends AbstractController
{
    public function __construct(
        protected readonly BrandRepository $repository,
        protected readonly OrganizationContext $organizationContext
    ) {}

    /**
     * List all Brands
     */
    #[Route('', name: 'brand_index', methods: ['GET'])]
        #[IsGranted('ROLE_DATA_ADMIN')]
    public function index(Request $request): Response
    {
        $page = max(1, $request->query->getInt('page', 1));
        $search = $request->query->get('search', '');

        $brands = $this->repository->findPaginated($page, 30);
        $total = $this->repository->count();

        return $this->render('brand/index.html.twig', [
            'brands' => $brands,
            'total' => $total,
            'page' => $page,
            'pages' => ceil($total / 30),
            'search' => $search,
        ]);
    }

    /**
     * Show Brand details
     */
    #[Route('/{id}', name: 'brand_show', methods: ['GET'])]
        #[IsGranted('ROLE_DATA_ADMIN')]
    public function show(Brand $brand): Response
    {
        $this->denyAccessUnlessGranted('VIEW', $brand);

        return $this->render('brand/show.html.twig', [
            'brand' => $brand,
        ]);
    }

    /**
     * Create new Brand
     */
    #[Route('/new', name: 'brand_new', methods: ['GET', 'POST'])]
        #[IsGranted('ROLE_DATA_ADMIN')]
    public function new(Request $request): Response
    {
        $brand = new Brand();

        // Set organization from context
        $organization = $this->organizationContext->getOrganization();
        if ($organization) {
            $brand->setOrganization($organization);
        }

        $form = $this->createForm(BrandType::class, $brand);
        $form->handleRequest($request);

        if ($form->isSubmitted() && $form->isValid()) {
            $this->repository->save($brand, true);

            $this->addFlash('success', 'Brand created successfully.');

            return $this->redirectToRoute('brand_index');
        }

        return $this->render('brand/form.html.twig', [
            'brand' => $brand,
            'form' => $form,
            'is_edit' => false,
        ]);
    }

    /**
     * Edit Brand
     */
    #[Route('/{id}/edit', name: 'brand_edit', methods: ['GET', 'POST'])]
        #[IsGranted('ROLE_DATA_ADMIN')]
    public function edit(Request $request, Brand $brand): Response
    {
        $this->denyAccessUnlessGranted('EDIT', $brand);

        $form = $this->createForm(BrandType::class, $brand);
        $form->handleRequest($request);

        if ($form->isSubmitted() && $form->isValid()) {
            $this->repository->save($brand, true);

            $this->addFlash('success', 'Brand updated successfully.');

            return $this->redirectToRoute('brand_index');
        }

        return $this->render('brand/form.html.twig', [
            'brand' => $brand,
            'form' => $form,
            'is_edit' => true,
        ]);
    }

    /**
     * Delete Brand
     */
    #[Route('/{id}', name: 'brand_delete', methods: ['POST'])]
        #[IsGranted('ROLE_DATA_ADMIN')]
    public function delete(Request $request, Brand $brand): Response
    {
        $this->denyAccessUnlessGranted('DELETE', $brand);

        if ($this->isCsrfTokenValid('delete_brand_' . $brand->getId(), $request->request->get('_token'))) {
            $this->repository->remove($brand, true);

            $this->addFlash('success', 'Brand deleted successfully.');
        }

        return $this->redirectToRoute('brand_index');
    }
}
