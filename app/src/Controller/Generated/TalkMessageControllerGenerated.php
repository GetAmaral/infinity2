<?php

declare(strict_types=1);

namespace App\Controller\Generated;

use App\Entity\TalkMessage;
use App\Form\TalkMessageType;
use App\Repository\TalkMessageRepository;
use App\Service\OrganizationContext;
use Symfony\Bundle\FrameworkBundle\Controller\AbstractController;
use Symfony\Component\HttpFoundation\Request;
use Symfony\Component\HttpFoundation\Response;
use Symfony\Component\Routing\Attribute\Route;
use Symfony\Component\Security\Http\Attribute\IsGranted;

/**
 * TalkMessage Controller (Generated Base Class)
 *
 * This class is ALWAYS regenerated from CSV. DO NOT edit this file.
 * Add custom actions to TalkMessageController.php instead.
 *
 * @generated by Luminai Code Generator
 */
#[Route('/talk-message')]
abstract class TalkMessageControllerGenerated extends AbstractController
{
    public function __construct(
        protected readonly TalkMessageRepository $repository,
        protected readonly OrganizationContext $organizationContext
    ) {}

    /**
     * List all TalkMessages
     */
    #[Route('', name: 'talk_message_index', methods: ['GET'])]
        #[IsGranted('ROLE_SUPPORT_AGENT')]
    public function index(Request $request): Response
    {
        $page = max(1, $request->query->getInt('page', 1));
        $search = $request->query->get('search', '');

        $talkmessages = $this->repository->findPaginated($page, 30);
        $total = $this->repository->count();

        return $this->render('talkmessage/index.html.twig', [
            'talkmessages' => $talkmessages,
            'total' => $total,
            'page' => $page,
            'pages' => ceil($total / 30),
            'search' => $search,
        ]);
    }

    /**
     * Show TalkMessage details
     */
    #[Route('/{id}', name: 'talk_message_show', methods: ['GET'])]
        #[IsGranted('ROLE_SUPPORT_AGENT')]
    public function show(TalkMessage $talkmessage): Response
    {
        $this->denyAccessUnlessGranted('VIEW', $talkmessage);

        return $this->render('talkmessage/show.html.twig', [
            'talkmessage' => $talkmessage,
        ]);
    }

    /**
     * Create new TalkMessage
     */
    #[Route('/new', name: 'talk_message_new', methods: ['GET', 'POST'])]
        #[IsGranted('ROLE_SUPPORT_AGENT')]
    public function new(Request $request): Response
    {
        $talkmessage = new TalkMessage();

        // Set organization from context
        $organization = $this->organizationContext->getOrganization();
        if ($organization) {
            $talkmessage->setOrganization($organization);
        }

        $form = $this->createForm(TalkMessageType::class, $talkmessage);
        $form->handleRequest($request);

        if ($form->isSubmitted() && $form->isValid()) {
            $this->repository->save($talkmessage, true);

            $this->addFlash('success', 'TalkMessage created successfully.');

            return $this->redirectToRoute('talk_message_index');
        }

        return $this->render('talkmessage/form.html.twig', [
            'talkmessage' => $talkmessage,
            'form' => $form,
            'is_edit' => false,
        ]);
    }

    /**
     * Edit TalkMessage
     */
    #[Route('/{id}/edit', name: 'talk_message_edit', methods: ['GET', 'POST'])]
        #[IsGranted('ROLE_SUPPORT_AGENT')]
    public function edit(Request $request, TalkMessage $talkmessage): Response
    {
        $this->denyAccessUnlessGranted('EDIT', $talkmessage);

        $form = $this->createForm(TalkMessageType::class, $talkmessage);
        $form->handleRequest($request);

        if ($form->isSubmitted() && $form->isValid()) {
            $this->repository->save($talkmessage, true);

            $this->addFlash('success', 'TalkMessage updated successfully.');

            return $this->redirectToRoute('talk_message_index');
        }

        return $this->render('talkmessage/form.html.twig', [
            'talkmessage' => $talkmessage,
            'form' => $form,
            'is_edit' => true,
        ]);
    }

    /**
     * Delete TalkMessage
     */
    #[Route('/{id}', name: 'talk_message_delete', methods: ['POST'])]
        #[IsGranted('ROLE_SUPPORT_AGENT')]
    public function delete(Request $request, TalkMessage $talkmessage): Response
    {
        $this->denyAccessUnlessGranted('DELETE', $talkmessage);

        if ($this->isCsrfTokenValid('delete_talk_message_' . $talkmessage->getId(), $request->request->get('_token'))) {
            $this->repository->remove($talkmessage, true);

            $this->addFlash('success', 'TalkMessage deleted successfully.');
        }

        return $this->redirectToRoute('talk_message_index');
    }
}
