<?php

declare(strict_types=1);

namespace App\Controller\Generated;

use App\Controller\Base\BaseApiController;
use App\Entity\CourseModule;
use App\Repository\CourseModuleRepository;
use App\Security\Voter\CourseModuleVoter;
use App\Form\CourseModuleFormType;
use App\Service\ListPreferencesService;
use Doctrine\ORM\EntityManagerInterface;
use Symfony\Component\HttpFoundation\Request;
use Symfony\Component\HttpFoundation\Response;
use Symfony\Component\HttpFoundation\JsonResponse;
use Symfony\Component\Security\Csrf\CsrfToken;
use Symfony\Component\Security\Csrf\CsrfTokenManagerInterface;
use Symfony\Component\Security\Core\Exception\InvalidCsrfTokenException;
use Symfony\Contracts\Translation\TranslatorInterface;

/**
 * Generated Base Controller for CourseModule
 *
 * ⚠️ WARNING: This file is ALWAYS regenerated by Genmax
 * DO NOT EDIT THIS FILE - Edit CourseModuleController instead
 *
 * @generated
 * @codeCoverageIgnore
 */
abstract class CourseModuleControllerGenerated extends BaseApiController
{
    public function __construct(
        protected readonly EntityManagerInterface $entityManager,
        protected readonly CourseModuleRepository $repository,
        protected readonly ListPreferencesService $listPreferencesService,
        protected readonly TranslatorInterface $translator,
        protected readonly CsrfTokenManagerInterface $csrfTokenManager,
    ) {}

    // ====================================
    // ABSTRACT METHOD IMPLEMENTATIONS
    // ====================================

    /**
     * Get the repository for this entity
     */
    protected function getRepository(): CourseModuleRepository
    {
        return $this->repository;
    }

    /**
     * Get the plural name for this entity
     */
    protected function getEntityPluralName(): string
    {
        return 'courseModules';
    }

    /**
     * Convert entity to array for API responses
     */
    protected function entityToArray(object $entity): array
    {
        if (!$entity instanceof CourseModule) {
            throw new \InvalidArgumentException('Expected CourseModule instance');
        }

        return [
            'id' => $entity->getId()->toString(),
            'name' => $entity->getName(),
            'description' => $entity->getDescription(),
            'releaseDate' => $entity->getReleaseDate()?->format('M d, Y'),
            'viewOrder' => $entity->getViewOrder(),
            'totalLengthSeconds' => $entity->getTotalLengthSeconds(),
            'course' => ($courseRel = $entity->getCourse()) ? [
                'id' => $courseRel->getId()->toString(),
                'display' => (string) $courseRel,
            ] : null,
            'lectures' => ($lecturesRel = $entity->getLectures()) ? [
                'id' => $lecturesRel->getId()->toString(),
                'display' => (string) $lecturesRel,
            ] : null,
            'active' => $entity->getActive(),
        ];
    }

    // ====================================
    // INDEX ACTION - List all CourseModules
    // ====================================

    /**
     * List CourseModules
     */
    protected function indexAction(): Response
    {
        $this->denyAccessUnlessGranted(CourseModuleVoter::LIST);

        $preferences = $this->listPreferencesService->getEntityPreferences('courseModules');
        $savedView = $preferences['view'] ?? 'grid';

        return $this->render('coursemodule/index.html.twig', [
            'entities' => [],  // Loaded via API
            'entity_name' => 'courseModule',
            'entity_name_plural' => 'courseModules',
            'page_icon' => 'bi-journal-text',
            'default_view' => $savedView,

            // List configuration from GeneratorProperty
            'enable_search' => true,
            'enable_filters' => false,
            'enable_sorting' => true,
            'enable_create_button' => true,

            // Property metadata for client-side rendering (as JSON strings)
            'list_fields' => '[{"name":"name","label":"Name","type":"string","sortable":false,"searchable":true,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getName"},{"name":"course","label":"Course","type":"","sortable":false,"searchable":false,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getCourse"},{"name":"active","label":"Active","type":"boolean","sortable":false,"searchable":false,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getActive"}]',
            'searchable_fields' => '[{"name":"name","label":"Name","type":"string"}]',
            'filterable_fields' => '[]',
            'sortable_fields' => '[{"name":"viewOrder","label":"View Order"}]',
        ]);
    }

    // ====================================
    // API SEARCH - Fetch CourseModules for list view
    // ====================================

    /**
     * API endpoint for searching CourseModules
     */
    protected function apiSearchAction(Request $request): JsonResponse
    {
        $this->denyAccessUnlessGranted(CourseModuleVoter::LIST);

        // This method uses the BaseApiController's handleSearchRequest
        // which integrates with API Platform's GetCollection operation
        return $this->handleSearchRequest($request);
    }

    // ====================================
    // NEW FORM ACTION - Display create form
    // ====================================

    /**
     * Display new courseModule form
     */
    protected function newFormAction(Request $request): Response
    {
        $this->denyAccessUnlessGranted(CourseModuleVoter::CREATE);

        $courseModule = new CourseModule();

        // Initialize with custom logic if needed
        $this->initializeNewEntity($courseModule);

        $form = $this->createForm(CourseModuleFormType::class, $courseModule);

        return $this->render('coursemodule/_form_modal.html.twig', [
            'form' => $form,
            'courseModule' => $courseModule,
            'is_edit' => false,
        ]);
    }

    // ====================================
    // CREATE ACTION - Process create form submission
    // ====================================

    /**
     * Process new courseModule form submission
     */
    protected function createAction(Request $request): Response
    {
        $this->denyAccessUnlessGranted(CourseModuleVoter::CREATE);

        $courseModule = new CourseModule();

        // Initialize with custom logic if needed
        $this->initializeNewEntity($courseModule);

        $form = $this->createForm(CourseModuleFormType::class, $courseModule);
        $form->handleRequest($request);

        if ($form->isSubmitted() && $form->isValid()) {
            try {
                // Before create hook
                $this->beforeCreate($courseModule);

                $this->entityManager->persist($courseModule);
                $this->entityManager->flush();

                // After create hook
                $this->afterCreate($courseModule);

                $this->addFlash('success', $this->translator->trans(
                    'coursemodule.flash.created_successfully',
                    ['%name%' => (string) $courseModule],
                    'coursemodule'
                ));

                return $this->redirectToRoute('coursemodule_index', [], Response::HTTP_SEE_OTHER);

            } catch (\Exception $e) {
                $this->addFlash('error', $this->translator->trans(
                    'coursemodule.flash.create_failed',
                    ['%error%' => $e->getMessage()],
                    'coursemodule'
                ));
            }
        }

        // Re-render with errors
        return $this->render('coursemodule/_form_modal.html.twig', [
            'form' => $form,
            'courseModule' => $courseModule,
            'is_edit' => false,
        ], new Response('', $form->isSubmitted() ? Response::HTTP_UNPROCESSABLE_ENTITY : Response::HTTP_OK));
    }

    // ====================================
    // EDIT FORM ACTION - Display edit form
    // ====================================

    /**
     * Display edit courseModule form
     */
    protected function editFormAction(CourseModule $courseModule, Request $request): Response
    {
        $this->denyAccessUnlessGranted(CourseModuleVoter::EDIT, $courseModule);

        $form = $this->createForm(CourseModuleFormType::class, $courseModule);

        return $this->render('coursemodule/_form_modal.html.twig', [
            'form' => $form,
            'courseModule' => $courseModule,
            'is_edit' => true,
        ]);
    }

    // ====================================
    // UPDATE ACTION - Process edit form submission
    // ====================================

    /**
     * Process edit courseModule form submission
     */
    protected function updateAction(CourseModule $courseModule, Request $request): Response
    {
        $this->denyAccessUnlessGranted(CourseModuleVoter::EDIT, $courseModule);

        $form = $this->createForm(CourseModuleFormType::class, $courseModule);
        $form->handleRequest($request);

        if ($form->isSubmitted() && $form->isValid()) {
            try {
                // Before update hook
                $this->beforeUpdate($courseModule);

                $this->entityManager->flush();

                // After update hook
                $this->afterUpdate($courseModule);

                $this->addFlash('success', $this->translator->trans(
                    'coursemodule.flash.updated_successfully',
                    ['%name%' => (string) $courseModule],
                    'coursemodule'
                ));

                return $this->redirectToRoute('coursemodule_index', [], Response::HTTP_SEE_OTHER);

            } catch (\Exception $e) {
                $this->addFlash('error', $this->translator->trans(
                    'coursemodule.flash.update_failed',
                    ['%error%' => $e->getMessage()],
                    'coursemodule'
                ));
            }
        }

        // Re-render with errors
        return $this->render('coursemodule/_form_modal.html.twig', [
            'form' => $form,
            'courseModule' => $courseModule,
            'is_edit' => true,
        ], new Response('', $form->isSubmitted() ? Response::HTTP_UNPROCESSABLE_ENTITY : Response::HTTP_OK));
    }

    // ====================================
    // DELETE ACTION - Delete courseModule
    // ====================================

    /**
     * Delete courseModule
     */
    protected function deleteAction(CourseModule $courseModule, Request $request): Response
    {
        $this->denyAccessUnlessGranted(CourseModuleVoter::DELETE, $courseModule);

        // Verify CSRF token
        $token = $request->request->get('_token');
        if (!$this->csrfTokenManager->isTokenValid(new CsrfToken('delete_courseModule_' . $courseModule->getId()->toString(), $token))) {
            throw new InvalidCsrfTokenException('Invalid CSRF token for delete operation.');
        }

        try {
            // Before delete hook
            $this->beforeDelete($courseModule);

            $name = (string) $courseModule;

            $this->entityManager->remove($courseModule);
            $this->entityManager->flush();

            // After delete hook
            $this->afterDelete();

            $this->addFlash('success', $this->translator->trans(
                'coursemodule.flash.deleted_successfully',
                ['%name%' => $name],
                'coursemodule'
            ));

        } catch (\Exception $e) {
            $this->addFlash('error', $this->translator->trans(
                'coursemodule.flash.delete_failed',
                ['%error%' => $e->getMessage()],
                'coursemodule'
            ));
        }

        return $this->redirectToRoute('coursemodule_index', [], Response::HTTP_SEE_OTHER);
    }

    // ====================================
    // SHOW ACTION - Display courseModule details
    // ====================================

    /**
     * Display courseModule details
     */
    protected function showAction(CourseModule $courseModule): Response
    {
        $this->denyAccessUnlessGranted(CourseModuleVoter::VIEW, $courseModule);

        return $this->render('coursemodule/show.html.twig', [
            'courseModule' => $courseModule,
        ]);
    }

    // ====================================
    // LIFECYCLE HOOKS
    // ====================================
    // Override these in CourseModuleController to add custom logic

    /**
     * Initialize new entity before creating form
     *
     * Note: Organization and Owner are set automatically by TenantEntityProcessor
     * Only use this for custom initialization logic
     */
    protected function initializeNewEntity(CourseModule $courseModule): void
    {
        // Organization and Owner are set automatically by TenantEntityProcessor
        // Add your custom initialization here
    }

    /**
     * Execute logic before creating entity
     */
    protected function beforeCreate(CourseModule $courseModule): void
    {
        // Override to add logic before create
    }

    /**
     * Execute logic after creating entity
     */
    protected function afterCreate(CourseModule $courseModule): void
    {
        // Override to add logic after create (e.g., send notification, trigger event)
    }

    /**
     * Execute logic before updating entity
     */
    protected function beforeUpdate(CourseModule $courseModule): void
    {
        // Override to add logic before update
    }

    /**
     * Execute logic after updating entity
     */
    protected function afterUpdate(CourseModule $courseModule): void
    {
        // Override to add logic after update (e.g., send notification, trigger event)
    }

    /**
     * Execute logic before deleting entity
     */
    protected function beforeDelete(CourseModule $courseModule): void
    {
        // Override to add logic before delete (e.g., check dependencies, archive)
    }

    /**
     * Execute logic after deleting entity
     */
    protected function afterDelete(): void
    {
        // Override to add logic after delete (e.g., cleanup, send notification)
    }
}
