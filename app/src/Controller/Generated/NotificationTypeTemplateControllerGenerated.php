<?php

declare(strict_types=1);

namespace App\Controller\Generated;

use App\Controller\Base\BaseApiController;
use App\Entity\NotificationTypeTemplate;
use App\Repository\NotificationTypeTemplateRepository;
use App\Security\Voter\NotificationTypeTemplateVoter;
use App\Form\NotificationTypeTemplateFormType;
use App\Service\ListPreferencesService;
use Doctrine\ORM\EntityManagerInterface;
use Symfony\Component\HttpFoundation\Request;
use Symfony\Component\HttpFoundation\Response;
use Symfony\Component\HttpFoundation\JsonResponse;
use Symfony\Component\Security\Csrf\CsrfToken;
use Symfony\Component\Security\Csrf\CsrfTokenManagerInterface;
use Symfony\Component\Security\Core\Exception\InvalidCsrfTokenException;
use Symfony\Contracts\Translation\TranslatorInterface;

/**
 * Generated Base Controller for NotificationTypeTemplate
 *
 * ⚠️ WARNING: This file is ALWAYS regenerated by Genmax
 * DO NOT EDIT THIS FILE - Edit NotificationTypeTemplateController instead
 *
 * @generated
 * @codeCoverageIgnore
 */
abstract class NotificationTypeTemplateControllerGenerated extends BaseApiController
{
    public function __construct(
        protected readonly EntityManagerInterface $entityManager,
        protected readonly NotificationTypeTemplateRepository $repository,
        protected readonly ListPreferencesService $listPreferencesService,
        protected readonly TranslatorInterface $translator,
        protected readonly CsrfTokenManagerInterface $csrfTokenManager,
    ) {}

    // ====================================
    // ABSTRACT METHOD IMPLEMENTATIONS
    // ====================================

    /**
     * Get the repository for this entity
     */
    protected function getRepository(): NotificationTypeTemplateRepository
    {
        return $this->repository;
    }

    /**
     * Get the plural name for this entity
     */
    protected function getEntityPluralName(): string
    {
        return 'notificationTypeTemplates';
    }

    /**
     * Convert entity to array for API responses
     */
    protected function entityToArray(object $entity): array
    {
        if (!$entity instanceof NotificationTypeTemplate) {
            throw new \InvalidArgumentException('Expected NotificationTypeTemplate instance');
        }

        return [
            'id' => $entity->getId()->toString(),
            'name' => $entity->getName(),
            'description' => $entity->getDescription(),
        ];
    }

    // ====================================
    // INDEX ACTION - List all NotificationTypeTemplates
    // ====================================

    /**
     * List NotificationTypeTemplates
     */
    protected function indexAction(): Response
    {
        $this->denyAccessUnlessGranted(NotificationTypeTemplateVoter::LIST);

        $preferences = $this->listPreferencesService->getEntityPreferences('notificationTypeTemplates');
        $savedView = $preferences['view'] ?? 'grid';

        return $this->render('notificationtypetemplate/index.html.twig', [
            'entities' => [],  // Loaded via API
            'entity_name' => 'notificationTypeTemplate',
            'entity_name_plural' => 'notificationTypeTemplates',
            'page_icon' => 'bi-bell-slash',
            'default_view' => $savedView,

            // List configuration from GeneratorProperty
            'enable_search' => true,
            'enable_filters' => false,
            'enable_sorting' => true,
            'enable_create_button' => true,

            // Property metadata for client-side rendering (as JSON strings)
            'list_fields' => '[{"name":"name","label":"Name","type":"string","sortable":true,"searchable":true,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getName"},{"name":"description","label":"Description","type":"text","sortable":true,"searchable":true,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getDescription"}]',
            'searchable_fields' => '[{"name":"name","label":"Name","type":"string"},{"name":"description","label":"Description","type":"text"}]',
            'filterable_fields' => '[]',
            'sortable_fields' => '[{"name":"name","label":"Name"},{"name":"description","label":"Description"}]',
        ]);
    }

    // ====================================
    // API SEARCH - Fetch NotificationTypeTemplates for list view
    // ====================================

    /**
     * API endpoint for searching NotificationTypeTemplates
     */
    protected function apiSearchAction(Request $request): JsonResponse
    {
        $this->denyAccessUnlessGranted(NotificationTypeTemplateVoter::LIST);

        // This method uses the BaseApiController's handleSearchRequest
        // which integrates with API Platform's GetCollection operation
        return $this->handleSearchRequest($request);
    }

    // ====================================
    // NEW FORM ACTION - Display create form
    // ====================================

    /**
     * Display new notificationTypeTemplate form
     */
    protected function newFormAction(Request $request): Response
    {
        $this->denyAccessUnlessGranted(NotificationTypeTemplateVoter::CREATE);

        $notificationTypeTemplate = new NotificationTypeTemplate();

        // Initialize with custom logic if needed
        $this->initializeNewEntity($notificationTypeTemplate);

        $form = $this->createForm(NotificationTypeTemplateFormType::class, $notificationTypeTemplate);

        return $this->render('notificationtypetemplate/_form_modal.html.twig', [
            'form' => $form,
            'notificationTypeTemplate' => $notificationTypeTemplate,
            'is_edit' => false,
        ]);
    }

    // ====================================
    // CREATE ACTION - Process create form submission
    // ====================================

    /**
     * Process new notificationTypeTemplate form submission
     */
    protected function createAction(Request $request): Response
    {
        $this->denyAccessUnlessGranted(NotificationTypeTemplateVoter::CREATE);

        $notificationTypeTemplate = new NotificationTypeTemplate();

        // Initialize with custom logic if needed
        $this->initializeNewEntity($notificationTypeTemplate);

        $form = $this->createForm(NotificationTypeTemplateFormType::class, $notificationTypeTemplate);
        $form->handleRequest($request);

        if ($form->isSubmitted() && $form->isValid()) {
            try {
                // Before create hook
                $this->beforeCreate($notificationTypeTemplate);

                $this->entityManager->persist($notificationTypeTemplate);
                $this->entityManager->flush();

                // After create hook
                $this->afterCreate($notificationTypeTemplate);

                $this->addFlash('success', $this->translator->trans(
                    'notificationtypetemplate.flash.created_successfully',
                    ['%name%' => (string) $notificationTypeTemplate],
                    'notificationtypetemplate'
                ));

                return $this->redirectToRoute('notificationtypetemplate_index', [], Response::HTTP_SEE_OTHER);

            } catch (\Exception $e) {
                $this->addFlash('error', $this->translator->trans(
                    'notificationtypetemplate.flash.create_failed',
                    ['%error%' => $e->getMessage()],
                    'notificationtypetemplate'
                ));
            }
        }

        // Re-render with errors
        return $this->render('notificationtypetemplate/_form_modal.html.twig', [
            'form' => $form,
            'notificationTypeTemplate' => $notificationTypeTemplate,
            'is_edit' => false,
        ], new Response('', $form->isSubmitted() ? Response::HTTP_UNPROCESSABLE_ENTITY : Response::HTTP_OK));
    }

    // ====================================
    // EDIT FORM ACTION - Display edit form
    // ====================================

    /**
     * Display edit notificationTypeTemplate form
     */
    protected function editFormAction(NotificationTypeTemplate $notificationTypeTemplate, Request $request): Response
    {
        $this->denyAccessUnlessGranted(NotificationTypeTemplateVoter::EDIT, $notificationTypeTemplate);

        $form = $this->createForm(NotificationTypeTemplateFormType::class, $notificationTypeTemplate);

        return $this->render('notificationtypetemplate/_form_modal.html.twig', [
            'form' => $form,
            'notificationTypeTemplate' => $notificationTypeTemplate,
            'is_edit' => true,
        ]);
    }

    // ====================================
    // UPDATE ACTION - Process edit form submission
    // ====================================

    /**
     * Process edit notificationTypeTemplate form submission
     */
    protected function updateAction(NotificationTypeTemplate $notificationTypeTemplate, Request $request): Response
    {
        $this->denyAccessUnlessGranted(NotificationTypeTemplateVoter::EDIT, $notificationTypeTemplate);

        $form = $this->createForm(NotificationTypeTemplateFormType::class, $notificationTypeTemplate);
        $form->handleRequest($request);

        if ($form->isSubmitted() && $form->isValid()) {
            try {
                // Before update hook
                $this->beforeUpdate($notificationTypeTemplate);

                $this->entityManager->flush();

                // After update hook
                $this->afterUpdate($notificationTypeTemplate);

                $this->addFlash('success', $this->translator->trans(
                    'notificationtypetemplate.flash.updated_successfully',
                    ['%name%' => (string) $notificationTypeTemplate],
                    'notificationtypetemplate'
                ));

                return $this->redirectToRoute('notificationtypetemplate_index', [], Response::HTTP_SEE_OTHER);

            } catch (\Exception $e) {
                $this->addFlash('error', $this->translator->trans(
                    'notificationtypetemplate.flash.update_failed',
                    ['%error%' => $e->getMessage()],
                    'notificationtypetemplate'
                ));
            }
        }

        // Re-render with errors
        return $this->render('notificationtypetemplate/_form_modal.html.twig', [
            'form' => $form,
            'notificationTypeTemplate' => $notificationTypeTemplate,
            'is_edit' => true,
        ], new Response('', $form->isSubmitted() ? Response::HTTP_UNPROCESSABLE_ENTITY : Response::HTTP_OK));
    }

    // ====================================
    // DELETE ACTION - Delete notificationTypeTemplate
    // ====================================

    /**
     * Delete notificationTypeTemplate
     */
    protected function deleteAction(NotificationTypeTemplate $notificationTypeTemplate, Request $request): Response
    {
        $this->denyAccessUnlessGranted(NotificationTypeTemplateVoter::DELETE, $notificationTypeTemplate);

        // Verify CSRF token
        $token = $request->request->get('_token');
        if (!$this->csrfTokenManager->isTokenValid(new CsrfToken('delete_notificationTypeTemplate_' . $notificationTypeTemplate->getId()->toString(), $token))) {
            throw new InvalidCsrfTokenException('Invalid CSRF token for delete operation.');
        }

        try {
            // Before delete hook
            $this->beforeDelete($notificationTypeTemplate);

            $name = (string) $notificationTypeTemplate;

            $this->entityManager->remove($notificationTypeTemplate);
            $this->entityManager->flush();

            // After delete hook
            $this->afterDelete();

            $this->addFlash('success', $this->translator->trans(
                'notificationtypetemplate.flash.deleted_successfully',
                ['%name%' => $name],
                'notificationtypetemplate'
            ));

        } catch (\Exception $e) {
            $this->addFlash('error', $this->translator->trans(
                'notificationtypetemplate.flash.delete_failed',
                ['%error%' => $e->getMessage()],
                'notificationtypetemplate'
            ));
        }

        return $this->redirectToRoute('notificationtypetemplate_index', [], Response::HTTP_SEE_OTHER);
    }

    // ====================================
    // SHOW ACTION - Display notificationTypeTemplate details
    // ====================================

    /**
     * Display notificationTypeTemplate details
     */
    protected function showAction(NotificationTypeTemplate $notificationTypeTemplate): Response
    {
        $this->denyAccessUnlessGranted(NotificationTypeTemplateVoter::VIEW, $notificationTypeTemplate);

        return $this->render('notificationtypetemplate/show.html.twig', [
            'notificationTypeTemplate' => $notificationTypeTemplate,
        ]);
    }

    // ====================================
    // LIFECYCLE HOOKS
    // ====================================
    // Override these in NotificationTypeTemplateController to add custom logic

    /**
     * Initialize new entity before creating form
     *
     * Note: Organization and Owner are set automatically by TenantEntityProcessor
     * Only use this for custom initialization logic
     */
    protected function initializeNewEntity(NotificationTypeTemplate $notificationTypeTemplate): void
    {
        // Organization and Owner are set automatically by TenantEntityProcessor
        // Add your custom initialization here
    }

    /**
     * Execute logic before creating entity
     */
    protected function beforeCreate(NotificationTypeTemplate $notificationTypeTemplate): void
    {
        // Override to add logic before create
    }

    /**
     * Execute logic after creating entity
     */
    protected function afterCreate(NotificationTypeTemplate $notificationTypeTemplate): void
    {
        // Override to add logic after create (e.g., send notification, trigger event)
    }

    /**
     * Execute logic before updating entity
     */
    protected function beforeUpdate(NotificationTypeTemplate $notificationTypeTemplate): void
    {
        // Override to add logic before update
    }

    /**
     * Execute logic after updating entity
     */
    protected function afterUpdate(NotificationTypeTemplate $notificationTypeTemplate): void
    {
        // Override to add logic after update (e.g., send notification, trigger event)
    }

    /**
     * Execute logic before deleting entity
     */
    protected function beforeDelete(NotificationTypeTemplate $notificationTypeTemplate): void
    {
        // Override to add logic before delete (e.g., check dependencies, archive)
    }

    /**
     * Execute logic after deleting entity
     */
    protected function afterDelete(): void
    {
        // Override to add logic after delete (e.g., cleanup, send notification)
    }
}
