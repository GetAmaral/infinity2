<?php

declare(strict_types=1);

namespace App\Controller\Generated;

use App\Controller\Base\BaseApiController;
use App\Entity\TimeZone;
use App\Repository\TimeZoneRepository;
use App\Security\Voter\TimeZoneVoter;
use App\Form\TimeZoneFormType;
use App\Service\ListPreferencesService;
use Doctrine\ORM\EntityManagerInterface;
use Symfony\Component\HttpFoundation\Request;
use Symfony\Component\HttpFoundation\Response;
use Symfony\Component\HttpFoundation\JsonResponse;
use Symfony\Component\Security\Csrf\CsrfToken;
use Symfony\Component\Security\Csrf\CsrfTokenManagerInterface;
use Symfony\Component\Security\Core\Exception\InvalidCsrfTokenException;
use Symfony\Contracts\Translation\TranslatorInterface;

/**
 * Generated Base Controller for TimeZone
 *
 * ⚠️ WARNING: This file is ALWAYS regenerated by Genmax
 * DO NOT EDIT THIS FILE - Edit TimeZoneController instead
 *
 * @generated
 * @codeCoverageIgnore
 */
abstract class TimeZoneControllerGenerated extends BaseApiController
{
    public function __construct(
        protected readonly EntityManagerInterface $entityManager,
        protected readonly TimeZoneRepository $repository,
        protected readonly ListPreferencesService $listPreferencesService,
        protected readonly TranslatorInterface $translator,
        protected readonly CsrfTokenManagerInterface $csrfTokenManager,
    ) {}

    // ====================================
    // ABSTRACT METHOD IMPLEMENTATIONS
    // ====================================

    /**
     * Get the repository for this entity
     */
    protected function getRepository(): TimeZoneRepository
    {
        return $this->repository;
    }

    /**
     * Get the plural name for this entity
     */
    protected function getEntityPluralName(): string
    {
        return 'timeZones';
    }

    /**
     * Convert entity to array for API responses
     */
    protected function entityToArray(object $entity): array
    {
        if (!$entity instanceof TimeZone) {
            throw new \InvalidArgumentException('Expected TimeZone instance');
        }

        return [
            'id' => $entity->getId()->toString(),
            'name' => $entity->getName(),
            'offsetMinutes' => $entity->getOffsetMinutes(),
            'workingHours' => ($workingHoursRel = $entity->getWorkingHours()) ? [
                'id' => $workingHoursRel->getId()->toString(),
                'display' => (string) $workingHoursRel,
            ] : null,
        ];
    }

    // ====================================
    // INDEX ACTION - List all TimeZones
    // ====================================

    /**
     * List TimeZones
     */
    protected function indexAction(): Response
    {
        $this->denyAccessUnlessGranted(TimeZoneVoter::LIST);

        $preferences = $this->listPreferencesService->getEntityPreferences('timeZones');
        $savedView = $preferences['view'] ?? 'grid';

        return $this->render('timezone/index.html.twig', [
            'entities' => [],  // Loaded via API
            'entity_name' => 'timeZone',
            'entity_name_plural' => 'timeZones',
            'page_icon' => 'bi-clock-history',
            'default_view' => $savedView,

            // List configuration from GeneratorProperty
            'enable_search' => true,
            'enable_filters' => false,
            'enable_sorting' => true,
            'enable_create_button' => true,

            // Property metadata for client-side rendering (as JSON strings)
            'list_fields' => '[{"name":"name","label":"Name","type":"string","sortable":true,"searchable":true,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getName"},{"name":"offsetMinutes","label":"OffsetMinutes","type":"integer","sortable":true,"searchable":false,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getOffsetMinutes"},{"name":"workingHours","label":"WorkingHours","type":"","sortable":true,"searchable":false,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getWorkingHours"}]',
            'searchable_fields' => '[{"name":"name","label":"Name","type":"string"}]',
            'filterable_fields' => '[]',
            'sortable_fields' => '[{"name":"name","label":"Name"},{"name":"offsetMinutes","label":"OffsetMinutes"},{"name":"workingHours","label":"WorkingHours"}]',
        ]);
    }

    // ====================================
    // API SEARCH - Fetch TimeZones for list view
    // ====================================

    /**
     * API endpoint for searching TimeZones
     */
    protected function apiSearchAction(Request $request): JsonResponse
    {
        $this->denyAccessUnlessGranted(TimeZoneVoter::LIST);

        // This method uses the BaseApiController's handleSearchRequest
        // which integrates with API Platform's GetCollection operation
        return $this->handleSearchRequest($request);
    }

    // ====================================
    // NEW FORM ACTION - Display create form
    // ====================================

    /**
     * Display new timeZone form
     */
    protected function newFormAction(Request $request): Response
    {
        $this->denyAccessUnlessGranted(TimeZoneVoter::CREATE);

        $timeZone = new TimeZone();

        // Initialize with custom logic if needed
        $this->initializeNewEntity($timeZone);

        $form = $this->createForm(TimeZoneFormType::class, $timeZone);

        return $this->render('timezone/_form_modal.html.twig', [
            'form' => $form,
            'timeZone' => $timeZone,
            'is_edit' => false,
        ]);
    }

    // ====================================
    // CREATE ACTION - Process create form submission
    // ====================================

    /**
     * Process new timeZone form submission
     */
    protected function createAction(Request $request): Response
    {
        $this->denyAccessUnlessGranted(TimeZoneVoter::CREATE);

        $timeZone = new TimeZone();

        // Initialize with custom logic if needed
        $this->initializeNewEntity($timeZone);

        $form = $this->createForm(TimeZoneFormType::class, $timeZone);
        $form->handleRequest($request);

        if ($form->isSubmitted() && $form->isValid()) {
            try {
                // Before create hook
                $this->beforeCreate($timeZone);

                $this->entityManager->persist($timeZone);
                $this->entityManager->flush();

                // After create hook
                $this->afterCreate($timeZone);

                $this->addFlash('success', $this->translator->trans(
                    'timezone.flash.created_successfully',
                    ['%name%' => (string) $timeZone],
                    'timezone'
                ));

                return $this->redirectToRoute('timezone_index', [], Response::HTTP_SEE_OTHER);

            } catch (\Exception $e) {
                $this->addFlash('error', $this->translator->trans(
                    'timezone.flash.create_failed',
                    ['%error%' => $e->getMessage()],
                    'timezone'
                ));
            }
        }

        // Re-render with errors
        return $this->render('timezone/_form_modal.html.twig', [
            'form' => $form,
            'timeZone' => $timeZone,
            'is_edit' => false,
        ], new Response('', $form->isSubmitted() ? Response::HTTP_UNPROCESSABLE_ENTITY : Response::HTTP_OK));
    }

    // ====================================
    // EDIT FORM ACTION - Display edit form
    // ====================================

    /**
     * Display edit timeZone form
     */
    protected function editFormAction(TimeZone $timeZone, Request $request): Response
    {
        $this->denyAccessUnlessGranted(TimeZoneVoter::EDIT, $timeZone);

        $form = $this->createForm(TimeZoneFormType::class, $timeZone);

        return $this->render('timezone/_form_modal.html.twig', [
            'form' => $form,
            'timeZone' => $timeZone,
            'is_edit' => true,
        ]);
    }

    // ====================================
    // UPDATE ACTION - Process edit form submission
    // ====================================

    /**
     * Process edit timeZone form submission
     */
    protected function updateAction(TimeZone $timeZone, Request $request): Response
    {
        $this->denyAccessUnlessGranted(TimeZoneVoter::EDIT, $timeZone);

        $form = $this->createForm(TimeZoneFormType::class, $timeZone);
        $form->handleRequest($request);

        if ($form->isSubmitted() && $form->isValid()) {
            try {
                // Before update hook
                $this->beforeUpdate($timeZone);

                $this->entityManager->flush();

                // After update hook
                $this->afterUpdate($timeZone);

                $this->addFlash('success', $this->translator->trans(
                    'timezone.flash.updated_successfully',
                    ['%name%' => (string) $timeZone],
                    'timezone'
                ));

                return $this->redirectToRoute('timezone_index', [], Response::HTTP_SEE_OTHER);

            } catch (\Exception $e) {
                $this->addFlash('error', $this->translator->trans(
                    'timezone.flash.update_failed',
                    ['%error%' => $e->getMessage()],
                    'timezone'
                ));
            }
        }

        // Re-render with errors
        return $this->render('timezone/_form_modal.html.twig', [
            'form' => $form,
            'timeZone' => $timeZone,
            'is_edit' => true,
        ], new Response('', $form->isSubmitted() ? Response::HTTP_UNPROCESSABLE_ENTITY : Response::HTTP_OK));
    }

    // ====================================
    // DELETE ACTION - Delete timeZone
    // ====================================

    /**
     * Delete timeZone
     */
    protected function deleteAction(TimeZone $timeZone, Request $request): Response
    {
        $this->denyAccessUnlessGranted(TimeZoneVoter::DELETE, $timeZone);

        // Verify CSRF token
        $token = $request->request->get('_token');
        if (!$this->csrfTokenManager->isTokenValid(new CsrfToken('delete_timeZone_' . $timeZone->getId()->toString(), $token))) {
            throw new InvalidCsrfTokenException('Invalid CSRF token for delete operation.');
        }

        try {
            // Before delete hook
            $this->beforeDelete($timeZone);

            $name = (string) $timeZone;

            $this->entityManager->remove($timeZone);
            $this->entityManager->flush();

            // After delete hook
            $this->afterDelete();

            $this->addFlash('success', $this->translator->trans(
                'timezone.flash.deleted_successfully',
                ['%name%' => $name],
                'timezone'
            ));

        } catch (\Exception $e) {
            $this->addFlash('error', $this->translator->trans(
                'timezone.flash.delete_failed',
                ['%error%' => $e->getMessage()],
                'timezone'
            ));
        }

        return $this->redirectToRoute('timezone_index', [], Response::HTTP_SEE_OTHER);
    }

    // ====================================
    // SHOW ACTION - Display timeZone details
    // ====================================

    /**
     * Display timeZone details
     */
    protected function showAction(TimeZone $timeZone): Response
    {
        $this->denyAccessUnlessGranted(TimeZoneVoter::VIEW, $timeZone);

        return $this->render('timezone/show.html.twig', [
            'timeZone' => $timeZone,
        ]);
    }

    // ====================================
    // LIFECYCLE HOOKS
    // ====================================
    // Override these in TimeZoneController to add custom logic

    /**
     * Initialize new entity before creating form
     *
     * Note: Organization and Owner are set automatically by TenantEntityProcessor
     * Only use this for custom initialization logic
     */
    protected function initializeNewEntity(TimeZone $timeZone): void
    {
        // Organization and Owner are set automatically by TenantEntityProcessor
        // Add your custom initialization here
    }

    /**
     * Execute logic before creating entity
     */
    protected function beforeCreate(TimeZone $timeZone): void
    {
        // Override to add logic before create
    }

    /**
     * Execute logic after creating entity
     */
    protected function afterCreate(TimeZone $timeZone): void
    {
        // Override to add logic after create (e.g., send notification, trigger event)
    }

    /**
     * Execute logic before updating entity
     */
    protected function beforeUpdate(TimeZone $timeZone): void
    {
        // Override to add logic before update
    }

    /**
     * Execute logic after updating entity
     */
    protected function afterUpdate(TimeZone $timeZone): void
    {
        // Override to add logic after update (e.g., send notification, trigger event)
    }

    /**
     * Execute logic before deleting entity
     */
    protected function beforeDelete(TimeZone $timeZone): void
    {
        // Override to add logic before delete (e.g., check dependencies, archive)
    }

    /**
     * Execute logic after deleting entity
     */
    protected function afterDelete(): void
    {
        // Override to add logic after delete (e.g., cleanup, send notification)
    }
}
