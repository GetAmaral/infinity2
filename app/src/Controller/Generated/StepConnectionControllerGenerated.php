<?php

declare(strict_types=1);

namespace App\Controller\Generated;

use App\Controller\Base\BaseApiController;
use App\Entity\StepConnection;
use App\Repository\StepConnectionRepository;
use App\Security\Voter\StepConnectionVoter;
use App\Form\StepConnectionFormType;
use App\Service\ListPreferencesService;
use Doctrine\ORM\EntityManagerInterface;
use Symfony\Component\HttpFoundation\Request;
use Symfony\Component\HttpFoundation\Response;
use Symfony\Component\HttpFoundation\JsonResponse;
use Symfony\Component\Security\Csrf\CsrfToken;
use Symfony\Component\Security\Csrf\CsrfTokenManagerInterface;
use Symfony\Component\Security\Core\Exception\InvalidCsrfTokenException;
use Symfony\Contracts\Translation\TranslatorInterface;

/**
 * Generated Base Controller for StepConnection
 *
 * ⚠️ WARNING: This file is ALWAYS regenerated by Genmax
 * DO NOT EDIT THIS FILE - Edit StepConnectionController instead
 *
 * @generated
 * @codeCoverageIgnore
 */
abstract class StepConnectionControllerGenerated extends BaseApiController
{
    public function __construct(
        protected readonly EntityManagerInterface $entityManager,
        protected readonly StepConnectionRepository $repository,
        protected readonly ListPreferencesService $listPreferencesService,
        protected readonly TranslatorInterface $translator,
        protected readonly CsrfTokenManagerInterface $csrfTokenManager,
    ) {}

    // ====================================
    // ABSTRACT METHOD IMPLEMENTATIONS
    // ====================================

    /**
     * Get the repository for this entity
     */
    protected function getRepository(): StepConnectionRepository
    {
        return $this->repository;
    }

    /**
     * Get the plural name for this entity
     */
    protected function getEntityPluralName(): string
    {
        return 'stepConnectia';
    }

    /**
     * Convert entity to array for API responses
     */
    protected function entityToArray(object $entity): array
    {
        if (!$entity instanceof StepConnection) {
            throw new \InvalidArgumentException('Expected StepConnection instance');
        }

        return [
            'id' => $entity->getId()->toString(),
            'sourceOutput' => ($sourceOutputRel = $entity->getSourceOutput()) ? [
                'id' => $sourceOutputRel->getId()->toString(),
                'display' => (string) $sourceOutputRel,
            ] : null,
            'targetInput' => ($targetInputRel = $entity->getTargetInput()) ? [
                'id' => $targetInputRel->getId()->toString(),
                'display' => (string) $targetInputRel,
            ] : null,
        ];
    }

    // ====================================
    // INDEX ACTION - List all StepConnectia
    // ====================================

    /**
     * List StepConnectia
     */
    protected function indexAction(): Response
    {
        $this->denyAccessUnlessGranted(StepConnectionVoter::LIST);

        $preferences = $this->listPreferencesService->getEntityPreferences('stepConnectia');
        $savedView = $preferences['view'] ?? 'grid';

        return $this->render('stepconnection/index.html.twig', [
            'entities' => [],  // Loaded via API
            'entity_name' => 'stepConnection',
            'entity_name_plural' => 'stepConnectia',
            'page_icon' => 'bi-arrow-left-right',
            'default_view' => $savedView,

            // List configuration from GeneratorProperty
            'enable_search' => true,
            'enable_filters' => true,
            'enable_sorting' => true,
            'enable_create_button' => true,

            // Property metadata for client-side rendering (as JSON strings)
            'list_fields' => '[{"name":"sourceOutput","label":"SourceOutput","type":"string","sortable":true,"searchable":true,"filterable":true,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getSourceOutput"},{"name":"targetInput","label":"TargetInput","type":"string","sortable":true,"searchable":true,"filterable":true,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getTargetInput"}]',
            'searchable_fields' => '[{"name":"sourceOutput","label":"SourceOutput","type":"string"},{"name":"targetInput","label":"TargetInput","type":"string"}]',
            'filterable_fields' => '[{"name":"sourceOutput","label":"SourceOutput","type":"string","strategy":null,"boolean":false,"date":false,"numericRange":false,"exists":false},{"name":"targetInput","label":"TargetInput","type":"string","strategy":null,"boolean":false,"date":false,"numericRange":false,"exists":false}]',
            'sortable_fields' => '[{"name":"sourceOutput","label":"SourceOutput"},{"name":"targetInput","label":"TargetInput"}]',
        ]);
    }


    // ====================================
    // NEW FORM ACTION - Display create form
    // ====================================

    /**
     * Display new stepConnection form
     */
    protected function newFormAction(Request $request): Response
    {
        $this->denyAccessUnlessGranted(StepConnectionVoter::CREATE);

        $stepConnection = new StepConnection();

        // Initialize with custom logic if needed
        $this->initializeNewEntity($stepConnection);

        $form = $this->createForm(StepConnectionFormType::class, $stepConnection);

        return $this->render('stepconnection/_form_modal.html.twig', [
            'form' => $form,
            'stepConnection' => $stepConnection,
            'is_edit' => false,
        ]);
    }

    // ====================================
    // CREATE ACTION - Process create form submission
    // ====================================

    /**
     * Process new stepConnection form submission
     */
    protected function createAction(Request $request): Response
    {
        $this->denyAccessUnlessGranted(StepConnectionVoter::CREATE);

        $stepConnection = new StepConnection();

        // Initialize with custom logic if needed
        $this->initializeNewEntity($stepConnection);

        $form = $this->createForm(StepConnectionFormType::class, $stepConnection);
        $form->handleRequest($request);

        if ($form->isSubmitted() && $form->isValid()) {
            try {
                // Before create hook
                $this->beforeCreate($stepConnection);

                $this->entityManager->persist($stepConnection);
                $this->entityManager->flush();

                // After create hook
                $this->afterCreate($stepConnection);

                $this->addFlash('success', $this->translator->trans(
                    'stepconnection.flash.created_successfully',
                    ['%name%' => (string) $stepConnection],
                    'stepconnection'
                ));

                return $this->redirectToRoute('stepconnection_index', [], Response::HTTP_SEE_OTHER);

            } catch (\Exception $e) {
                $this->addFlash('error', $this->translator->trans(
                    'stepconnection.flash.create_failed',
                    ['%error%' => $e->getMessage()],
                    'stepconnection'
                ));
            }
        }

        // Re-render with errors
        return $this->render('stepconnection/_form_modal.html.twig', [
            'form' => $form,
            'stepConnection' => $stepConnection,
            'is_edit' => false,
        ], new Response('', $form->isSubmitted() ? Response::HTTP_UNPROCESSABLE_ENTITY : Response::HTTP_OK));
    }

    // ====================================
    // EDIT FORM ACTION - Display edit form
    // ====================================

    /**
     * Display edit stepConnection form
     */
    protected function editFormAction(StepConnection $stepConnection, Request $request): Response
    {
        $this->denyAccessUnlessGranted(StepConnectionVoter::EDIT, $stepConnection);

        $form = $this->createForm(StepConnectionFormType::class, $stepConnection);

        return $this->render('stepconnection/_form_modal.html.twig', [
            'form' => $form,
            'stepConnection' => $stepConnection,
            'is_edit' => true,
        ]);
    }

    // ====================================
    // UPDATE ACTION - Process edit form submission
    // ====================================

    /**
     * Process edit stepConnection form submission
     */
    protected function updateAction(StepConnection $stepConnection, Request $request): Response
    {
        $this->denyAccessUnlessGranted(StepConnectionVoter::EDIT, $stepConnection);

        $form = $this->createForm(StepConnectionFormType::class, $stepConnection);
        $form->handleRequest($request);

        if ($form->isSubmitted() && $form->isValid()) {
            try {
                // Before update hook
                $this->beforeUpdate($stepConnection);

                $this->entityManager->flush();

                // After update hook
                $this->afterUpdate($stepConnection);

                $this->addFlash('success', $this->translator->trans(
                    'stepconnection.flash.updated_successfully',
                    ['%name%' => (string) $stepConnection],
                    'stepconnection'
                ));

                return $this->redirectToRoute('stepconnection_index', [], Response::HTTP_SEE_OTHER);

            } catch (\Exception $e) {
                $this->addFlash('error', $this->translator->trans(
                    'stepconnection.flash.update_failed',
                    ['%error%' => $e->getMessage()],
                    'stepconnection'
                ));
            }
        }

        // Re-render with errors
        return $this->render('stepconnection/_form_modal.html.twig', [
            'form' => $form,
            'stepConnection' => $stepConnection,
            'is_edit' => true,
        ], new Response('', $form->isSubmitted() ? Response::HTTP_UNPROCESSABLE_ENTITY : Response::HTTP_OK));
    }

    // ====================================
    // DELETE ACTION - Delete stepConnection
    // ====================================

    /**
     * Delete stepConnection
     */
    protected function deleteAction(StepConnection $stepConnection, Request $request): Response
    {
        $this->denyAccessUnlessGranted(StepConnectionVoter::DELETE, $stepConnection);

        // Verify CSRF token
        $token = $request->request->get('_token');
        if (!$this->csrfTokenManager->isTokenValid(new CsrfToken('delete_stepConnection_' . $stepConnection->getId()->toString(), $token))) {
            throw new InvalidCsrfTokenException('Invalid CSRF token for delete operation.');
        }

        try {
            // Before delete hook
            $this->beforeDelete($stepConnection);

            $name = (string) $stepConnection;

            $this->entityManager->remove($stepConnection);
            $this->entityManager->flush();

            // After delete hook
            $this->afterDelete();

            $this->addFlash('success', $this->translator->trans(
                'stepconnection.flash.deleted_successfully',
                ['%name%' => $name],
                'stepconnection'
            ));

        } catch (\Exception $e) {
            $this->addFlash('error', $this->translator->trans(
                'stepconnection.flash.delete_failed',
                ['%error%' => $e->getMessage()],
                'stepconnection'
            ));
        }

        return $this->redirectToRoute('stepconnection_index', [], Response::HTTP_SEE_OTHER);
    }

    // ====================================
    // SHOW ACTION - Display stepConnection details
    // ====================================

    /**
     * Display stepConnection details
     */
    protected function showAction(StepConnection $stepConnection): Response
    {
        $this->denyAccessUnlessGranted(StepConnectionVoter::VIEW, $stepConnection);

        return $this->render('stepconnection/show.html.twig', [
            'stepConnection' => $stepConnection,
        ]);
    }

    // ====================================
    // LIFECYCLE HOOKS
    // ====================================
    // Override these in StepConnectionController to add custom logic

    /**
     * Initialize new entity before creating form
     *
     * Note: Organization and Owner are set automatically by TenantEntityProcessor
     * Only use this for custom initialization logic
     */
    protected function initializeNewEntity(StepConnection $stepConnection): void
    {
        // Organization and Owner are set automatically by TenantEntityProcessor
        // Add your custom initialization here
    }

    /**
     * Execute logic before creating entity
     */
    protected function beforeCreate(StepConnection $stepConnection): void
    {
        // Override to add logic before create
    }

    /**
     * Execute logic after creating entity
     */
    protected function afterCreate(StepConnection $stepConnection): void
    {
        // Override to add logic after create (e.g., send notification, trigger event)
    }

    /**
     * Execute logic before updating entity
     */
    protected function beforeUpdate(StepConnection $stepConnection): void
    {
        // Override to add logic before update
    }

    /**
     * Execute logic after updating entity
     */
    protected function afterUpdate(StepConnection $stepConnection): void
    {
        // Override to add logic after update (e.g., send notification, trigger event)
    }

    /**
     * Execute logic before deleting entity
     */
    protected function beforeDelete(StepConnection $stepConnection): void
    {
        // Override to add logic before delete (e.g., check dependencies, archive)
    }

    /**
     * Execute logic after deleting entity
     */
    protected function afterDelete(): void
    {
        // Override to add logic after delete (e.g., cleanup, send notification)
    }
}
