<?php

declare(strict_types=1);

namespace App\Controller\Generated;

use App\Controller\Base\BaseApiController;
use App\Entity\Reminder;
use App\Repository\ReminderRepository;
use App\Security\Voter\ReminderVoter;
use App\Form\ReminderFormType;
use App\Service\ListPreferencesService;
use Doctrine\ORM\EntityManagerInterface;
use Symfony\Component\HttpFoundation\Request;
use Symfony\Component\HttpFoundation\Response;
use Symfony\Component\HttpFoundation\JsonResponse;
use Symfony\Component\Security\Csrf\CsrfToken;
use Symfony\Component\Security\Csrf\CsrfTokenManagerInterface;
use Symfony\Component\Security\Core\Exception\InvalidCsrfTokenException;
use Symfony\Contracts\Translation\TranslatorInterface;

/**
 * Generated Base Controller for Reminder
 *
 * ⚠️ WARNING: This file is ALWAYS regenerated by Genmax
 * DO NOT EDIT THIS FILE - Edit ReminderController instead
 *
 * @generated
 * @codeCoverageIgnore
 */
abstract class ReminderControllerGenerated extends BaseApiController
{
    public function __construct(
        protected readonly EntityManagerInterface $entityManager,
        protected readonly ReminderRepository $repository,
        protected readonly ListPreferencesService $listPreferencesService,
        protected readonly TranslatorInterface $translator,
        protected readonly CsrfTokenManagerInterface $csrfTokenManager,
    ) {}

    // ====================================
    // ABSTRACT METHOD IMPLEMENTATIONS
    // ====================================

    /**
     * Get the repository for this entity
     */
    protected function getRepository(): ReminderRepository
    {
        return $this->repository;
    }

    /**
     * Get the plural name for this entity
     */
    protected function getEntityPluralName(): string
    {
        return 'reminders';
    }

    /**
     * Convert entity to array for API responses
     */
    protected function entityToArray(object $entity): array
    {
        if (!$entity instanceof Reminder) {
            throw new \InvalidArgumentException('Expected Reminder instance');
        }

        return [
            'id' => $entity->getId()->toString(),
            'organization' => ($organizationRel = $entity->getOrganization()) ? [
                'id' => $organizationRel->getId()->toString(),
                'display' => (string) $organizationRel,
            ] : null,
            'name' => $entity->getName(),
            'active' => $entity->getActive(),
            'communicationMethod' => ($communicationMethodRel = $entity->getCommunicationMethod()) ? [
                'id' => $communicationMethodRel->getId()->toString(),
                'display' => (string) $communicationMethodRel,
            ] : null,
            'event' => ($eventRel = $entity->getEvent()) ? [
                'id' => $eventRel->getId()->toString(),
                'display' => (string) $eventRel,
            ] : null,
            'minutesBeforeStart' => $entity->getMinutesBeforeStart(),
            'notifications' => ($notificationsRel = $entity->getNotifications()) ? [
                'id' => $notificationsRel->getId()->toString(),
                'display' => (string) $notificationsRel,
            ] : null,
        ];
    }

    // ====================================
    // INDEX ACTION - List all Reminders
    // ====================================

    /**
     * List Reminders
     */
    protected function indexAction(): Response
    {
        $this->denyAccessUnlessGranted(ReminderVoter::LIST);

        $preferences = $this->listPreferencesService->getEntityPreferences('reminders');
        $savedView = $preferences['view'] ?? 'grid';

        return $this->render('reminder/index.html.twig', [
            'entities' => [],  // Loaded via API
            'entity_name' => 'reminder',
            'entity_name_plural' => 'reminders',
            'page_icon' => 'bi-alarm',
            'default_view' => $savedView,

            // List configuration from GeneratorProperty
            'enable_search' => true,
            'enable_filters' => true,
            'enable_sorting' => true,
            'enable_create_button' => true,

            // Property metadata for client-side rendering (as JSON strings)
            'list_fields' => '[{"name":"name","label":"Name","type":"string","sortable":true,"searchable":true,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getName"},{"name":"active","label":"Active","type":"boolean","sortable":true,"searchable":false,"filterable":true,"filterStrategy":null,"filterBoolean":true,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getActive"},{"name":"communicationMethod","label":"CommunicationMethod","type":"","sortable":true,"searchable":false,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getCommunicationMethod"},{"name":"event","label":"Event","type":"","sortable":true,"searchable":false,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getEvent"},{"name":"minutesBeforeStart","label":"Minutes Before Trigger","type":"integer","sortable":true,"searchable":false,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getMinutesBeforeStart"},{"name":"notifications","label":"Notifications","type":"","sortable":true,"searchable":false,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getNotifications"}]',
            'searchable_fields' => '[{"name":"name","label":"Name","type":"string"}]',
            'filterable_fields' => '[{"name":"active","label":"Active","type":"boolean","strategy":null,"boolean":true,"date":false,"numericRange":false,"exists":false}]',
            'sortable_fields' => '[{"name":"name","label":"Name"},{"name":"active","label":"Active"},{"name":"communicationMethod","label":"CommunicationMethod"},{"name":"event","label":"Event"},{"name":"minutesBeforeStart","label":"Minutes Before Trigger"},{"name":"notifications","label":"Notifications"}]',
        ]);
    }

    // ====================================
    // API SEARCH - Fetch Reminders for list view
    // ====================================

    /**
     * API endpoint for searching Reminders
     */
    protected function apiSearchAction(Request $request): JsonResponse
    {
        $this->denyAccessUnlessGranted(ReminderVoter::LIST);

        // This method uses the BaseApiController's handleSearchRequest
        // which integrates with API Platform's GetCollection operation
        return $this->handleSearchRequest($request);
    }

    // ====================================
    // NEW FORM ACTION - Display create form
    // ====================================

    /**
     * Display new reminder form
     */
    protected function newFormAction(Request $request): Response
    {
        $this->denyAccessUnlessGranted(ReminderVoter::CREATE);

        $reminder = new Reminder();

        // Initialize with custom logic if needed
        $this->initializeNewEntity($reminder);

        $form = $this->createForm(ReminderFormType::class, $reminder);

        return $this->render('reminder/_form_modal.html.twig', [
            'form' => $form,
            'reminder' => $reminder,
            'is_edit' => false,
        ]);
    }

    // ====================================
    // CREATE ACTION - Process create form submission
    // ====================================

    /**
     * Process new reminder form submission
     */
    protected function createAction(Request $request): Response
    {
        $this->denyAccessUnlessGranted(ReminderVoter::CREATE);

        $reminder = new Reminder();

        // Initialize with custom logic if needed
        $this->initializeNewEntity($reminder);

        $form = $this->createForm(ReminderFormType::class, $reminder);
        $form->handleRequest($request);

        if ($form->isSubmitted() && $form->isValid()) {
            try {
                // Before create hook
                $this->beforeCreate($reminder);

                $this->entityManager->persist($reminder);
                $this->entityManager->flush();

                // After create hook
                $this->afterCreate($reminder);

                $this->addFlash('success', $this->translator->trans(
                    'reminder.flash.created_successfully',
                    ['%name%' => (string) $reminder],
                    'reminder'
                ));

                return $this->redirectToRoute('reminder_index', [], Response::HTTP_SEE_OTHER);

            } catch (\Exception $e) {
                $this->addFlash('error', $this->translator->trans(
                    'reminder.flash.create_failed',
                    ['%error%' => $e->getMessage()],
                    'reminder'
                ));
            }
        }

        // Re-render with errors
        return $this->render('reminder/_form_modal.html.twig', [
            'form' => $form,
            'reminder' => $reminder,
            'is_edit' => false,
        ], new Response('', $form->isSubmitted() ? Response::HTTP_UNPROCESSABLE_ENTITY : Response::HTTP_OK));
    }

    // ====================================
    // EDIT FORM ACTION - Display edit form
    // ====================================

    /**
     * Display edit reminder form
     */
    protected function editFormAction(Reminder $reminder, Request $request): Response
    {
        $this->denyAccessUnlessGranted(ReminderVoter::EDIT, $reminder);

        $form = $this->createForm(ReminderFormType::class, $reminder);

        return $this->render('reminder/_form_modal.html.twig', [
            'form' => $form,
            'reminder' => $reminder,
            'is_edit' => true,
        ]);
    }

    // ====================================
    // UPDATE ACTION - Process edit form submission
    // ====================================

    /**
     * Process edit reminder form submission
     */
    protected function updateAction(Reminder $reminder, Request $request): Response
    {
        $this->denyAccessUnlessGranted(ReminderVoter::EDIT, $reminder);

        $form = $this->createForm(ReminderFormType::class, $reminder);
        $form->handleRequest($request);

        if ($form->isSubmitted() && $form->isValid()) {
            try {
                // Before update hook
                $this->beforeUpdate($reminder);

                $this->entityManager->flush();

                // After update hook
                $this->afterUpdate($reminder);

                $this->addFlash('success', $this->translator->trans(
                    'reminder.flash.updated_successfully',
                    ['%name%' => (string) $reminder],
                    'reminder'
                ));

                return $this->redirectToRoute('reminder_index', [], Response::HTTP_SEE_OTHER);

            } catch (\Exception $e) {
                $this->addFlash('error', $this->translator->trans(
                    'reminder.flash.update_failed',
                    ['%error%' => $e->getMessage()],
                    'reminder'
                ));
            }
        }

        // Re-render with errors
        return $this->render('reminder/_form_modal.html.twig', [
            'form' => $form,
            'reminder' => $reminder,
            'is_edit' => true,
        ], new Response('', $form->isSubmitted() ? Response::HTTP_UNPROCESSABLE_ENTITY : Response::HTTP_OK));
    }

    // ====================================
    // DELETE ACTION - Delete reminder
    // ====================================

    /**
     * Delete reminder
     */
    protected function deleteAction(Reminder $reminder, Request $request): Response
    {
        $this->denyAccessUnlessGranted(ReminderVoter::DELETE, $reminder);

        // Verify CSRF token
        $token = $request->request->get('_token');
        if (!$this->csrfTokenManager->isTokenValid(new CsrfToken('delete_reminder_' . $reminder->getId()->toString(), $token))) {
            throw new InvalidCsrfTokenException('Invalid CSRF token for delete operation.');
        }

        try {
            // Before delete hook
            $this->beforeDelete($reminder);

            $name = (string) $reminder;

            $this->entityManager->remove($reminder);
            $this->entityManager->flush();

            // After delete hook
            $this->afterDelete();

            $this->addFlash('success', $this->translator->trans(
                'reminder.flash.deleted_successfully',
                ['%name%' => $name],
                'reminder'
            ));

        } catch (\Exception $e) {
            $this->addFlash('error', $this->translator->trans(
                'reminder.flash.delete_failed',
                ['%error%' => $e->getMessage()],
                'reminder'
            ));
        }

        return $this->redirectToRoute('reminder_index', [], Response::HTTP_SEE_OTHER);
    }

    // ====================================
    // SHOW ACTION - Display reminder details
    // ====================================

    /**
     * Display reminder details
     */
    protected function showAction(Reminder $reminder): Response
    {
        $this->denyAccessUnlessGranted(ReminderVoter::VIEW, $reminder);

        return $this->render('reminder/show.html.twig', [
            'reminder' => $reminder,
        ]);
    }

    // ====================================
    // LIFECYCLE HOOKS
    // ====================================
    // Override these in ReminderController to add custom logic

    /**
     * Initialize new entity before creating form
     *
     * Note: Organization and Owner are set automatically by TenantEntityProcessor
     * Only use this for custom initialization logic
     */
    protected function initializeNewEntity(Reminder $reminder): void
    {
        // Organization and Owner are set automatically by TenantEntityProcessor
        // Add your custom initialization here
    }

    /**
     * Execute logic before creating entity
     */
    protected function beforeCreate(Reminder $reminder): void
    {
        // Override to add logic before create
    }

    /**
     * Execute logic after creating entity
     */
    protected function afterCreate(Reminder $reminder): void
    {
        // Override to add logic after create (e.g., send notification, trigger event)
    }

    /**
     * Execute logic before updating entity
     */
    protected function beforeUpdate(Reminder $reminder): void
    {
        // Override to add logic before update
    }

    /**
     * Execute logic after updating entity
     */
    protected function afterUpdate(Reminder $reminder): void
    {
        // Override to add logic after update (e.g., send notification, trigger event)
    }

    /**
     * Execute logic before deleting entity
     */
    protected function beforeDelete(Reminder $reminder): void
    {
        // Override to add logic before delete (e.g., check dependencies, archive)
    }

    /**
     * Execute logic after deleting entity
     */
    protected function afterDelete(): void
    {
        // Override to add logic after delete (e.g., cleanup, send notification)
    }
}
