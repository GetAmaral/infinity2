<?php

declare(strict_types=1);

namespace App\Controller\Generated;

use App\Controller\Base\BaseApiController;
use App\Entity\ProfileTemplate;
use App\Repository\ProfileTemplateRepository;
use App\Security\Voter\ProfileTemplateVoter;
use App\Form\ProfileTemplateType;
use App\Service\ListPreferencesService;
use Doctrine\ORM\EntityManagerInterface;
use Symfony\Component\HttpFoundation\Request;
use Symfony\Component\HttpFoundation\Response;
use Symfony\Component\HttpFoundation\JsonResponse;
use Symfony\Component\Security\Csrf\CsrfToken;
use Symfony\Component\Security\Csrf\CsrfTokenManagerInterface;
use Symfony\Component\Security\Core\Exception\InvalidCsrfTokenException;
use Symfony\Contracts\Translation\TranslatorInterface;

/**
 * Generated Base Controller for ProfileTemplate
 *
 * ⚠️ WARNING: This file is ALWAYS regenerated by Genmax
 * DO NOT EDIT THIS FILE - Edit ProfileTemplateController instead
 *
 * @generated
 * @codeCoverageIgnore
 */
abstract class ProfileTemplateControllerGenerated extends BaseApiController
{
    public function __construct(
        protected readonly EntityManagerInterface $entityManager,
        protected readonly ProfileTemplateRepository $repository,
        protected readonly ListPreferencesService $listPreferencesService,
        protected readonly TranslatorInterface $translator,
        protected readonly CsrfTokenManagerInterface $csrfTokenManager,
    ) {}

    // ====================================
    // ABSTRACT METHOD IMPLEMENTATIONS
    // ====================================

    /**
     * Get the repository for this entity
     */
    protected function getRepository(): ProfileTemplateRepository
    {
        return $this->repository;
    }

    /**
     * Get the plural name for this entity
     */
    protected function getEntityPluralName(): string
    {
        return 'profileTemplates';
    }

    /**
     * Convert entity to array for API responses
     *
     * ⚠️ IMPORTANT: Enum Properties Stored as Strings
     * -------------------------------------------------
     * Properties with enum-backed types (like InputType) are stored as STRINGS
     * in the database and getters return strings directly, NOT enum objects.
     *
     * ❌ WRONG:   $entity->getType()->value   (Error: "Attempt to read property 'value' on string")
     * ✅ CORRECT: $entity->getType()          (Already a string - 'fully_completed')
     *
     * If you need the enum object for business logic, use:
     *    InputType::from($entity->getType())
     */
    protected function entityToArray(object $entity): array
    {
        if (!$entity instanceof ProfileTemplate) {
            throw new \InvalidArgumentException('Expected ProfileTemplate instance');
        }

        return [
            'id' => $entity->getId()->toString(),
            'name' => $entity->getName(),
            'grantedRoles' => ($grantedRolesRel = $entity->getGrantedRoles()) ? array_map(
                fn($item) => [
                    'id' => $item->getId()->toString(),
                    'display' => (string) $item,
                ],
                $grantedRolesRel->toArray()
            ) : [],
        ];
    }

    // ====================================
    // INDEX ACTION - List all ProfileTemplates
    // ====================================

    /**
     * List ProfileTemplates
     */
    protected function indexAction(): Response
    {
        $this->denyAccessUnlessGranted(ProfileTemplateVoter::LIST);

        $preferences = $this->listPreferencesService->getEntityPreferences('profileTemplates');
        $savedView = $preferences['view'] ?? 'grid';

        return $this->render('profiletemplate/index.html.twig', [
            'entities' => [],  // Loaded via API
            'entity_name' => 'profiletemplate',
            'entity_name_plural' => 'profileTemplates',
            'page_icon' => 'bi-person-lines-fill',
            'default_view' => $savedView,

            // List configuration from GeneratorProperty
            'enable_search' => true,
            'enable_filters' => false,
            'enable_sorting' => true,
            'enable_create_button' => true,
            'create_permission' => ProfileTemplateVoter::CREATE,

            // Property metadata for Twig templates (as PHP arrays)
            'listProperties' => json_decode('[{"name":"name","label":"Name","type":"string","sortable":true,"searchable":true,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getName","isRelationship":false},{"name":"grantedRoles","label":"GrantedRoles","type":"","sortable":true,"searchable":false,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getGrantedRoles","isRelationship":true}]', true),
            'searchableFields' => json_decode('[{"name":"name","label":"Name","type":"string"}]', true),
            'filterableFields' => json_decode('[]', true),
            'sortableFields' => json_decode('[{"name":"name","label":"Name"},{"name":"grantedRoles","label":"GrantedRoles"}]', true),

            // Property metadata for client-side rendering (as JSON strings)
            'list_fields' => '[{"name":"name","label":"Name","type":"string","sortable":true,"searchable":true,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getName","isRelationship":false},{"name":"grantedRoles","label":"GrantedRoles","type":"","sortable":true,"searchable":false,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getGrantedRoles","isRelationship":true}]',
            'searchable_fields' => '[{"name":"name","label":"Name","type":"string"}]',
            'filterable_fields' => '[]',
            'sortable_fields' => '[{"name":"name","label":"Name"},{"name":"grantedRoles","label":"GrantedRoles"}]',
        ]);
    }

    // ====================================
    // API SEARCH - Fetch ProfileTemplates for list view
    // ====================================

    /**
     * API endpoint for searching ProfileTemplates
     */
    protected function apiSearchAction(Request $request): JsonResponse
    {
        $this->denyAccessUnlessGranted(ProfileTemplateVoter::LIST);

        // Delegate to parent BaseApiController which handles
        // search, filtering, sorting, and pagination
        return parent::apiSearchAction($request);
    }

    // ====================================
    // NEW FORM ACTION - Display create form
    // ====================================

    /**
     * Display new profileTemplate form
     */
    protected function newFormAction(Request $request): Response
    {
        $this->denyAccessUnlessGranted(ProfileTemplateVoter::CREATE);

        $profileTemplate = new ProfileTemplate();

        // Initialize with custom logic if needed
        $this->initializeNewEntity($profileTemplate);

        $form = $this->createForm(ProfileTemplateType::class, $profileTemplate);

        return $this->render('profiletemplate/_form_modal.html.twig', [
            'form' => $form,
            'profileTemplate' => $profileTemplate,
            'is_edit' => false,
        ]);
    }

    // ====================================
    // CREATE ACTION - Process create form submission
    // ====================================

    /**
     * Process new profileTemplate form submission
     */
    protected function createAction(Request $request): Response
    {
        $this->denyAccessUnlessGranted(ProfileTemplateVoter::CREATE);

        $profileTemplate = new ProfileTemplate();

        // Initialize with custom logic if needed
        $this->initializeNewEntity($profileTemplate);

        $form = $this->createForm(ProfileTemplateType::class, $profileTemplate);
        $form->handleRequest($request);

        if ($form->isSubmitted() && $form->isValid()) {
            try {
                // Before create hook
                $this->beforeCreate($profileTemplate);

                $this->entityManager->persist($profileTemplate);
                $this->entityManager->flush();

                // After create hook
                $this->afterCreate($profileTemplate);

                $this->addFlash('success', $this->translator->trans(
                    'profiletemplate.flash.created_successfully',
                    ['%name%' => (string) $profileTemplate],
                    'profiletemplate'
                ));

                return $this->redirectToRoute('profiletemplate_index', [], Response::HTTP_SEE_OTHER);

            } catch (\Exception $e) {
                $this->addFlash('error', $this->translator->trans(
                    'profiletemplate.flash.create_failed',
                    ['%error%' => $e->getMessage()],
                    'profiletemplate'
                ));
            }
        }

        // Re-render with errors
        return $this->render('profiletemplate/_form_modal.html.twig', [
            'form' => $form,
            'profileTemplate' => $profileTemplate,
            'is_edit' => false,
        ], new Response('', $form->isSubmitted() ? Response::HTTP_UNPROCESSABLE_ENTITY : Response::HTTP_OK));
    }

    // ====================================
    // EDIT FORM ACTION - Display edit form
    // ====================================

    /**
     * Display edit profileTemplate form
     */
    protected function editFormAction(ProfileTemplate $profileTemplate, Request $request): Response
    {
        $this->denyAccessUnlessGranted(ProfileTemplateVoter::EDIT, $profileTemplate);

        $form = $this->createForm(ProfileTemplateType::class, $profileTemplate);

        return $this->render('profiletemplate/_form_modal.html.twig', [
            'form' => $form,
            'profileTemplate' => $profileTemplate,
            'is_edit' => true,
        ]);
    }

    // ====================================
    // UPDATE ACTION - Process edit form submission
    // ====================================

    /**
     * Process edit profileTemplate form submission
     */
    protected function updateAction(ProfileTemplate $profileTemplate, Request $request): Response
    {
        $this->denyAccessUnlessGranted(ProfileTemplateVoter::EDIT, $profileTemplate);

        $form = $this->createForm(ProfileTemplateType::class, $profileTemplate);
        $form->handleRequest($request);

        if ($form->isSubmitted() && $form->isValid()) {
            try {
                // Before update hook
                $this->beforeUpdate($profileTemplate);

                $this->entityManager->flush();

                // After update hook
                $this->afterUpdate($profileTemplate);

                $this->addFlash('success', $this->translator->trans(
                    'profiletemplate.flash.updated_successfully',
                    ['%name%' => (string) $profileTemplate],
                    'profiletemplate'
                ));

                return $this->redirectToRoute('profiletemplate_index', [], Response::HTTP_SEE_OTHER);

            } catch (\Exception $e) {
                $this->addFlash('error', $this->translator->trans(
                    'profiletemplate.flash.update_failed',
                    ['%error%' => $e->getMessage()],
                    'profiletemplate'
                ));
            }
        }

        // Re-render with errors
        return $this->render('profiletemplate/_form_modal.html.twig', [
            'form' => $form,
            'profileTemplate' => $profileTemplate,
            'is_edit' => true,
        ], new Response('', $form->isSubmitted() ? Response::HTTP_UNPROCESSABLE_ENTITY : Response::HTTP_OK));
    }

    // ====================================
    // DELETE ACTION - Delete profileTemplate
    // ====================================

    /**
     * Delete profileTemplate
     */
    protected function deleteAction(ProfileTemplate $profileTemplate, Request $request): Response
    {
        $this->denyAccessUnlessGranted(ProfileTemplateVoter::DELETE, $profileTemplate);

        // Verify CSRF token
        $token = $request->request->get('_token');
        if (!$this->csrfTokenManager->isTokenValid(new CsrfToken('delete_profileTemplate_' . $profileTemplate->getId()->toString(), $token))) {
            throw new InvalidCsrfTokenException('Invalid CSRF token for delete operation.');
        }

        try {
            // Before delete hook
            $this->beforeDelete($profileTemplate);

            $name = (string) $profileTemplate;

            $this->entityManager->remove($profileTemplate);
            $this->entityManager->flush();

            // After delete hook
            $this->afterDelete();

            $this->addFlash('success', $this->translator->trans(
                'profiletemplate.flash.deleted_successfully',
                ['%name%' => $name],
                'profiletemplate'
            ));

        } catch (\Exception $e) {
            $this->addFlash('error', $this->translator->trans(
                'profiletemplate.flash.delete_failed',
                ['%error%' => $e->getMessage()],
                'profiletemplate'
            ));
        }

        return $this->redirectToRoute('profiletemplate_index', [], Response::HTTP_SEE_OTHER);
    }

    // ====================================
    // SHOW ACTION - Display profileTemplate details
    // ====================================

    /**
     * Display profileTemplate details
     */
    protected function showAction(ProfileTemplate $profileTemplate): Response
    {
        $this->denyAccessUnlessGranted(ProfileTemplateVoter::VIEW, $profileTemplate);

        return $this->render('profiletemplate/show.html.twig', [
            'profileTemplate' => $profileTemplate,
        ]);
    }

    // ====================================
    // LIFECYCLE HOOKS
    // ====================================
    // Override these in ProfileTemplateController to add custom logic

    /**
     * Initialize new entity before creating form
     *
     * Note: Organization and Owner are set automatically by TenantEntityProcessor
     * Only use this for custom initialization logic
     */
    protected function initializeNewEntity(ProfileTemplate $profileTemplate): void
    {
        // Organization and Owner are set automatically by TenantEntityProcessor
        // Add your custom initialization here
    }

    /**
     * Execute logic before creating entity
     */
    protected function beforeCreate(ProfileTemplate $profileTemplate): void
    {
        // Override to add logic before create
    }

    /**
     * Execute logic after creating entity
     */
    protected function afterCreate(ProfileTemplate $profileTemplate): void
    {
        // Override to add logic after create (e.g., send notification, trigger event)
    }

    /**
     * Execute logic before updating entity
     */
    protected function beforeUpdate(ProfileTemplate $profileTemplate): void
    {
        // Override to add logic before update
    }

    /**
     * Execute logic after updating entity
     */
    protected function afterUpdate(ProfileTemplate $profileTemplate): void
    {
        // Override to add logic after update (e.g., send notification, trigger event)
    }

    /**
     * Execute logic before deleting entity
     */
    protected function beforeDelete(ProfileTemplate $profileTemplate): void
    {
        // Override to add logic before delete (e.g., check dependencies, archive)
    }

    /**
     * Execute logic after deleting entity
     */
    protected function afterDelete(): void
    {
        // Override to add logic after delete (e.g., cleanup, send notification)
    }
}
