<?php

declare(strict_types=1);

namespace App\Controller\Generated;

use App\Controller\Base\BaseApiController;
use App\Entity\TreeFlow;
use App\MultiTenant\TenantContext;
use App\Repository\TreeFlowRepository;
use App\Security\Voter\TreeFlowVoter;
use App\Form\TreeFlowType;
use App\Service\ListPreferencesService;
use Doctrine\ORM\EntityManagerInterface;
use Symfony\Component\HttpFoundation\Request;
use Symfony\Component\HttpFoundation\Response;
use Symfony\Component\HttpFoundation\JsonResponse;
use Symfony\Component\Security\Csrf\CsrfToken;
use Symfony\Component\Security\Csrf\CsrfTokenManagerInterface;
use Symfony\Component\Security\Core\Exception\InvalidCsrfTokenException;
use Symfony\Contracts\Translation\TranslatorInterface;

/**
 * Generated Base Controller for TreeFlow
 *
 * ⚠️ WARNING: This file is ALWAYS regenerated by Genmax
 * DO NOT EDIT THIS FILE - Edit TreeFlowController instead
 *
 * @generated
 * @codeCoverageIgnore
 */
abstract class TreeFlowControllerGenerated extends BaseApiController
{
    public function __construct(
        protected readonly EntityManagerInterface $entityManager,
        protected readonly TreeFlowRepository $repository,
        protected readonly ListPreferencesService $listPreferencesService,
        protected readonly TranslatorInterface $translator,
        protected readonly CsrfTokenManagerInterface $csrfTokenManager,
        protected readonly TenantContext $tenantContext,
    ) {}

    // ====================================
    // ABSTRACT METHOD IMPLEMENTATIONS
    // ====================================

    /**
     * Get the repository for this entity
     */
    protected function getRepository(): TreeFlowRepository
    {
        return $this->repository;
    }

    /**
     * Get the plural name for this entity
     */
    protected function getEntityPluralName(): string
    {
        return 'treeFlows';
    }

    /**
     * Convert entity to array for API responses
     *
     * ⚠️ IMPORTANT: Enum Properties Stored as Strings
     * -------------------------------------------------
     * Properties with enum-backed types (like InputType) are stored as STRINGS
     * in the database and getters return strings directly, NOT enum objects.
     *
     * ❌ WRONG:   $entity->getType()->value   (Error: "Attempt to read property 'value' on string")
     * ✅ CORRECT: $entity->getType()          (Already a string - 'fully_completed')
     *
     * If you need the enum object for business logic, use:
     *    InputType::from($entity->getType())
     */
    protected function entityToArray(object $entity): array
    {
        if (!$entity instanceof TreeFlow) {
            throw new \InvalidArgumentException('Expected TreeFlow instance');
        }

        return [
            'id' => $entity->getId()->toString(),
            'organization' => ($organizationRel = $entity->getOrganization()) ? [
                'id' => $organizationRel->getId()->toString(),
                'display' => (string) $organizationRel,
            ] : null,
            'name' => $entity->getName(),
            'slug' => $entity->getSlug(),
            'version' => $entity->getVersion(),
            'canvasViewState' => $entity->getCanvasViewState(),
            'jsonStructure' => $entity->getJsonStructure(),
            'talkFlow' => $entity->getTalkFlow(),
            'steps' => ($stepsRel = $entity->getSteps()) ? array_map(
                fn($item) => [
                    'id' => $item->getId()->toString(),
                    'display' => (string) $item,
                ],
                $stepsRel->toArray()
            ) : [],
            'active' => $entity->getActive(),
        ];
    }

    // ====================================
    // INDEX ACTION - List all TreeFlows
    // ====================================

    /**
     * List TreeFlows
     */
    protected function indexAction(): Response
    {
        $this->denyAccessUnlessGranted(TreeFlowVoter::LIST);

        $preferences = $this->listPreferencesService->getEntityPreferences('treeFlows');
        $savedView = $preferences['view'] ?? 'grid';

        return $this->render('treeflow/index.html.twig', [
            'entities' => [],  // Loaded via API
            'entity_name' => 'treeflow',
            'entity_name_plural' => 'treeFlows',
            'page_icon' => 'bi-diagram-3',
            'default_view' => $savedView,

            // List configuration from GeneratorProperty
            'enable_search' => true,
            'enable_filters' => true,
            'enable_sorting' => true,
            'enable_create_button' => true,
            'create_permission' => TreeFlowVoter::CREATE,

            // Property metadata for Twig templates (as PHP arrays)
            'listProperties' => json_decode('[{"name":"name","label":"Name","type":"string","sortable":true,"searchable":true,"filterable":true,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getName","isRelationship":false},{"name":"slug","label":"Slug","type":"string","sortable":true,"searchable":true,"filterable":true,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getSlug","isRelationship":false},{"name":"version","label":"Version","type":"integer","sortable":true,"searchable":false,"filterable":true,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getVersion","isRelationship":false},{"name":"active","label":"Active","type":"boolean","sortable":true,"searchable":false,"filterable":true,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getActive","isRelationship":false}]', true),
            'searchableFields' => json_decode('[{"name":"name","label":"Name","type":"string"},{"name":"slug","label":"Slug","type":"string"},{"name":"steps","label":"Steps","type":"string"}]', true),
            'filterableFields' => json_decode('[{"name":"name","label":"Name","type":"string","strategy":null,"boolean":false,"date":false,"numericRange":false,"exists":false},{"name":"slug","label":"Slug","type":"string","strategy":null,"boolean":false,"date":false,"numericRange":false,"exists":false},{"name":"version","label":"Version","type":"integer","strategy":null,"boolean":false,"date":false,"numericRange":false,"exists":false},{"name":"canvasViewState","label":"CanvasViewState","type":"json","strategy":null,"boolean":false,"date":false,"numericRange":false,"exists":false},{"name":"jsonStructure","label":"JsonStructure","type":"json","strategy":null,"boolean":false,"date":false,"numericRange":false,"exists":false},{"name":"talkFlow","label":"TalkFlow","type":"json","strategy":null,"boolean":false,"date":false,"numericRange":false,"exists":false},{"name":"steps","label":"Steps","type":"string","strategy":null,"boolean":false,"date":false,"numericRange":false,"exists":false},{"name":"active","label":"Active","type":"boolean","strategy":null,"boolean":false,"date":false,"numericRange":false,"exists":false}]', true),
            'sortableFields' => json_decode('[{"name":"name","label":"Name"},{"name":"slug","label":"Slug"},{"name":"version","label":"Version"},{"name":"canvasViewState","label":"CanvasViewState"},{"name":"jsonStructure","label":"JsonStructure"},{"name":"talkFlow","label":"TalkFlow"},{"name":"steps","label":"Steps"},{"name":"active","label":"Active"}]', true),

            // Property metadata for client-side rendering (as JSON strings)
            'list_fields' => '[{"name":"name","label":"Name","type":"string","sortable":true,"searchable":true,"filterable":true,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getName","isRelationship":false},{"name":"slug","label":"Slug","type":"string","sortable":true,"searchable":true,"filterable":true,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getSlug","isRelationship":false},{"name":"version","label":"Version","type":"integer","sortable":true,"searchable":false,"filterable":true,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getVersion","isRelationship":false},{"name":"active","label":"Active","type":"boolean","sortable":true,"searchable":false,"filterable":true,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getActive","isRelationship":false}]',
            'searchable_fields' => '[{"name":"name","label":"Name","type":"string"},{"name":"slug","label":"Slug","type":"string"},{"name":"steps","label":"Steps","type":"string"}]',
            'filterable_fields' => '[{"name":"name","label":"Name","type":"string","strategy":null,"boolean":false,"date":false,"numericRange":false,"exists":false},{"name":"slug","label":"Slug","type":"string","strategy":null,"boolean":false,"date":false,"numericRange":false,"exists":false},{"name":"version","label":"Version","type":"integer","strategy":null,"boolean":false,"date":false,"numericRange":false,"exists":false},{"name":"canvasViewState","label":"CanvasViewState","type":"json","strategy":null,"boolean":false,"date":false,"numericRange":false,"exists":false},{"name":"jsonStructure","label":"JsonStructure","type":"json","strategy":null,"boolean":false,"date":false,"numericRange":false,"exists":false},{"name":"talkFlow","label":"TalkFlow","type":"json","strategy":null,"boolean":false,"date":false,"numericRange":false,"exists":false},{"name":"steps","label":"Steps","type":"string","strategy":null,"boolean":false,"date":false,"numericRange":false,"exists":false},{"name":"active","label":"Active","type":"boolean","strategy":null,"boolean":false,"date":false,"numericRange":false,"exists":false}]',
            'sortable_fields' => '[{"name":"name","label":"Name"},{"name":"slug","label":"Slug"},{"name":"version","label":"Version"},{"name":"canvasViewState","label":"CanvasViewState"},{"name":"jsonStructure","label":"JsonStructure"},{"name":"talkFlow","label":"TalkFlow"},{"name":"steps","label":"Steps"},{"name":"active","label":"Active"}]',
        ]);
    }

    // ====================================
    // API SEARCH - Fetch TreeFlows for list view
    // ====================================

    /**
     * API endpoint for searching TreeFlows
     */
    protected function apiSearchAction(Request $request): JsonResponse
    {
        $this->denyAccessUnlessGranted(TreeFlowVoter::LIST);

        // Delegate to parent BaseApiController which handles
        // search, filtering, sorting, and pagination
        return parent::apiSearchAction($request);
    }

    // ====================================
    // NEW FORM ACTION - Display create form
    // ====================================

    /**
     * Display new treeFlow form
     */
    protected function newFormAction(Request $request): Response
    {
        $this->denyAccessUnlessGranted(TreeFlowVoter::CREATE);

        $treeFlow = new TreeFlow();

        // Initialize with custom logic if needed
        $this->initializeNewEntity($treeFlow);

        $form = $this->createForm(TreeFlowType::class, $treeFlow);

        return $this->render('treeflow/_form_modal.html.twig', [
            'form' => $form,
            'treeFlow' => $treeFlow,
            'is_edit' => false,
        ]);
    }

    // ====================================
    // CREATE ACTION - Process create form submission
    // ====================================

    /**
     * Process new treeFlow form submission
     */
    protected function createAction(Request $request): Response
    {
        $this->denyAccessUnlessGranted(TreeFlowVoter::CREATE);

        $treeFlow = new TreeFlow();

        // Initialize with custom logic if needed
        $this->initializeNewEntity($treeFlow);

        $form = $this->createForm(TreeFlowType::class, $treeFlow);
        $form->handleRequest($request);

        if ($form->isSubmitted()) {
            // Re-set organization after form handling (form excludes this field)
            $organization = $this->tenantContext->getOrganizationForNewEntity();
            if ($organization) {
                $treeFlow->setOrganization($organization);
                error_log('✅ TreeFlowController: Organization re-set after form handling to ' . $organization->getName());
            }

            if ($form->isValid()) {
                try {
                    // Before create hook
                    $this->beforeCreate($treeFlow);

                    $this->entityManager->persist($treeFlow);
                    $this->entityManager->flush();

                    // After create hook
                    $this->afterCreate($treeFlow);

                    $this->addFlash('success', $this->translator->trans(
                        'treeflow.flash.created_successfully',
                        ['%name%' => (string) $treeFlow],
                        'treeflow'
                    ));

                    // If this is a modal/AJAX request (from "+" button), return Turbo Stream with event dispatch
                    // Check both GET and POST for modal parameter
                    if ($request->headers->get('X-Requested-With') === 'turbo-frame' ||
                        $request->get('modal') === '1') {

                        // Get display text for the entity
                        $displayText = (string) $treeFlow;

                        $response = $this->render('_entity_created_success_stream.html.twig', [
                            'entityType' => 'TreeFlow',
                            'entityId' => $treeFlow->getId()->toRfc4122(),
                            'displayText' => $displayText,
                        ]);

                        // Set Turbo Stream content type so Turbo processes it without navigating
                        $response->headers->set('Content-Type', 'text/vnd.turbo-stream.html');

                        return $response;
                    }

                    return $this->redirectToRoute('treeflow_index', [], Response::HTTP_SEE_OTHER);

                } catch (\Exception $e) {
                    $this->addFlash('error', $this->translator->trans(
                        'treeflow.flash.create_failed',
                        ['%error%' => $e->getMessage()],
                        'treeflow'
                    ));
                }
            }
        }

        // Re-render with errors
        return $this->render('treeflow/_form_modal.html.twig', [
            'form' => $form,
            'treeFlow' => $treeFlow,
            'is_edit' => false,
        ], new Response('', $form->isSubmitted() ? Response::HTTP_UNPROCESSABLE_ENTITY : Response::HTTP_OK));
    }

    // ====================================
    // EDIT FORM ACTION - Display edit form
    // ====================================

    /**
     * Display edit treeFlow form
     */
    protected function editFormAction(TreeFlow $treeFlow, Request $request): Response
    {
        $this->denyAccessUnlessGranted(TreeFlowVoter::EDIT, $treeFlow);

        $form = $this->createForm(TreeFlowType::class, $treeFlow);

        return $this->render('treeflow/_form_modal.html.twig', [
            'form' => $form,
            'treeFlow' => $treeFlow,
            'is_edit' => true,
        ]);
    }

    // ====================================
    // UPDATE ACTION - Process edit form submission
    // ====================================

    /**
     * Process edit treeFlow form submission
     */
    protected function updateAction(TreeFlow $treeFlow, Request $request): Response
    {
        $this->denyAccessUnlessGranted(TreeFlowVoter::EDIT, $treeFlow);

        // Store original organization to preserve it
        $originalOrganization = $treeFlow->getOrganization();

        $form = $this->createForm(TreeFlowType::class, $treeFlow);
        $form->handleRequest($request);

        if ($form->isSubmitted()) {
            // Restore organization after form handling (form excludes this field)
            if ($originalOrganization) {
                $treeFlow->setOrganization($originalOrganization);
            }

            if ($form->isValid()) {
                try {
                    // Before update hook
                    $this->beforeUpdate($treeFlow);

                    $this->entityManager->flush();

                    // After update hook
                    $this->afterUpdate($treeFlow);

                    $this->addFlash('success', $this->translator->trans(
                        'treeflow.flash.updated_successfully',
                        ['%name%' => (string) $treeFlow],
                        'treeflow'
                    ));

                    return $this->redirectToRoute('treeflow_index', [], Response::HTTP_SEE_OTHER);

                } catch (\Exception $e) {
                    $this->addFlash('error', $this->translator->trans(
                        'treeflow.flash.update_failed',
                        ['%error%' => $e->getMessage()],
                        'treeflow'
                    ));
                }
            }
        }

        // Re-render with errors
        return $this->render('treeflow/_form_modal.html.twig', [
            'form' => $form,
            'treeFlow' => $treeFlow,
            'is_edit' => true,
        ], new Response('', $form->isSubmitted() ? Response::HTTP_UNPROCESSABLE_ENTITY : Response::HTTP_OK));
    }

    // ====================================
    // DELETE ACTION - Delete treeFlow
    // ====================================

    /**
     * Delete treeFlow
     */
    protected function deleteAction(TreeFlow $treeFlow, Request $request): Response
    {
        $this->denyAccessUnlessGranted(TreeFlowVoter::DELETE, $treeFlow);

        // Verify CSRF token
        $token = $request->request->get('_token');
        if (!$this->csrfTokenManager->isTokenValid(new CsrfToken('delete_treeFlow_' . $treeFlow->getId()->toString(), $token))) {
            throw new InvalidCsrfTokenException('Invalid CSRF token for delete operation.');
        }

        try {
            // Before delete hook
            $this->beforeDelete($treeFlow);

            $name = (string) $treeFlow;

            $this->entityManager->remove($treeFlow);
            $this->entityManager->flush();

            // After delete hook
            $this->afterDelete();

            $this->addFlash('success', $this->translator->trans(
                'treeflow.flash.deleted_successfully',
                ['%name%' => $name],
                'treeflow'
            ));

        } catch (\Exception $e) {
            $this->addFlash('error', $this->translator->trans(
                'treeflow.flash.delete_failed',
                ['%error%' => $e->getMessage()],
                'treeflow'
            ));
        }

        return $this->redirectToRoute('treeflow_index', [], Response::HTTP_SEE_OTHER);
    }

    // ====================================
    // SHOW ACTION - Display treeFlow details
    // ====================================

    /**
     * Display treeFlow details
     */
    protected function showAction(TreeFlow $treeFlow): Response
    {
        $this->denyAccessUnlessGranted(TreeFlowVoter::VIEW, $treeFlow);

        // Build show properties configuration for view
        $showProperties = $this->buildShowProperties($treeFlow);

        return $this->render('treeflow/show.html.twig', [
            'treeFlow' => $treeFlow,
            'showProperties' => $showProperties,
        ]);
    }

    /**
     * Build show properties configuration
     * Override this method in TreeFlowController to customize displayed properties
     */
    protected function buildShowProperties(TreeFlow $treeFlow): array
    {
        return [];
    }

    // ====================================
    // LIFECYCLE HOOKS
    // ====================================
    // Override these in TreeFlowController to add custom logic

    /**
     * Initialize new entity before creating form
     *
     * Sets organization from multi-tenant context.
     * Multi-tenant system handles: subdomain OR user's organization fallback.
     *
     * This runs BEFORE form validation, ensuring required organization field is set.
     */
    protected function initializeNewEntity(TreeFlow $treeFlow): void
    {
        // Auto-set organization from multi-tenant context
        $organization = $this->tenantContext->getOrganizationForNewEntity();
        if ($organization) {
            $treeFlow->setOrganization($organization);
            error_log('✅ TreeFlowController: Organization set to ' . $organization->getName());
        } else {
            error_log('❌ TreeFlowController: No organization available from TenantContext');
        }

        // Add your custom initialization here
    }

    /**
     * Execute logic before creating entity
     */
    protected function beforeCreate(TreeFlow $treeFlow): void
    {
        // Override to add logic before create
    }

    /**
     * Execute logic after creating entity
     */
    protected function afterCreate(TreeFlow $treeFlow): void
    {
        // Override to add logic after create (e.g., send notification, trigger event)
    }

    /**
     * Execute logic before updating entity
     */
    protected function beforeUpdate(TreeFlow $treeFlow): void
    {
        // Override to add logic before update
    }

    /**
     * Execute logic after updating entity
     */
    protected function afterUpdate(TreeFlow $treeFlow): void
    {
        // Override to add logic after update (e.g., send notification, trigger event)
    }

    /**
     * Execute logic before deleting entity
     */
    protected function beforeDelete(TreeFlow $treeFlow): void
    {
        // Override to add logic before delete (e.g., check dependencies, archive)
    }

    /**
     * Execute logic after deleting entity
     */
    protected function afterDelete(): void
    {
        // Override to add logic after delete (e.g., cleanup, send notification)
    }
}
