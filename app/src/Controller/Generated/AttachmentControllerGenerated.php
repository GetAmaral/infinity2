<?php

declare(strict_types=1);

namespace App\Controller\Generated;

use App\Controller\Base\BaseApiController;
use App\Entity\Attachment;
use App\Repository\AttachmentRepository;
use App\Security\Voter\AttachmentVoter;
use App\Form\AttachmentFormType;
use App\Service\ListPreferencesService;
use Doctrine\ORM\EntityManagerInterface;
use Symfony\Component\HttpFoundation\Request;
use Symfony\Component\HttpFoundation\Response;
use Symfony\Component\HttpFoundation\JsonResponse;
use Symfony\Component\Security\Csrf\CsrfToken;
use Symfony\Component\Security\Csrf\CsrfTokenManagerInterface;
use Symfony\Component\Security\Core\Exception\InvalidCsrfTokenException;
use Symfony\Contracts\Translation\TranslatorInterface;

/**
 * Generated Base Controller for Attachment
 *
 * ⚠️ WARNING: This file is ALWAYS regenerated by Genmax
 * DO NOT EDIT THIS FILE - Edit AttachmentController instead
 *
 * @generated
 * @codeCoverageIgnore
 */
abstract class AttachmentControllerGenerated extends BaseApiController
{
    public function __construct(
        protected readonly EntityManagerInterface $entityManager,
        protected readonly AttachmentRepository $repository,
        protected readonly ListPreferencesService $listPreferencesService,
        protected readonly TranslatorInterface $translator,
        protected readonly CsrfTokenManagerInterface $csrfTokenManager,
    ) {}

    // ====================================
    // ABSTRACT METHOD IMPLEMENTATIONS
    // ====================================

    /**
     * Get the repository for this entity
     */
    protected function getRepository(): AttachmentRepository
    {
        return $this->repository;
    }

    /**
     * Get the plural name for this entity
     */
    protected function getEntityPluralName(): string
    {
        return 'attachments';
    }

    /**
     * Convert entity to array for API responses
     */
    protected function entityToArray(object $entity): array
    {
        if (!$entity instanceof Attachment) {
            throw new \InvalidArgumentException('Expected Attachment instance');
        }

        return [
            'id' => $entity->getId()->toString(),
            'organization' => ($organizationRel = $entity->getOrganization()) ? [
                'id' => $organizationRel->getId()->toString(),
                'display' => (string) $organizationRel,
            ] : null,
            'event' => ($eventRel = $entity->getEvent()) ? [
                'id' => $eventRel->getId()->toString(),
                'display' => (string) $eventRel,
            ] : null,
            'fileSize' => $entity->getFileSize(),
            'fileType' => $entity->getFileType(),
            'filename' => $entity->getFilename(),
            'product' => ($productRel = $entity->getProduct()) ? [
                'id' => $productRel->getId()->toString(),
                'display' => (string) $productRel,
            ] : null,
            'talkMessage' => ($talkMessageRel = $entity->getTalkMessage()) ? [
                'id' => $talkMessageRel->getId()->toString(),
                'display' => (string) $talkMessageRel,
            ] : null,
            'url' => $entity->getUrl(),
        ];
    }

    // ====================================
    // INDEX ACTION - List all Attachments
    // ====================================

    /**
     * List Attachments
     */
    protected function indexAction(): Response
    {
        $this->denyAccessUnlessGranted(AttachmentVoter::LIST);

        $preferences = $this->listPreferencesService->getEntityPreferences('attachments');
        $savedView = $preferences['view'] ?? 'grid';

        return $this->render('attachment/index.html.twig', [
            'entities' => [],  // Loaded via API
            'entity_name' => 'attachment',
            'entity_name_plural' => 'attachments',
            'page_icon' => 'bi-paperclip',
            'default_view' => $savedView,

            // List configuration from GeneratorProperty
            'enable_search' => true,
            'enable_filters' => false,
            'enable_sorting' => true,
            'enable_create_button' => true,

            // Property metadata for client-side rendering (as JSON strings)
            'list_fields' => '[{"name":"event","label":"Event","type":"","sortable":true,"searchable":false,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getEvent"},{"name":"fileSize","label":"FileSize","type":"integer","sortable":true,"searchable":false,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getFileSize"},{"name":"fileType","label":"FileType","type":"string","sortable":true,"searchable":true,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getFileType"},{"name":"filename","label":"Filename","type":"string","sortable":true,"searchable":true,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getFilename"},{"name":"product","label":"Product","type":"","sortable":true,"searchable":false,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getProduct"},{"name":"talkMessage","label":"TalkMessage","type":"","sortable":true,"searchable":false,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getTalkMessage"},{"name":"url","label":"Url","type":"string","sortable":true,"searchable":true,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getUrl"}]',
            'searchable_fields' => '[{"name":"fileType","label":"FileType","type":"string"},{"name":"filename","label":"Filename","type":"string"},{"name":"url","label":"Url","type":"string"}]',
            'filterable_fields' => '[]',
            'sortable_fields' => '[{"name":"event","label":"Event"},{"name":"fileSize","label":"FileSize"},{"name":"fileType","label":"FileType"},{"name":"filename","label":"Filename"},{"name":"product","label":"Product"},{"name":"talkMessage","label":"TalkMessage"},{"name":"url","label":"Url"}]',
        ]);
    }

    // ====================================
    // API SEARCH - Fetch Attachments for list view
    // ====================================

    /**
     * API endpoint for searching Attachments
     */
    protected function apiSearchAction(Request $request): JsonResponse
    {
        $this->denyAccessUnlessGranted(AttachmentVoter::LIST);

        // This method uses the BaseApiController's handleSearchRequest
        // which integrates with API Platform's GetCollection operation
        return $this->handleSearchRequest($request);
    }

    // ====================================
    // NEW FORM ACTION - Display create form
    // ====================================

    /**
     * Display new attachment form
     */
    protected function newFormAction(Request $request): Response
    {
        $this->denyAccessUnlessGranted(AttachmentVoter::CREATE);

        $attachment = new Attachment();

        // Initialize with custom logic if needed
        $this->initializeNewEntity($attachment);

        $form = $this->createForm(AttachmentFormType::class, $attachment);

        return $this->render('attachment/_form_modal.html.twig', [
            'form' => $form,
            'attachment' => $attachment,
            'is_edit' => false,
        ]);
    }

    // ====================================
    // CREATE ACTION - Process create form submission
    // ====================================

    /**
     * Process new attachment form submission
     */
    protected function createAction(Request $request): Response
    {
        $this->denyAccessUnlessGranted(AttachmentVoter::CREATE);

        $attachment = new Attachment();

        // Initialize with custom logic if needed
        $this->initializeNewEntity($attachment);

        $form = $this->createForm(AttachmentFormType::class, $attachment);
        $form->handleRequest($request);

        if ($form->isSubmitted() && $form->isValid()) {
            try {
                // Before create hook
                $this->beforeCreate($attachment);

                $this->entityManager->persist($attachment);
                $this->entityManager->flush();

                // After create hook
                $this->afterCreate($attachment);

                $this->addFlash('success', $this->translator->trans(
                    'attachment.flash.created_successfully',
                    ['%name%' => (string) $attachment],
                    'attachment'
                ));

                return $this->redirectToRoute('attachment_index', [], Response::HTTP_SEE_OTHER);

            } catch (\Exception $e) {
                $this->addFlash('error', $this->translator->trans(
                    'attachment.flash.create_failed',
                    ['%error%' => $e->getMessage()],
                    'attachment'
                ));
            }
        }

        // Re-render with errors
        return $this->render('attachment/_form_modal.html.twig', [
            'form' => $form,
            'attachment' => $attachment,
            'is_edit' => false,
        ], new Response('', $form->isSubmitted() ? Response::HTTP_UNPROCESSABLE_ENTITY : Response::HTTP_OK));
    }

    // ====================================
    // EDIT FORM ACTION - Display edit form
    // ====================================

    /**
     * Display edit attachment form
     */
    protected function editFormAction(Attachment $attachment, Request $request): Response
    {
        $this->denyAccessUnlessGranted(AttachmentVoter::EDIT, $attachment);

        $form = $this->createForm(AttachmentFormType::class, $attachment);

        return $this->render('attachment/_form_modal.html.twig', [
            'form' => $form,
            'attachment' => $attachment,
            'is_edit' => true,
        ]);
    }

    // ====================================
    // UPDATE ACTION - Process edit form submission
    // ====================================

    /**
     * Process edit attachment form submission
     */
    protected function updateAction(Attachment $attachment, Request $request): Response
    {
        $this->denyAccessUnlessGranted(AttachmentVoter::EDIT, $attachment);

        $form = $this->createForm(AttachmentFormType::class, $attachment);
        $form->handleRequest($request);

        if ($form->isSubmitted() && $form->isValid()) {
            try {
                // Before update hook
                $this->beforeUpdate($attachment);

                $this->entityManager->flush();

                // After update hook
                $this->afterUpdate($attachment);

                $this->addFlash('success', $this->translator->trans(
                    'attachment.flash.updated_successfully',
                    ['%name%' => (string) $attachment],
                    'attachment'
                ));

                return $this->redirectToRoute('attachment_index', [], Response::HTTP_SEE_OTHER);

            } catch (\Exception $e) {
                $this->addFlash('error', $this->translator->trans(
                    'attachment.flash.update_failed',
                    ['%error%' => $e->getMessage()],
                    'attachment'
                ));
            }
        }

        // Re-render with errors
        return $this->render('attachment/_form_modal.html.twig', [
            'form' => $form,
            'attachment' => $attachment,
            'is_edit' => true,
        ], new Response('', $form->isSubmitted() ? Response::HTTP_UNPROCESSABLE_ENTITY : Response::HTTP_OK));
    }

    // ====================================
    // DELETE ACTION - Delete attachment
    // ====================================

    /**
     * Delete attachment
     */
    protected function deleteAction(Attachment $attachment, Request $request): Response
    {
        $this->denyAccessUnlessGranted(AttachmentVoter::DELETE, $attachment);

        // Verify CSRF token
        $token = $request->request->get('_token');
        if (!$this->csrfTokenManager->isTokenValid(new CsrfToken('delete_attachment_' . $attachment->getId()->toString(), $token))) {
            throw new InvalidCsrfTokenException('Invalid CSRF token for delete operation.');
        }

        try {
            // Before delete hook
            $this->beforeDelete($attachment);

            $name = (string) $attachment;

            $this->entityManager->remove($attachment);
            $this->entityManager->flush();

            // After delete hook
            $this->afterDelete();

            $this->addFlash('success', $this->translator->trans(
                'attachment.flash.deleted_successfully',
                ['%name%' => $name],
                'attachment'
            ));

        } catch (\Exception $e) {
            $this->addFlash('error', $this->translator->trans(
                'attachment.flash.delete_failed',
                ['%error%' => $e->getMessage()],
                'attachment'
            ));
        }

        return $this->redirectToRoute('attachment_index', [], Response::HTTP_SEE_OTHER);
    }

    // ====================================
    // SHOW ACTION - Display attachment details
    // ====================================

    /**
     * Display attachment details
     */
    protected function showAction(Attachment $attachment): Response
    {
        $this->denyAccessUnlessGranted(AttachmentVoter::VIEW, $attachment);

        return $this->render('attachment/show.html.twig', [
            'attachment' => $attachment,
        ]);
    }

    // ====================================
    // LIFECYCLE HOOKS
    // ====================================
    // Override these in AttachmentController to add custom logic

    /**
     * Initialize new entity before creating form
     *
     * Note: Organization and Owner are set automatically by TenantEntityProcessor
     * Only use this for custom initialization logic
     */
    protected function initializeNewEntity(Attachment $attachment): void
    {
        // Organization and Owner are set automatically by TenantEntityProcessor
        // Add your custom initialization here
    }

    /**
     * Execute logic before creating entity
     */
    protected function beforeCreate(Attachment $attachment): void
    {
        // Override to add logic before create
    }

    /**
     * Execute logic after creating entity
     */
    protected function afterCreate(Attachment $attachment): void
    {
        // Override to add logic after create (e.g., send notification, trigger event)
    }

    /**
     * Execute logic before updating entity
     */
    protected function beforeUpdate(Attachment $attachment): void
    {
        // Override to add logic before update
    }

    /**
     * Execute logic after updating entity
     */
    protected function afterUpdate(Attachment $attachment): void
    {
        // Override to add logic after update (e.g., send notification, trigger event)
    }

    /**
     * Execute logic before deleting entity
     */
    protected function beforeDelete(Attachment $attachment): void
    {
        // Override to add logic before delete (e.g., check dependencies, archive)
    }

    /**
     * Execute logic after deleting entity
     */
    protected function afterDelete(): void
    {
        // Override to add logic after delete (e.g., cleanup, send notification)
    }
}
