<?php

declare(strict_types=1);

namespace App\Controller\Generated;

use App\Controller\Base\BaseApiController;
use App\Entity\Module;
use App\Repository\ModuleRepository;
use App\Security\Voter\ModuleVoter;
use App\Form\ModuleType;
use App\Service\ListPreferencesService;
use Doctrine\ORM\EntityManagerInterface;
use Symfony\Component\HttpFoundation\Request;
use Symfony\Component\HttpFoundation\Response;
use Symfony\Component\HttpFoundation\JsonResponse;
use Symfony\Component\Security\Csrf\CsrfToken;
use Symfony\Component\Security\Csrf\CsrfTokenManagerInterface;
use Symfony\Component\Security\Core\Exception\InvalidCsrfTokenException;
use Symfony\Contracts\Translation\TranslatorInterface;

/**
 * Generated Base Controller for Module
 *
 * ⚠️ WARNING: This file is ALWAYS regenerated by Genmax
 * DO NOT EDIT THIS FILE - Edit ModuleController instead
 *
 * @generated
 * @codeCoverageIgnore
 */
abstract class ModuleControllerGenerated extends BaseApiController
{
    public function __construct(
        protected readonly EntityManagerInterface $entityManager,
        protected readonly ModuleRepository $repository,
        protected readonly ListPreferencesService $listPreferencesService,
        protected readonly TranslatorInterface $translator,
        protected readonly CsrfTokenManagerInterface $csrfTokenManager,
    ) {}

    // ====================================
    // ABSTRACT METHOD IMPLEMENTATIONS
    // ====================================

    /**
     * Get the repository for this entity
     */
    protected function getRepository(): ModuleRepository
    {
        return $this->repository;
    }

    /**
     * Get the plural name for this entity
     */
    protected function getEntityPluralName(): string
    {
        return 'modules';
    }

    /**
     * Convert entity to array for API responses
     *
     * ⚠️ IMPORTANT: Enum Properties Stored as Strings
     * -------------------------------------------------
     * Properties with enum-backed types (like InputType) are stored as STRINGS
     * in the database and getters return strings directly, NOT enum objects.
     *
     * ❌ WRONG:   $entity->getType()->value   (Error: "Attempt to read property 'value' on string")
     * ✅ CORRECT: $entity->getType()          (Already a string - 'fully_completed')
     *
     * If you need the enum object for business logic, use:
     *    InputType::from($entity->getType())
     */
    protected function entityToArray(object $entity): array
    {
        if (!$entity instanceof Module) {
            throw new \InvalidArgumentException('Expected Module instance');
        }

        return [
            'id' => $entity->getId()->toString(),
            'name' => $entity->getName(),
            'description' => $entity->getDescription(),
            'enabled' => $entity->getEnabled(),
            'version' => $entity->getVersion(),
        ];
    }

    // ====================================
    // INDEX ACTION - List all Modules
    // ====================================

    /**
     * List Modules
     */
    protected function indexAction(): Response
    {
        $this->denyAccessUnlessGranted(ModuleVoter::LIST);

        $preferences = $this->listPreferencesService->getEntityPreferences('modules');
        $savedView = $preferences['view'] ?? 'grid';

        return $this->render('module/index.html.twig', [
            'entities' => [],  // Loaded via API
            'entity_name' => 'module',
            'entity_name_plural' => 'modules',
            'page_icon' => 'bi-grid',
            'default_view' => $savedView,

            // List configuration from GeneratorProperty
            'enable_search' => true,
            'enable_filters' => false,
            'enable_sorting' => true,
            'enable_create_button' => true,
            'create_permission' => ModuleVoter::CREATE,

            // Property metadata for Twig templates (as PHP arrays)
            'listProperties' => json_decode('[{"name":"name","label":"Name","type":"string","sortable":true,"searchable":true,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getName","isRelationship":false},{"name":"description","label":"Description","type":"string","sortable":true,"searchable":true,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getDescription","isRelationship":false},{"name":"enabled","label":"Enabled","type":"boolean","sortable":true,"searchable":false,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getEnabled","isRelationship":false},{"name":"version","label":"Version","type":"string","sortable":true,"searchable":true,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getVersion","isRelationship":false}]', true),
            'searchableFields' => json_decode('[{"name":"name","label":"Name","type":"string"},{"name":"description","label":"Description","type":"string"},{"name":"version","label":"Version","type":"string"}]', true),
            'filterableFields' => json_decode('[]', true),
            'sortableFields' => json_decode('[{"name":"name","label":"Name"},{"name":"description","label":"Description"},{"name":"enabled","label":"Enabled"},{"name":"version","label":"Version"}]', true),

            // Property metadata for client-side rendering (as JSON strings)
            'list_fields' => '[{"name":"name","label":"Name","type":"string","sortable":true,"searchable":true,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getName","isRelationship":false},{"name":"description","label":"Description","type":"string","sortable":true,"searchable":true,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getDescription","isRelationship":false},{"name":"enabled","label":"Enabled","type":"boolean","sortable":true,"searchable":false,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getEnabled","isRelationship":false},{"name":"version","label":"Version","type":"string","sortable":true,"searchable":true,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getVersion","isRelationship":false}]',
            'searchable_fields' => '[{"name":"name","label":"Name","type":"string"},{"name":"description","label":"Description","type":"string"},{"name":"version","label":"Version","type":"string"}]',
            'filterable_fields' => '[]',
            'sortable_fields' => '[{"name":"name","label":"Name"},{"name":"description","label":"Description"},{"name":"enabled","label":"Enabled"},{"name":"version","label":"Version"}]',
        ]);
    }

    // ====================================
    // API SEARCH - Fetch Modules for list view
    // ====================================

    /**
     * API endpoint for searching Modules
     */
    protected function apiSearchAction(Request $request): JsonResponse
    {
        $this->denyAccessUnlessGranted(ModuleVoter::LIST);

        // Delegate to parent BaseApiController which handles
        // search, filtering, sorting, and pagination
        return parent::apiSearchAction($request);
    }

    // ====================================
    // NEW FORM ACTION - Display create form
    // ====================================

    /**
     * Display new module form
     */
    protected function newFormAction(Request $request): Response
    {
        $this->denyAccessUnlessGranted(ModuleVoter::CREATE);

        $module = new Module();

        // Initialize with custom logic if needed
        $this->initializeNewEntity($module);

        $form = $this->createForm(ModuleType::class, $module);

        return $this->render('module/_form_modal.html.twig', [
            'form' => $form,
            'module' => $module,
            'is_edit' => false,
        ]);
    }

    // ====================================
    // CREATE ACTION - Process create form submission
    // ====================================

    /**
     * Process new module form submission
     */
    protected function createAction(Request $request): Response
    {
        $this->denyAccessUnlessGranted(ModuleVoter::CREATE);

        $module = new Module();

        // Initialize with custom logic if needed
        $this->initializeNewEntity($module);

        $form = $this->createForm(ModuleType::class, $module);
        $form->handleRequest($request);

        if ($form->isSubmitted()) {
            if ($form->isValid()) {
                try {
                    // Before create hook
                    $this->beforeCreate($module);

                    $this->entityManager->persist($module);
                    $this->entityManager->flush();

                    // After create hook
                    $this->afterCreate($module);

                    $this->addFlash('success', $this->translator->trans(
                        'module.flash.created_successfully',
                        ['%name%' => (string) $module],
                        'module'
                    ));

                    // If this is a modal/AJAX request (from "+" button), return Turbo Stream with event dispatch
                    // Check both GET and POST for modal parameter
                    if ($request->headers->get('X-Requested-With') === 'turbo-frame' ||
                        $request->get('modal') === '1') {

                        // Get display text for the entity
                        $displayText = (string) $module;

                        $response = $this->render('_entity_created_success_stream.html.twig', [
                            'entityType' => 'Module',
                            'entityId' => $module->getId()->toRfc4122(),
                            'displayText' => $displayText,
                        ]);

                        // Set Turbo Stream content type so Turbo processes it without navigating
                        $response->headers->set('Content-Type', 'text/vnd.turbo-stream.html');

                        return $response;
                    }

                    return $this->redirectToRoute('module_index', [], Response::HTTP_SEE_OTHER);

                } catch (\Exception $e) {
                    $this->addFlash('error', $this->translator->trans(
                        'module.flash.create_failed',
                        ['%error%' => $e->getMessage()],
                        'module'
                    ));
                }
            }
        }

        // Re-render with errors
        return $this->render('module/_form_modal.html.twig', [
            'form' => $form,
            'module' => $module,
            'is_edit' => false,
        ], new Response('', $form->isSubmitted() ? Response::HTTP_UNPROCESSABLE_ENTITY : Response::HTTP_OK));
    }

    // ====================================
    // EDIT FORM ACTION - Display edit form
    // ====================================

    /**
     * Display edit module form
     */
    protected function editFormAction(Module $module, Request $request): Response
    {
        $this->denyAccessUnlessGranted(ModuleVoter::EDIT, $module);

        $form = $this->createForm(ModuleType::class, $module);

        return $this->render('module/_form_modal.html.twig', [
            'form' => $form,
            'module' => $module,
            'is_edit' => true,
        ]);
    }

    // ====================================
    // UPDATE ACTION - Process edit form submission
    // ====================================

    /**
     * Process edit module form submission
     */
    protected function updateAction(Module $module, Request $request): Response
    {
        $this->denyAccessUnlessGranted(ModuleVoter::EDIT, $module);

        $form = $this->createForm(ModuleType::class, $module);
        $form->handleRequest($request);

        if ($form->isSubmitted()) {
            if ($form->isValid()) {
                try {
                    // Before update hook
                    $this->beforeUpdate($module);

                    $this->entityManager->flush();

                    // After update hook
                    $this->afterUpdate($module);

                    $this->addFlash('success', $this->translator->trans(
                        'module.flash.updated_successfully',
                        ['%name%' => (string) $module],
                        'module'
                    ));

                    return $this->redirectToRoute('module_index', [], Response::HTTP_SEE_OTHER);

                } catch (\Exception $e) {
                    $this->addFlash('error', $this->translator->trans(
                        'module.flash.update_failed',
                        ['%error%' => $e->getMessage()],
                        'module'
                    ));
                }
            }
        }

        // Re-render with errors
        return $this->render('module/_form_modal.html.twig', [
            'form' => $form,
            'module' => $module,
            'is_edit' => true,
        ], new Response('', $form->isSubmitted() ? Response::HTTP_UNPROCESSABLE_ENTITY : Response::HTTP_OK));
    }

    // ====================================
    // DELETE ACTION - Delete module
    // ====================================

    /**
     * Delete module
     */
    protected function deleteAction(Module $module, Request $request): Response
    {
        $this->denyAccessUnlessGranted(ModuleVoter::DELETE, $module);

        // Verify CSRF token
        $token = $request->request->get('_token');
        if (!$this->csrfTokenManager->isTokenValid(new CsrfToken('delete_module_' . $module->getId()->toString(), $token))) {
            throw new InvalidCsrfTokenException('Invalid CSRF token for delete operation.');
        }

        try {
            // Before delete hook
            $this->beforeDelete($module);

            $name = (string) $module;

            $this->entityManager->remove($module);
            $this->entityManager->flush();

            // After delete hook
            $this->afterDelete();

            $this->addFlash('success', $this->translator->trans(
                'module.flash.deleted_successfully',
                ['%name%' => $name],
                'module'
            ));

        } catch (\Exception $e) {
            $this->addFlash('error', $this->translator->trans(
                'module.flash.delete_failed',
                ['%error%' => $e->getMessage()],
                'module'
            ));
        }

        return $this->redirectToRoute('module_index', [], Response::HTTP_SEE_OTHER);
    }

    // ====================================
    // SHOW ACTION - Display module details
    // ====================================

    /**
     * Display module details
     */
    protected function showAction(Module $module): Response
    {
        $this->denyAccessUnlessGranted(ModuleVoter::VIEW, $module);

        // Build show properties configuration for view
        $showProperties = $this->buildShowProperties($module);

        return $this->render('module/show.html.twig', [
            'module' => $module,
            'showProperties' => $showProperties,
        ]);
    }

    /**
     * Build show properties configuration
     * Override this method in ModuleController to customize displayed properties
     */
    protected function buildShowProperties(Module $module): array
    {
        return [];
    }

    // ====================================
    // LIFECYCLE HOOKS
    // ====================================
    // Override these in ModuleController to add custom logic

    /**
     * Initialize new entity before creating form
     *
     * Override this method to add custom initialization logic.
     */
    protected function initializeNewEntity(Module $module): void
    {
        // Add your custom initialization here
    }

    /**
     * Execute logic before creating entity
     */
    protected function beforeCreate(Module $module): void
    {
        // Override to add logic before create
    }

    /**
     * Execute logic after creating entity
     */
    protected function afterCreate(Module $module): void
    {
        // Override to add logic after create (e.g., send notification, trigger event)
    }

    /**
     * Execute logic before updating entity
     */
    protected function beforeUpdate(Module $module): void
    {
        // Override to add logic before update
    }

    /**
     * Execute logic after updating entity
     */
    protected function afterUpdate(Module $module): void
    {
        // Override to add logic after update (e.g., send notification, trigger event)
    }

    /**
     * Execute logic before deleting entity
     */
    protected function beforeDelete(Module $module): void
    {
        // Override to add logic before delete (e.g., check dependencies, archive)
    }

    /**
     * Execute logic after deleting entity
     */
    protected function afterDelete(): void
    {
        // Override to add logic after delete (e.g., cleanup, send notification)
    }
}
