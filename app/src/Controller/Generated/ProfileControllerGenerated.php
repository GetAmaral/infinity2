<?php

declare(strict_types=1);

namespace App\Controller\Generated;

use App\Controller\Base\BaseApiController;
use App\Entity\Profile;
use App\Repository\ProfileRepository;
use App\Security\Voter\ProfileVoter;
use App\Form\ProfileType;
use App\Service\ListPreferencesService;
use Doctrine\ORM\EntityManagerInterface;
use Symfony\Component\HttpFoundation\Request;
use Symfony\Component\HttpFoundation\Response;
use Symfony\Component\HttpFoundation\JsonResponse;
use Symfony\Component\Security\Csrf\CsrfToken;
use Symfony\Component\Security\Csrf\CsrfTokenManagerInterface;
use Symfony\Component\Security\Core\Exception\InvalidCsrfTokenException;
use Symfony\Contracts\Translation\TranslatorInterface;

/**
 * Generated Base Controller for Profile
 *
 * ⚠️ WARNING: This file is ALWAYS regenerated by Genmax
 * DO NOT EDIT THIS FILE - Edit ProfileController instead
 *
 * @generated
 * @codeCoverageIgnore
 */
abstract class ProfileControllerGenerated extends BaseApiController
{
    public function __construct(
        protected readonly EntityManagerInterface $entityManager,
        protected readonly ProfileRepository $repository,
        protected readonly ListPreferencesService $listPreferencesService,
        protected readonly TranslatorInterface $translator,
        protected readonly CsrfTokenManagerInterface $csrfTokenManager,
    ) {}

    // ====================================
    // ABSTRACT METHOD IMPLEMENTATIONS
    // ====================================

    /**
     * Get the repository for this entity
     */
    protected function getRepository(): ProfileRepository
    {
        return $this->repository;
    }

    /**
     * Get the plural name for this entity
     */
    protected function getEntityPluralName(): string
    {
        return 'profiles';
    }

    /**
     * Convert entity to array for API responses
     *
     * ⚠️ IMPORTANT: Enum Properties Stored as Strings
     * -------------------------------------------------
     * Properties with enum-backed types (like InputType) are stored as STRINGS
     * in the database and getters return strings directly, NOT enum objects.
     *
     * ❌ WRONG:   $entity->getType()->value   (Error: "Attempt to read property 'value' on string")
     * ✅ CORRECT: $entity->getType()          (Already a string - 'fully_completed')
     *
     * If you need the enum object for business logic, use:
     *    InputType::from($entity->getType())
     */
    protected function entityToArray(object $entity): array
    {
        if (!$entity instanceof Profile) {
            throw new \InvalidArgumentException('Expected Profile instance');
        }

        return [
            'id' => $entity->getId()->toString(),
            'organization' => ($organizationRel = $entity->getOrganization()) ? [
                'id' => $organizationRel->getId()->toString(),
                'display' => (string) $organizationRel,
            ] : null,
            'name' => $entity->getName(),
            'description' => $entity->getDescription(),
            'grantedRoles' => ($grantedRolesRel = $entity->getGrantedRoles()) ? array_map(
                fn($item) => [
                    'id' => $item->getId()->toString(),
                    'display' => (string) $item,
                ],
                $grantedRolesRel->toArray()
            ) : [],
            'users' => ($usersRel = $entity->getUsers()) ? array_map(
                fn($item) => [
                    'id' => $item->getId()->toString(),
                    'display' => (string) $item,
                ],
                $usersRel->toArray()
            ) : [],
        ];
    }

    // ====================================
    // INDEX ACTION - List all Profiles
    // ====================================

    /**
     * List Profiles
     */
    protected function indexAction(): Response
    {
        $this->denyAccessUnlessGranted(ProfileVoter::LIST);

        $preferences = $this->listPreferencesService->getEntityPreferences('profiles');
        $savedView = $preferences['view'] ?? 'grid';

        return $this->render('profile/index.html.twig', [
            'entities' => [],  // Loaded via API
            'entity_name' => 'profile',
            'entity_name_plural' => 'profiles',
            'page_icon' => 'bi-person-gear',
            'default_view' => $savedView,

            // List configuration from GeneratorProperty
            'enable_search' => true,
            'enable_filters' => false,
            'enable_sorting' => true,
            'enable_create_button' => true,
            'create_permission' => ProfileVoter::CREATE,

            // Property metadata for Twig templates (as PHP arrays)
            'listProperties' => json_decode('[{"name":"name","label":"Name","type":"string","sortable":true,"searchable":true,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getName","isRelationship":false},{"name":"description","label":"Description","type":"string","sortable":true,"searchable":true,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getDescription","isRelationship":false},{"name":"grantedRoles","label":"GrantedRoles","type":"","sortable":true,"searchable":false,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getGrantedRoles","isRelationship":true},{"name":"users","label":"Users","type":"","sortable":true,"searchable":false,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getUsers","isRelationship":true}]', true),
            'searchableFields' => json_decode('[{"name":"name","label":"Name","type":"string"},{"name":"description","label":"Description","type":"string"}]', true),
            'filterableFields' => json_decode('[]', true),
            'sortableFields' => json_decode('[{"name":"name","label":"Name"},{"name":"description","label":"Description"},{"name":"grantedRoles","label":"GrantedRoles"},{"name":"users","label":"Users"}]', true),

            // Property metadata for client-side rendering (as JSON strings)
            'list_fields' => '[{"name":"name","label":"Name","type":"string","sortable":true,"searchable":true,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getName","isRelationship":false},{"name":"description","label":"Description","type":"string","sortable":true,"searchable":true,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getDescription","isRelationship":false},{"name":"grantedRoles","label":"GrantedRoles","type":"","sortable":true,"searchable":false,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getGrantedRoles","isRelationship":true},{"name":"users","label":"Users","type":"","sortable":true,"searchable":false,"filterable":false,"filterStrategy":null,"filterBoolean":false,"filterDate":false,"filterNumericRange":false,"filterExists":false,"getter":"getUsers","isRelationship":true}]',
            'searchable_fields' => '[{"name":"name","label":"Name","type":"string"},{"name":"description","label":"Description","type":"string"}]',
            'filterable_fields' => '[]',
            'sortable_fields' => '[{"name":"name","label":"Name"},{"name":"description","label":"Description"},{"name":"grantedRoles","label":"GrantedRoles"},{"name":"users","label":"Users"}]',
        ]);
    }

    // ====================================
    // API SEARCH - Fetch Profiles for list view
    // ====================================

    /**
     * API endpoint for searching Profiles
     */
    protected function apiSearchAction(Request $request): JsonResponse
    {
        $this->denyAccessUnlessGranted(ProfileVoter::LIST);

        // Delegate to parent BaseApiController which handles
        // search, filtering, sorting, and pagination
        return parent::apiSearchAction($request);
    }

    // ====================================
    // NEW FORM ACTION - Display create form
    // ====================================

    /**
     * Display new profile form
     */
    protected function newFormAction(Request $request): Response
    {
        $this->denyAccessUnlessGranted(ProfileVoter::CREATE);

        $profile = new Profile();

        // Initialize with custom logic if needed
        $this->initializeNewEntity($profile);

        $form = $this->createForm(ProfileType::class, $profile);

        return $this->render('profile/_form_modal.html.twig', [
            'form' => $form,
            'profile' => $profile,
            'is_edit' => false,
        ]);
    }

    // ====================================
    // CREATE ACTION - Process create form submission
    // ====================================

    /**
     * Process new profile form submission
     */
    protected function createAction(Request $request): Response
    {
        $this->denyAccessUnlessGranted(ProfileVoter::CREATE);

        $profile = new Profile();

        // Initialize with custom logic if needed
        $this->initializeNewEntity($profile);

        $form = $this->createForm(ProfileType::class, $profile);
        $form->handleRequest($request);

        if ($form->isSubmitted() && $form->isValid()) {
            try {
                // Before create hook
                $this->beforeCreate($profile);

                $this->entityManager->persist($profile);
                $this->entityManager->flush();

                // After create hook
                $this->afterCreate($profile);

                $this->addFlash('success', $this->translator->trans(
                    'profile.flash.created_successfully',
                    ['%name%' => (string) $profile],
                    'profile'
                ));

                return $this->redirectToRoute('profile_index', [], Response::HTTP_SEE_OTHER);

            } catch (\Exception $e) {
                $this->addFlash('error', $this->translator->trans(
                    'profile.flash.create_failed',
                    ['%error%' => $e->getMessage()],
                    'profile'
                ));
            }
        }

        // Re-render with errors
        return $this->render('profile/_form_modal.html.twig', [
            'form' => $form,
            'profile' => $profile,
            'is_edit' => false,
        ], new Response('', $form->isSubmitted() ? Response::HTTP_UNPROCESSABLE_ENTITY : Response::HTTP_OK));
    }

    // ====================================
    // EDIT FORM ACTION - Display edit form
    // ====================================

    /**
     * Display edit profile form
     */
    protected function editFormAction(Profile $profile, Request $request): Response
    {
        $this->denyAccessUnlessGranted(ProfileVoter::EDIT, $profile);

        $form = $this->createForm(ProfileType::class, $profile);

        return $this->render('profile/_form_modal.html.twig', [
            'form' => $form,
            'profile' => $profile,
            'is_edit' => true,
        ]);
    }

    // ====================================
    // UPDATE ACTION - Process edit form submission
    // ====================================

    /**
     * Process edit profile form submission
     */
    protected function updateAction(Profile $profile, Request $request): Response
    {
        $this->denyAccessUnlessGranted(ProfileVoter::EDIT, $profile);

        $form = $this->createForm(ProfileType::class, $profile);
        $form->handleRequest($request);

        if ($form->isSubmitted() && $form->isValid()) {
            try {
                // Before update hook
                $this->beforeUpdate($profile);

                $this->entityManager->flush();

                // After update hook
                $this->afterUpdate($profile);

                $this->addFlash('success', $this->translator->trans(
                    'profile.flash.updated_successfully',
                    ['%name%' => (string) $profile],
                    'profile'
                ));

                return $this->redirectToRoute('profile_index', [], Response::HTTP_SEE_OTHER);

            } catch (\Exception $e) {
                $this->addFlash('error', $this->translator->trans(
                    'profile.flash.update_failed',
                    ['%error%' => $e->getMessage()],
                    'profile'
                ));
            }
        }

        // Re-render with errors
        return $this->render('profile/_form_modal.html.twig', [
            'form' => $form,
            'profile' => $profile,
            'is_edit' => true,
        ], new Response('', $form->isSubmitted() ? Response::HTTP_UNPROCESSABLE_ENTITY : Response::HTTP_OK));
    }

    // ====================================
    // DELETE ACTION - Delete profile
    // ====================================

    /**
     * Delete profile
     */
    protected function deleteAction(Profile $profile, Request $request): Response
    {
        $this->denyAccessUnlessGranted(ProfileVoter::DELETE, $profile);

        // Verify CSRF token
        $token = $request->request->get('_token');
        if (!$this->csrfTokenManager->isTokenValid(new CsrfToken('delete_profile_' . $profile->getId()->toString(), $token))) {
            throw new InvalidCsrfTokenException('Invalid CSRF token for delete operation.');
        }

        try {
            // Before delete hook
            $this->beforeDelete($profile);

            $name = (string) $profile;

            $this->entityManager->remove($profile);
            $this->entityManager->flush();

            // After delete hook
            $this->afterDelete();

            $this->addFlash('success', $this->translator->trans(
                'profile.flash.deleted_successfully',
                ['%name%' => $name],
                'profile'
            ));

        } catch (\Exception $e) {
            $this->addFlash('error', $this->translator->trans(
                'profile.flash.delete_failed',
                ['%error%' => $e->getMessage()],
                'profile'
            ));
        }

        return $this->redirectToRoute('profile_index', [], Response::HTTP_SEE_OTHER);
    }

    // ====================================
    // SHOW ACTION - Display profile details
    // ====================================

    /**
     * Display profile details
     */
    protected function showAction(Profile $profile): Response
    {
        $this->denyAccessUnlessGranted(ProfileVoter::VIEW, $profile);

        return $this->render('profile/show.html.twig', [
            'profile' => $profile,
        ]);
    }

    // ====================================
    // LIFECYCLE HOOKS
    // ====================================
    // Override these in ProfileController to add custom logic

    /**
     * Initialize new entity before creating form
     *
     * Note: Organization and Owner are set automatically by TenantEntityProcessor
     * Only use this for custom initialization logic
     */
    protected function initializeNewEntity(Profile $profile): void
    {
        // Organization and Owner are set automatically by TenantEntityProcessor
        // Add your custom initialization here
    }

    /**
     * Execute logic before creating entity
     */
    protected function beforeCreate(Profile $profile): void
    {
        // Override to add logic before create
    }

    /**
     * Execute logic after creating entity
     */
    protected function afterCreate(Profile $profile): void
    {
        // Override to add logic after create (e.g., send notification, trigger event)
    }

    /**
     * Execute logic before updating entity
     */
    protected function beforeUpdate(Profile $profile): void
    {
        // Override to add logic before update
    }

    /**
     * Execute logic after updating entity
     */
    protected function afterUpdate(Profile $profile): void
    {
        // Override to add logic after update (e.g., send notification, trigger event)
    }

    /**
     * Execute logic before deleting entity
     */
    protected function beforeDelete(Profile $profile): void
    {
        // Override to add logic before delete (e.g., check dependencies, archive)
    }

    /**
     * Execute logic after deleting entity
     */
    protected function afterDelete(): void
    {
        // Override to add logic after delete (e.g., cleanup, send notification)
    }
}
