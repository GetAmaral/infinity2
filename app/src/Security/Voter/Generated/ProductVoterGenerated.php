<?php

declare(strict_types=1);

namespace App\Security\Voter\Generated;

use App\Entity\Product;
use App\Entity\User;
use Symfony\Component\Security\Core\Authentication\Token\TokenInterface;
use Symfony\Component\Security\Core\Authorization\Voter\Voter;
use Symfony\Component\Security\Core\Role\RoleHierarchyInterface;

/**
 * Product Voter (Generated Base Class)
 *
 * This class is ALWAYS regenerated. DO NOT edit this file.
 * Add custom authorization logic to ProductVoter.php instead.
 *
 * @generated by Genmax Code Generator
 */
abstract class ProductVoterGenerated extends Voter
{
    public function __construct(
        protected readonly RoleHierarchyInterface $roleHierarchy
    ) {}

    // Permission constants
    public const VIEW = 'PRODUCT_VIEW';
    public const EDIT = 'PRODUCT_EDIT';
    public const DELETE = 'PRODUCT_DELETE';
    public const CREATE = 'PRODUCT_CREATE';
    public const MANAGE_INVENTORY = 'PRODUCT_MANAGE_INVENTORY';
    public const MANAGE_PRICING = 'PRODUCT_MANAGE_PRICING';

    /**
     * Check if user has a specific role (respects role hierarchy)
     */
    protected function hasRole(User $user, string $role): bool
    {
        $userRoles = $this->roleHierarchy->getReachableRoleNames($user->getRoles());
        return in_array($role, $userRoles, true);
    }

    protected function supports(string $attribute, mixed $subject): bool
    {
        // Check if this is a supported permission
        if (!in_array($attribute, [
            self::VIEW,
            self::EDIT,
            self::DELETE,
            self::CREATE,
            self::MANAGE_INVENTORY,
            self::MANAGE_PRICING,
        ], true)) {
            return false;
        }

        // For class-based permissions (LIST, CREATE), subject can be null
        if (in_array($attribute, [
            self::CREATE,
        ], true)) {
            return true;
        }

        // For instance-based permissions, subject must be a Product
        return $subject instanceof Product;
    }

    protected function voteOnAttribute(string $attribute, mixed $subject, TokenInterface $token): bool
    {
        $user = $token->getUser();

        // User must be logged in
        if (!$user instanceof User) {
            return false;
        }

        /** @var Product|null $product */
        $product = $subject;

        return match ($attribute) {
            self::VIEW => $this->canVIEW($product, $user),
            self::EDIT => $this->canEDIT($product, $user),
            self::DELETE => $this->canDELETE($product, $user),
            self::CREATE => $this->canCREATE($user),
            self::MANAGE_INVENTORY => $this->canMANAGE_INVENTORY($product, $user),
            self::MANAGE_PRICING => $this->canMANAGE_PRICING($product, $user),
            default => false,
        };
    }

    /**
     * Check if user can view Product(s)     */
    protected function canVIEW(?Product $product, User $user): bool
    {
        if (!$product) {
            return false;
        }

        // ADMIN and SUPER_ADMIN can do anything
        if ($this->hasRole($user, 'ROLE_ADMIN')
            || $this->hasRole($user, 'ROLE_SUPER_ADMIN')) {
            return true;
        }

        // Must be in same organization
        $sameOrganization = $user->getOrganization()
            && $product->getOrganization()
            && $user->getOrganization()->getId()->equals($product->getOrganization()->getId());

        if (!$sameOrganization) {
            return false;
        }

        // All authenticated users in same organization can view
        return true;
    }

    /**
     * Check if user can edit Product(s)     */
    protected function canEDIT(?Product $product, User $user): bool
    {
        if (!$product) {
            return false;
        }

        // ADMIN and SUPER_ADMIN can do anything
        if ($this->hasRole($user, 'ROLE_ADMIN')
            || $this->hasRole($user, 'ROLE_SUPER_ADMIN')) {
            return true;
        }

        // Must be in same organization
        $sameOrganization = $user->getOrganization()
            && $product->getOrganization()
            && $user->getOrganization()->getId()->equals($product->getOrganization()->getId());

        if (!$sameOrganization) {
            return false;
        }

        // ORGANIZATION_ADMIN can edit within their organization
        if ($this->hasRole($user, 'ROLE_ORGANIZATION_ADMIN')) {
            return true;
        }

        // Regular users can edit (customize as needed)
        return true;
    }

    /**
     * Check if user can delete Product(s)     */
    protected function canDELETE(?Product $product, User $user): bool
    {
        if (!$product) {
            return false;
        }

        // ADMIN and SUPER_ADMIN can do anything
        if ($this->hasRole($user, 'ROLE_ADMIN')
            || $this->hasRole($user, 'ROLE_SUPER_ADMIN')) {
            return true;
        }

        // Must be in same organization
        $sameOrganization = $user->getOrganization()
            && $product->getOrganization()
            && $user->getOrganization()->getId()->equals($product->getOrganization()->getId());

        if (!$sameOrganization) {
            return false;
        }

        // Only ORGANIZATION_ADMIN can delete
        return $this->hasRole($user, 'ROLE_ORGANIZATION_ADMIN');
    }

    /**
     * Check if user can create Product     */
    protected function canCREATE(User $user): bool
    {
        // ADMIN and SUPER_ADMIN can do anything
        if ($this->hasRole($user, 'ROLE_ADMIN')
            || $this->hasRole($user, 'ROLE_SUPER_ADMIN')) {
            return true;
        }

        // ORGANIZATION_ADMIN can create
        return $this->hasRole($user, 'ROLE_ORGANIZATION_ADMIN');
    }

    /**
     * Check if user can manage_inventory Product(s)     */
    protected function canMANAGE_INVENTORY(?Product $product, User $user): bool
    {
        if (!$product) {
            return false;
        }

        // ADMIN and SUPER_ADMIN can do anything
        if ($this->hasRole($user, 'ROLE_ADMIN')
            || $this->hasRole($user, 'ROLE_SUPER_ADMIN')) {
            return true;
        }

        // Must be in same organization
        $sameOrganization = $user->getOrganization()
            && $product->getOrganization()
            && $user->getOrganization()->getId()->equals($product->getOrganization()->getId());

        if (!$sameOrganization) {
            return false;
        }

        // Default: authenticated users in same organization
        return true;
    }

    /**
     * Check if user can manage_pricing Product(s)     */
    protected function canMANAGE_PRICING(?Product $product, User $user): bool
    {
        if (!$product) {
            return false;
        }

        // ADMIN and SUPER_ADMIN can do anything
        if ($this->hasRole($user, 'ROLE_ADMIN')
            || $this->hasRole($user, 'ROLE_SUPER_ADMIN')) {
            return true;
        }

        // Must be in same organization
        $sameOrganization = $user->getOrganization()
            && $product->getOrganization()
            && $user->getOrganization()->getId()->equals($product->getOrganization()->getId());

        if (!$sameOrganization) {
            return false;
        }

        // Default: authenticated users in same organization
        return true;
    }

}
