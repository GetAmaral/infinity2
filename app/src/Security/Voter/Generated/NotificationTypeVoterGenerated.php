<?php

declare(strict_types=1);

namespace App\Security\Voter\Generated;

use App\Entity\NotificationType;
use App\Entity\User;
use Symfony\Component\Security\Core\Authentication\Token\TokenInterface;
use Symfony\Component\Security\Core\Authorization\Voter\Voter;
use Symfony\Component\Security\Core\Role\RoleHierarchyInterface;

/**
 * NotificationType Voter (Generated Base Class)
 *
 * This class is ALWAYS regenerated. DO NOT edit this file.
 * Add custom authorization logic to NotificationTypeVoter.php instead.
 *
 * @generated by Genmax Code Generator
 */
abstract class NotificationTypeVoterGenerated extends Voter
{
    public function __construct(
        protected readonly RoleHierarchyInterface $roleHierarchy
    ) {}

    // Permission constants
    public const LIST = 'NOTIFICATION_TYPE_LIST';
    public const CREATE = 'NOTIFICATION_TYPE_CREATE';
    public const VIEW = 'NOTIFICATION_TYPE_VIEW';
    public const EDIT = 'NOTIFICATION_TYPE_EDIT';
    public const DELETE = 'NOTIFICATION_TYPE_DELETE';

    /**
     * Check if user has a specific role (respects role hierarchy)
     */
    protected function hasRole(User $user, string $role): bool
    {
        $userRoles = $this->roleHierarchy->getReachableRoleNames($user->getRoles());
        return in_array($role, $userRoles, true);
    }

    protected function supports(string $attribute, mixed $subject): bool
    {
        // Check if this is a supported permission
        if (!in_array($attribute, [
            self::LIST,
            self::CREATE,
            self::VIEW,
            self::EDIT,
            self::DELETE,
        ], true)) {
            return false;
        }

        // For class-based permissions (LIST, CREATE), subject can be null
        if (in_array($attribute, [
            self::LIST,
            self::CREATE,
        ], true)) {
            return true;
        }

        // For instance-based permissions, subject must be a NotificationType
        return $subject instanceof NotificationType;
    }

    protected function voteOnAttribute(string $attribute, mixed $subject, TokenInterface $token): bool
    {
        $user = $token->getUser();

        // User must be logged in
        if (!$user instanceof User) {
            return false;
        }

        /** @var NotificationType|null $notificationType */
        $notificationType = $subject;

        return match ($attribute) {
            self::LIST => $this->canLIST($user),
            self::CREATE => $this->canCREATE($user),
            self::VIEW => $this->canVIEW($notificationType, $user),
            self::EDIT => $this->canEDIT($notificationType, $user),
            self::DELETE => $this->canDELETE($notificationType, $user),
            default => false,
        };
    }

    /**
     * Check if user can list NotificationType     */
    protected function canLIST(User $user): bool
    {
        // ADMIN and SUPER_ADMIN can do anything
        if ($this->hasRole($user, 'ROLE_ADMIN')
            || $this->hasRole($user, 'ROLE_SUPER_ADMIN')) {
            return true;
        }

        // ORGANIZATION_ADMIN can list
        return $this->hasRole($user, 'ROLE_ORGANIZATION_ADMIN');
    }

    /**
     * Check if user can create NotificationType     */
    protected function canCREATE(User $user): bool
    {
        // ADMIN and SUPER_ADMIN can do anything
        if ($this->hasRole($user, 'ROLE_ADMIN')
            || $this->hasRole($user, 'ROLE_SUPER_ADMIN')) {
            return true;
        }

        // ORGANIZATION_ADMIN can create
        return $this->hasRole($user, 'ROLE_ORGANIZATION_ADMIN');
    }

    /**
     * Check if user can view NotificationType(s)     */
    protected function canVIEW(?NotificationType $notificationType, User $user): bool
    {
        if (!$notificationType) {
            return false;
        }

        // ADMIN and SUPER_ADMIN can do anything
        if ($this->hasRole($user, 'ROLE_ADMIN')
            || $this->hasRole($user, 'ROLE_SUPER_ADMIN')) {
            return true;
        }

        // Must be in same organization
        $sameOrganization = $user->getOrganization()
            && $notificationType->getOrganization()
            && $user->getOrganization()->getId()->equals($notificationType->getOrganization()->getId());

        if (!$sameOrganization) {
            return false;
        }

        // All authenticated users in same organization can view
        return true;
    }

    /**
     * Check if user can edit NotificationType(s)     */
    protected function canEDIT(?NotificationType $notificationType, User $user): bool
    {
        if (!$notificationType) {
            return false;
        }

        // ADMIN and SUPER_ADMIN can do anything
        if ($this->hasRole($user, 'ROLE_ADMIN')
            || $this->hasRole($user, 'ROLE_SUPER_ADMIN')) {
            return true;
        }

        // Must be in same organization
        $sameOrganization = $user->getOrganization()
            && $notificationType->getOrganization()
            && $user->getOrganization()->getId()->equals($notificationType->getOrganization()->getId());

        if (!$sameOrganization) {
            return false;
        }

        // ORGANIZATION_ADMIN can edit within their organization
        if ($this->hasRole($user, 'ROLE_ORGANIZATION_ADMIN')) {
            return true;
        }

        // Regular users can edit (customize as needed)
        return true;
    }

    /**
     * Check if user can delete NotificationType(s)     */
    protected function canDELETE(?NotificationType $notificationType, User $user): bool
    {
        if (!$notificationType) {
            return false;
        }

        // ADMIN and SUPER_ADMIN can do anything
        if ($this->hasRole($user, 'ROLE_ADMIN')
            || $this->hasRole($user, 'ROLE_SUPER_ADMIN')) {
            return true;
        }

        // Must be in same organization
        $sameOrganization = $user->getOrganization()
            && $notificationType->getOrganization()
            && $user->getOrganization()->getId()->equals($notificationType->getOrganization()->getId());

        if (!$sameOrganization) {
            return false;
        }

        // Only ORGANIZATION_ADMIN can delete
        return $this->hasRole($user, 'ROLE_ORGANIZATION_ADMIN');
    }

}
