<?php

declare(strict_types=1);

namespace App\Security\Voter\Generated;

use App\Entity\WinReason;
use App\Entity\User;
use Symfony\Component\Security\Core\Authentication\Token\TokenInterface;
use Symfony\Component\Security\Core\Authorization\Voter\Voter;

/**
 * WinReason Voter (Generated Base Class)
 *
 * This class is ALWAYS regenerated from CSV. DO NOT edit this file.
 * Add custom authorization logic to WinReasonVoter.php instead.
 *
 * @generated by Luminai Code Generator
 */
abstract class WinReasonVoterGenerated extends Voter
{
    public const VIEW = 'VIEW';
    public const EDIT = 'EDIT';
    public const DELETE = 'DELETE';

    protected function supports(string $attribute, mixed $subject): bool
    {
        // Only vote on WinReason objects with supported attributes
        if (!in_array($attribute, ['VIEW', 'EDIT', 'DELETE'], true)) {
            return false;
        }

        if (!$subject instanceof WinReason) {
            return false;
        }

        return true;
    }

    protected function voteOnAttribute(string $attribute, mixed $subject, TokenInterface $token): bool
    {
        $user = $token->getUser();

        // User must be logged in
        if (!$user instanceof User) {
            return false;
        }

        /** @var WinReason $winreason */
        $winreason = $subject;

        return match ($attribute) {
            self::VIEW => $this->canView($winreason, $user),
            self::EDIT => $this->canEdit($winreason, $user),
            self::DELETE => $this->canDelete($winreason, $user),
            default => false,
        };
    }

    /**
     * Check if user can view the WinReason
     */
    protected function canView(WinReason $winreason, User $user): bool
    {
        // Check if user belongs to the same organization
        if ($winreason->getOrganization()->getId() !== $user->getOrganization()->getId()) {
            return false;
        }

        // Admins and super admins can do anything
        if (in_array('ROLE_ADMIN', $user->getRoles(), true) || in_array('ROLE_SUPER_ADMIN', $user->getRoles(), true)) {
            return true;
        }

        // All authenticated users can view
        return true;
    }

    /**
     * Check if user can edit the WinReason
     */
    protected function canEdit(WinReason $winreason, User $user): bool
    {
        // Check if user belongs to the same organization
        if ($winreason->getOrganization()->getId() !== $user->getOrganization()->getId()) {
            return false;
        }

        // Admins and super admins can do anything
        if (in_array('ROLE_ADMIN', $user->getRoles(), true) || in_array('ROLE_SUPER_ADMIN', $user->getRoles(), true)) {
            return true;
        }

        // Users can edit their own organization's entities
        return true;
    }

    /**
     * Check if user can delete the WinReason
     */
    protected function canDelete(WinReason $winreason, User $user): bool
    {
        // Check if user belongs to the same organization
        if ($winreason->getOrganization()->getId() !== $user->getOrganization()->getId()) {
            return false;
        }

        // Admins and super admins can do anything
        if (in_array('ROLE_ADMIN', $user->getRoles(), true) || in_array('ROLE_SUPER_ADMIN', $user->getRoles(), true)) {
            return true;
        }

        // Only admins can delete
        return false;
    }

}
