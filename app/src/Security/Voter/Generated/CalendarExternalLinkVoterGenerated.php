<?php

declare(strict_types=1);

namespace App\Security\Voter\Generated;

use App\Entity\CalendarExternalLink;
use App\Entity\User;
use Symfony\Component\Security\Core\Authentication\Token\TokenInterface;
use Symfony\Component\Security\Core\Authorization\Voter\Voter;

/**
 * CalendarExternalLink Voter (Generated Base Class)
 *
 * This class is ALWAYS regenerated from CSV. DO NOT edit this file.
 * Add custom authorization logic to CalendarExternalLinkVoter.php instead.
 *
 * @generated by Luminai Code Generator
 */
abstract class CalendarExternalLinkVoterGenerated extends Voter
{
    public const VIEW = 'VIEW';
    public const EDIT = 'EDIT';
    public const DELETE = 'DELETE';

    protected function supports(string $attribute, mixed $subject): bool
    {
        // Only vote on CalendarExternalLink objects with supported attributes
        if (!in_array($attribute, ['VIEW', 'EDIT', 'DELETE'], true)) {
            return false;
        }

        if (!$subject instanceof CalendarExternalLink) {
            return false;
        }

        return true;
    }

    protected function voteOnAttribute(string $attribute, mixed $subject, TokenInterface $token): bool
    {
        $user = $token->getUser();

        // User must be logged in
        if (!$user instanceof User) {
            return false;
        }

        /** @var CalendarExternalLink $calendarexternallink */
        $calendarexternallink = $subject;

        return match ($attribute) {
            self::VIEW => $this->canView($calendarexternallink, $user),
            self::EDIT => $this->canEdit($calendarexternallink, $user),
            self::DELETE => $this->canDelete($calendarexternallink, $user),
            default => false,
        };
    }

    /**
     * Check if user can view the CalendarExternalLink
     */
    protected function canView(CalendarExternalLink $calendarexternallink, User $user): bool
    {
        // Admins and super admins can do anything
        if (in_array('ROLE_ADMIN', $user->getRoles(), true) || in_array('ROLE_SUPER_ADMIN', $user->getRoles(), true)) {
            return true;
        }

        // All authenticated users can view
        return true;
    }

    /**
     * Check if user can edit the CalendarExternalLink
     */
    protected function canEdit(CalendarExternalLink $calendarexternallink, User $user): bool
    {
        // Admins and super admins can do anything
        if (in_array('ROLE_ADMIN', $user->getRoles(), true) || in_array('ROLE_SUPER_ADMIN', $user->getRoles(), true)) {
            return true;
        }

        // Users can edit their own organization's entities
        return true;
    }

    /**
     * Check if user can delete the CalendarExternalLink
     */
    protected function canDelete(CalendarExternalLink $calendarexternallink, User $user): bool
    {
        // Admins and super admins can do anything
        if (in_array('ROLE_ADMIN', $user->getRoles(), true) || in_array('ROLE_SUPER_ADMIN', $user->getRoles(), true)) {
            return true;
        }

        // Only admins can delete
        return false;
    }

}
