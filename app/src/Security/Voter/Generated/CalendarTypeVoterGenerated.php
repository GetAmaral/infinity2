<?php

declare(strict_types=1);

namespace App\Security\Voter\Generated;

use App\Entity\CalendarType;
use App\Entity\User;
use Symfony\Component\Security\Core\Authentication\Token\TokenInterface;
use Symfony\Component\Security\Core\Authorization\Voter\Voter;

/**
 * CalendarType Voter (Generated Base Class)
 *
 * This class is ALWAYS regenerated from CSV. DO NOT edit this file.
 * Add custom authorization logic to CalendarTypeVoter.php instead.
 *
 * @generated by Luminai Code Generator
 */
abstract class CalendarTypeVoterGenerated extends Voter
{
    public const VIEW = 'VIEW';
    public const EDIT = 'EDIT';
    public const DELETE = 'DELETE';

    protected function supports(string $attribute, mixed $subject): bool
    {
        // Only vote on CalendarType objects with supported attributes
        if (!in_array($attribute, ['VIEW', 'EDIT', 'DELETE'], true)) {
            return false;
        }

        if (!$subject instanceof CalendarType) {
            return false;
        }

        return true;
    }

    protected function voteOnAttribute(string $attribute, mixed $subject, TokenInterface $token): bool
    {
        $user = $token->getUser();

        // User must be logged in
        if (!$user instanceof User) {
            return false;
        }

        /** @var CalendarType $calendartype */
        $calendartype = $subject;

        return match ($attribute) {
            self::VIEW => $this->canView($calendartype, $user),
            self::EDIT => $this->canEdit($calendartype, $user),
            self::DELETE => $this->canDelete($calendartype, $user),
            default => false,
        };
    }

    /**
     * Check if user can view the CalendarType
     */
    protected function canView(CalendarType $calendartype, User $user): bool
    {
        // Admins and super admins can do anything
        if (in_array('ROLE_ADMIN', $user->getRoles(), true) || in_array('ROLE_SUPER_ADMIN', $user->getRoles(), true)) {
            return true;
        }

        // All authenticated users can view
        return true;
    }

    /**
     * Check if user can edit the CalendarType
     */
    protected function canEdit(CalendarType $calendartype, User $user): bool
    {
        // Admins and super admins can do anything
        if (in_array('ROLE_ADMIN', $user->getRoles(), true) || in_array('ROLE_SUPER_ADMIN', $user->getRoles(), true)) {
            return true;
        }

        // Users can edit their own organization's entities
        return true;
    }

    /**
     * Check if user can delete the CalendarType
     */
    protected function canDelete(CalendarType $calendartype, User $user): bool
    {
        // Admins and super admins can do anything
        if (in_array('ROLE_ADMIN', $user->getRoles(), true) || in_array('ROLE_SUPER_ADMIN', $user->getRoles(), true)) {
            return true;
        }

        // Only admins can delete
        return false;
    }

}
