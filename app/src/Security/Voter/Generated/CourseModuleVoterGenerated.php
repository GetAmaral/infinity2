<?php

declare(strict_types=1);

namespace App\Security\Voter\Generated;

use App\Entity\CourseModule;
use App\Entity\User;
use Symfony\Component\Security\Core\Authentication\Token\TokenInterface;
use Symfony\Component\Security\Core\Authorization\Voter\Voter;
use Symfony\Component\Security\Core\Role\RoleHierarchyInterface;

/**
 * CourseModule Voter (Generated Base Class)
 *
 * This class is ALWAYS regenerated. DO NOT edit this file.
 * Add custom authorization logic to CourseModuleVoter.php instead.
 *
 * @generated by Genmax Code Generator
 */
abstract class CourseModuleVoterGenerated extends Voter
{
    public function __construct(
        protected readonly RoleHierarchyInterface $roleHierarchy
    ) {}

    // Permission constants
    public const VIEW = 'COURSE_MODULE_VIEW';
    public const EDIT = 'COURSE_MODULE_EDIT';
    public const DELETE = 'COURSE_MODULE_DELETE';

    /**
     * Check if user has a specific role (respects role hierarchy)
     */
    protected function hasRole(User $user, string $role): bool
    {
        $userRoles = $this->roleHierarchy->getReachableRoleNames($user->getRoles());
        return in_array($role, $userRoles, true);
    }

    protected function supports(string $attribute, mixed $subject): bool
    {
        // Check if this is a supported permission
        if (!in_array($attribute, [
            self::VIEW,
            self::EDIT,
            self::DELETE,
        ], true)) {
            return false;
        }

        // For class-based permissions (LIST, CREATE), subject can be null
        if (in_array($attribute, [
        ], true)) {
            return true;
        }

        // For instance-based permissions, subject must be a CourseModule
        return $subject instanceof CourseModule;
    }

    protected function voteOnAttribute(string $attribute, mixed $subject, TokenInterface $token): bool
    {
        $user = $token->getUser();

        // User must be logged in
        if (!$user instanceof User) {
            return false;
        }

        /** @var CourseModule|null $courseModule */
        $courseModule = $subject;

        return match ($attribute) {
            self::VIEW => $this->canVIEW($courseModule, $user),
            self::EDIT => $this->canEDIT($courseModule, $user),
            self::DELETE => $this->canDELETE($courseModule, $user),
            default => false,
        };
    }

    /**
     * Check if user can view CourseModule(s)     */
    protected function canVIEW(?CourseModule $courseModule, User $user): bool
    {
        if (!$courseModule) {
            return false;
        }

        // ADMIN and SUPER_ADMIN can do anything
        if ($this->hasRole($user, 'ROLE_ADMIN')
            || $this->hasRole($user, 'ROLE_SUPER_ADMIN')) {
            return true;
        }

        // All authenticated users in same organization can view
        return true;
    }

    /**
     * Check if user can edit CourseModule(s)     */
    protected function canEDIT(?CourseModule $courseModule, User $user): bool
    {
        if (!$courseModule) {
            return false;
        }

        // ADMIN and SUPER_ADMIN can do anything
        if ($this->hasRole($user, 'ROLE_ADMIN')
            || $this->hasRole($user, 'ROLE_SUPER_ADMIN')) {
            return true;
        }

        // ORGANIZATION_ADMIN can edit within their organization
        if ($this->hasRole($user, 'ROLE_ORGANIZATION_ADMIN')) {
            return true;
        }

        // Regular users can edit (customize as needed)
        return true;
    }

    /**
     * Check if user can delete CourseModule(s)     */
    protected function canDELETE(?CourseModule $courseModule, User $user): bool
    {
        if (!$courseModule) {
            return false;
        }

        // ADMIN and SUPER_ADMIN can do anything
        if ($this->hasRole($user, 'ROLE_ADMIN')
            || $this->hasRole($user, 'ROLE_SUPER_ADMIN')) {
            return true;
        }

        // Only ORGANIZATION_ADMIN can delete
        return $this->hasRole($user, 'ROLE_ORGANIZATION_ADMIN');
    }

}
