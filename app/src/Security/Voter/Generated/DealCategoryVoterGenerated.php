<?php

declare(strict_types=1);

namespace App\Security\Voter\Generated;

use App\Entity\DealCategory;
use App\Entity\User;
use Symfony\Component\Security\Core\Authentication\Token\TokenInterface;
use Symfony\Component\Security\Core\Authorization\Voter\Voter;
use Symfony\Component\Security\Core\Role\RoleHierarchyInterface;

/**
 * DealCategory Voter (Generated Base Class)
 *
 * This class is ALWAYS regenerated. DO NOT edit this file.
 * Add custom authorization logic to DealCategoryVoter.php instead.
 *
 * @generated by Genmax Code Generator
 */
abstract class DealCategoryVoterGenerated extends Voter
{
    public function __construct(
        protected readonly RoleHierarchyInterface $roleHierarchy
    ) {}

    // Permission constants
    public const LIST = 'DEAL_CATEGORY_LIST';
    public const CREATE = 'DEAL_CATEGORY_CREATE';
    public const VIEW = 'DEAL_CATEGORY_VIEW';
    public const EDIT = 'DEAL_CATEGORY_EDIT';
    public const DELETE = 'DEAL_CATEGORY_DELETE';

    /**
     * Check if user has a specific role (respects role hierarchy)
     */
    protected function hasRole(User $user, string $role): bool
    {
        $userRoles = $this->roleHierarchy->getReachableRoleNames($user->getRoles());
        return in_array($role, $userRoles, true);
    }

    protected function supports(string $attribute, mixed $subject): bool
    {
        // Check if this is a supported permission
        if (!in_array($attribute, [
            self::LIST,
            self::CREATE,
            self::VIEW,
            self::EDIT,
            self::DELETE,
        ], true)) {
            return false;
        }

        // For class-based permissions (LIST, CREATE), subject can be null
        if (in_array($attribute, [
            self::LIST,
            self::CREATE,
        ], true)) {
            return true;
        }

        // For instance-based permissions, subject must be a DealCategory
        return $subject instanceof DealCategory;
    }

    protected function voteOnAttribute(string $attribute, mixed $subject, TokenInterface $token): bool
    {
        $user = $token->getUser();

        // User must be logged in
        if (!$user instanceof User) {
            return false;
        }

        /** @var DealCategory|null $dealCategory */
        $dealCategory = $subject;

        return match ($attribute) {
            self::LIST => $this->canLIST($user),
            self::CREATE => $this->canCREATE($user),
            self::VIEW => $this->canVIEW($dealCategory, $user),
            self::EDIT => $this->canEDIT($dealCategory, $user),
            self::DELETE => $this->canDELETE($dealCategory, $user),
            default => false,
        };
    }

    /**
     * Check if user can list DealCategory     */
    protected function canLIST(User $user): bool
    {
        // ADMIN and SUPER_ADMIN can do anything
        if ($this->hasRole($user, 'ROLE_ADMIN')
            || $this->hasRole($user, 'ROLE_SUPER_ADMIN')) {
            return true;
        }

        // ORGANIZATION_ADMIN can list
        return $this->hasRole($user, 'ROLE_ORGANIZATION_ADMIN');
    }

    /**
     * Check if user can create DealCategory     */
    protected function canCREATE(User $user): bool
    {
        // ADMIN and SUPER_ADMIN can do anything
        if ($this->hasRole($user, 'ROLE_ADMIN')
            || $this->hasRole($user, 'ROLE_SUPER_ADMIN')) {
            return true;
        }

        // ORGANIZATION_ADMIN can create
        return $this->hasRole($user, 'ROLE_ORGANIZATION_ADMIN');
    }

    /**
     * Check if user can view DealCategory(s)     */
    protected function canVIEW(?DealCategory $dealCategory, User $user): bool
    {
        if (!$dealCategory) {
            return false;
        }

        // ADMIN and SUPER_ADMIN can do anything
        if ($this->hasRole($user, 'ROLE_ADMIN')
            || $this->hasRole($user, 'ROLE_SUPER_ADMIN')) {
            return true;
        }

        // Must be in same organization
        $sameOrganization = $user->getOrganization()
            && $dealCategory->getOrganization()
            && $user->getOrganization()->getId()->equals($dealCategory->getOrganization()->getId());

        if (!$sameOrganization) {
            return false;
        }

        // All authenticated users in same organization can view
        return true;
    }

    /**
     * Check if user can edit DealCategory(s)     */
    protected function canEDIT(?DealCategory $dealCategory, User $user): bool
    {
        if (!$dealCategory) {
            return false;
        }

        // ADMIN and SUPER_ADMIN can do anything
        if ($this->hasRole($user, 'ROLE_ADMIN')
            || $this->hasRole($user, 'ROLE_SUPER_ADMIN')) {
            return true;
        }

        // Must be in same organization
        $sameOrganization = $user->getOrganization()
            && $dealCategory->getOrganization()
            && $user->getOrganization()->getId()->equals($dealCategory->getOrganization()->getId());

        if (!$sameOrganization) {
            return false;
        }

        // ORGANIZATION_ADMIN can edit within their organization
        if ($this->hasRole($user, 'ROLE_ORGANIZATION_ADMIN')) {
            return true;
        }

        // Regular users can edit (customize as needed)
        return true;
    }

    /**
     * Check if user can delete DealCategory(s)     */
    protected function canDELETE(?DealCategory $dealCategory, User $user): bool
    {
        if (!$dealCategory) {
            return false;
        }

        // ADMIN and SUPER_ADMIN can do anything
        if ($this->hasRole($user, 'ROLE_ADMIN')
            || $this->hasRole($user, 'ROLE_SUPER_ADMIN')) {
            return true;
        }

        // Must be in same organization
        $sameOrganization = $user->getOrganization()
            && $dealCategory->getOrganization()
            && $user->getOrganization()->getId()->equals($dealCategory->getOrganization()->getId());

        if (!$sameOrganization) {
            return false;
        }

        // Only ORGANIZATION_ADMIN can delete
        return $this->hasRole($user, 'ROLE_ORGANIZATION_ADMIN');
    }

}
