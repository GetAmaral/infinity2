<turbo-stream action="prepend" target="pipelinestage-list">
    <template>
        <tr id="pipelinestage-{{ pipelinestage.id }}">

            <td>{{ pipelinestage.name }}</td>

            <td>{{ pipelinestage.organization }}</td>

            <td>{{ pipelinestage.pipeline }}</td>

            <td>{{ pipelinestage.description }}</td>

            <td>{{ pipelinestage.migrationCriteria }}</td>

            <td>{{ pipelinestage.order }}</td>

            <td>{{ pipelinestage.next }}</td>

            <td>{{ pipelinestage.previous }}</td>

            <td>
                <div class="btn-group" role="group">
                    <a href="{{ path('pipeline_stage_show', {id: pipelinestage.id}) }}"
                       class="btn btn-sm btn-info">
                        <i class="bi bi-eye"></i>
                    </a>
                    <a href="{{ path('pipeline_stage_edit', {id: pipelinestage.id}) }}"
                       class="btn btn-sm btn-warning">
                        <i class="bi bi-pencil"></i>
                    </a>
                    <form method="post"
                          action="{{ path('pipeline_stage_delete', {id: pipelinestage.id}) }}"
                          style="display: inline;"
                          onsubmit="return confirm('{{ 'action.confirm_delete'|trans }}');">
                        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pipelinestage.id) }}">
                        <button type="submit" class="btn btn-sm btn-danger">
                            <i class="bi bi-trash"></i>
                        </button>
                    </form>
                </div>
            </td>
        </tr>
    </template>
</turbo-stream>

<turbo-stream action="update" target="flash-messages">
    <template>
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            <i class="bi bi-check-circle me-2"></i>
            {{ 'PipelineStage'|trans }} created successfully!
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    </template>
</turbo-stream>
