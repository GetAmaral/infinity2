{% extends 'base.html.twig' %}

{% block title %}
    {% if talktypetemplate.id %}
        {{ 'action.edit'|trans }} {{ 'TalkTypeTemplate'|trans }}
    {% else %}
        {{ 'action.create'|trans }} {{ 'TalkTypeTemplate'|trans }}
    {% endif %}
{% endblock %}

{% block body %}
<div class="container py-4">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card">
                <div class="card-header">
                    <h2>
                        {% if talktypetemplate.id %}
                            <i class="bi bi-pencil me-2"></i>
                            {{ 'action.edit'|trans }} {{ 'TalkTypeTemplate'|trans }}
                        {% else %}
                            <i class="bi bi-plus-circle me-2"></i>
                            {{ 'action.create'|trans }} {{ 'TalkTypeTemplate'|trans }}
                        {% endif %}
                    </h2>
                </div>
                <div class="card-body">
                    {{ form_start(form, {'attr': {'data-turbo': 'true'}}) }}

                    {{ form_errors(form) }}


                    <div class="mb-3">
                        {{ form_label(form.name) }}
                        {{ form_widget(form.name) }}
                        {{ form_errors(form.name) }}

                    </div>

                    <div class="mb-3">
                        {{ form_label(form.description) }}
                        {{ form_widget(form.description) }}
                        {{ form_errors(form.description) }}

                    </div>

                    <div class="mb-3">
                        {{ form_label(form.iconUrl) }}
                        {{ form_widget(form.iconUrl) }}
                        {{ form_errors(form.iconUrl) }}

                    </div>


                    <div class="d-flex justify-content-between mt-4">
                        <a href="{{ path('talk_type_template_index') }}"
                           class="btn btn-secondary">
                            <i class="bi bi-arrow-left me-1"></i>
                            {{ 'action.back'|trans }}
                        </a>
                        <button type="submit" class="btn btn-primary">
                            <i class="bi bi-check-circle me-1"></i>
                            {{ 'action.save'|trans }}
                        </button>
                    </div>

                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
