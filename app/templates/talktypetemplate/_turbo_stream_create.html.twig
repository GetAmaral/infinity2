<turbo-stream action="prepend" target="talktypetemplate-list">
    <template>
        <tr id="talktypetemplate-{{ talktypetemplate.id }}">

            <td>{{ talktypetemplate.name }}</td>

            <td>{{ talktypetemplate.description }}</td>

            <td>{{ talktypetemplate.iconUrl }}</td>

            <td>
                <div class="btn-group" role="group">
                    <a href="{{ path('talk_type_template_show', {id: talktypetemplate.id}) }}"
                       class="btn btn-sm btn-info">
                        <i class="bi bi-eye"></i>
                    </a>
                    <a href="{{ path('talk_type_template_edit', {id: talktypetemplate.id}) }}"
                       class="btn btn-sm btn-warning">
                        <i class="bi bi-pencil"></i>
                    </a>
                    <form method="post"
                          action="{{ path('talk_type_template_delete', {id: talktypetemplate.id}) }}"
                          style="display: inline;"
                          onsubmit="return confirm('{{ 'action.confirm_delete'|trans }}');">
                        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ talktypetemplate.id) }}">
                        <button type="submit" class="btn btn-sm btn-danger">
                            <i class="bi bi-trash"></i>
                        </button>
                    </form>
                </div>
            </td>
        </tr>
    </template>
</turbo-stream>

<turbo-stream action="update" target="flash-messages">
    <template>
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            <i class="bi bi-check-circle me-2"></i>
            {{ 'TalkTypeTemplate'|trans }} created successfully!
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    </template>
</turbo-stream>
