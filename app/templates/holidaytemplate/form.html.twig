{% extends 'base.html.twig' %}

{% block title %}
    {% if holidaytemplate.id %}
        {{ 'action.edit'|trans }} {{ 'HolidayTemplate'|trans }}
    {% else %}
        {{ 'action.create'|trans }} {{ 'HolidayTemplate'|trans }}
    {% endif %}
{% endblock %}

{% block body %}
<div class="container py-4">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card">
                <div class="card-header">
                    <h2>
                        {% if holidaytemplate.id %}
                            <i class="bi bi-pencil me-2"></i>
                            {{ 'action.edit'|trans }} {{ 'HolidayTemplate'|trans }}
                        {% else %}
                            <i class="bi bi-plus-circle me-2"></i>
                            {{ 'action.create'|trans }} {{ 'HolidayTemplate'|trans }}
                        {% endif %}
                    </h2>
                </div>
                <div class="card-body">
                    {{ form_start(form, {'attr': {'data-turbo': 'true'}}) }}

                    {{ form_errors(form) }}


                    <div class="mb-3">
                        {{ form_label(form.name) }}
                        {{ form_widget(form.name) }}
                        {{ form_errors(form.name) }}

                    </div>

                    <div class="mb-3">
                        {{ form_label(form.date) }}
                        {{ form_widget(form.date) }}
                        {{ form_errors(form.date) }}

                    </div>

                    <div class="mb-3">
                        {{ form_label(form.recurrenceInterval) }}
                        {{ form_widget(form.recurrenceInterval) }}
                        {{ form_errors(form.recurrenceInterval) }}

                    </div>

                    <div class="mb-3">
                        {{ form_label(form.recurrenceFrequency) }}
                        {{ form_widget(form.recurrenceFrequency) }}
                        {{ form_errors(form.recurrenceFrequency) }}

                    </div>

                    <div class="mb-3">
                        {{ form_label(form.blockSchedule) }}
                        {{ form_widget(form.blockSchedule) }}
                        {{ form_errors(form.blockSchedule) }}

                    </div>


                    <div class="d-flex justify-content-between mt-4">
                        <a href="{{ path('holiday_template_index') }}"
                           class="btn btn-secondary">
                            <i class="bi bi-arrow-left me-1"></i>
                            {{ 'action.back'|trans }}
                        </a>
                        <button type="submit" class="btn btn-primary">
                            <i class="bi bi-check-circle me-1"></i>
                            {{ 'action.save'|trans }}
                        </button>
                    </div>

                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
