{% extends 'base.html.twig' %}

{% block title %}{{ 'SocialMedia'|trans }} - {{ socialmedia }}{% endblock %}

{% block body %}
<div class="container py-4">
    <div class="row justify-content-center">
        <div class="col-md-10">
            <div class="card">
                <div class="card-header">
                    <div class="d-flex justify-content-between align-items-center">
                        <h2>
                            <i class="bi-circle me-2"></i>
                            {{ 'SocialMedia'|trans }} Details
                        </h2>
                        <div class="btn-group">
                            <a href="{{ path('social_media_edit', {id: socialmedia.id}) }}"
                               class="btn btn-warning">
                                <i class="bi bi-pencil me-1"></i>
                                {{ 'action.edit'|trans }}
                            </a>
                            <a href="{{ path('social_media_index') }}"
                               class="btn btn-secondary">
                                <i class="bi bi-arrow-left me-1"></i>
                                {{ 'action.back'|trans }}
                            </a>
                        </div>
                    </div>
                </div>
                <div class="card-body">
                    <dl class="row">

                        <dt class="col-sm-3">{{ 'Name'|trans }}</dt>
                        <dd class="col-sm-9">{{ socialmedia.name }}</dd>

                        <dt class="col-sm-3">{{ 'Url'|trans }}</dt>
                        <dd class="col-sm-9">{{ socialmedia.url }}</dd>

                        <dt class="col-sm-3">{{ 'ApiKey'|trans }}</dt>
                        <dd class="col-sm-9">{{ socialmedia.apiKey }}</dd>

                        <dt class="col-sm-3">{{ 'SocialMediaType'|trans }}</dt>
                        <dd class="col-sm-9">{{ socialmedia.socialMediaType }}</dd>

                        <dt class="col-sm-3">{{ 'Organization'|trans }}</dt>
                        <dd class="col-sm-9">{{ socialmedia.organization }}</dd>

                        <dt class="col-sm-3">{{ 'User'|trans }}</dt>
                        <dd class="col-sm-9">{{ socialmedia.user }}</dd>

                        <dt class="col-sm-3">{{ 'Contact'|trans }}</dt>
                        <dd class="col-sm-9">{{ socialmedia.contact }}</dd>

                        <dt class="col-sm-3">{{ 'Company'|trans }}</dt>
                        <dd class="col-sm-9">{{ socialmedia.company }}</dd>

                        <dt class="col-sm-3">{{ 'field.created_at'|trans }}</dt>
                        <dd class="col-sm-9">{{ socialmedia.createdAt|date('Y-m-d H:i:s') }}</dd>

                        <dt class="col-sm-3">{{ 'field.updated_at'|trans }}</dt>
                        <dd class="col-sm-9">{{ socialmedia.updatedAt|date('Y-m-d H:i:s') }}</dd>
                    </dl>
                </div>
                <div class="card-footer">
                    <form method="post"
                          action="{{ path('social_media_delete', {id: socialmedia.id}) }}"
                          onsubmit="return confirm('{{ 'action.confirm_delete'|trans }}');">
                        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ socialmedia.id) }}">
                        <button type="submit" class="btn btn-danger">
                            <i class="bi bi-trash me-1"></i>
                            {{ 'action.delete'|trans }}
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
