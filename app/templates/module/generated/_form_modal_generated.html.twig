{% extends '_base_modal.html.twig' %}

{# Configure modal appearance and behavior #}
{% set entity_type = 'module' %}
{% set icon = 'grid' %}
{% set form_action = (is_edit and module.id) ? path('module_edit', {id: module.id}) : path('module_new') %}
{% set title_create = 'module.modal.create.title'|trans({}, 'module') %}
{% set title_edit = 'module.modal.edit.title'|trans({}, 'module') %}
{% set subtitle_create = 'module.modal.create.subtitle'|trans({}, 'module') %}
{% set subtitle_edit = 'module.modal.edit.subtitle'|trans({}, 'module') %}
{% set submit_key = is_edit ? 'button.save.changes' : 'module.button.create.module' %}
{% set submit_domain = is_edit ? 'messages' : 'module' %}
{% set entity = module %}

{# Form fields - auto-render all fields #}
{% block modal_body %}
    {# Render all form fields automatically #}
    {% for child in form.children %}
        {% if child.vars.name not in ['_token'] %}
            <div class="form-group-modern">
                {% if child.vars.block_prefixes[1] == 'checkbox' %}
                    {# Checkbox/Switch styling #}
                    <div class="form-check form-switch">
                        {{ form_widget(child, {
                            'attr': {
                                'class': 'form-check-input',
                                'style': 'cursor: pointer;'
                            }
                        }) }}
                        <label class="form-check-label" for="{{ child.vars.id }}" style="color: var(--luminai-text-primary); cursor: pointer; margin-left: 0.5rem;">
                            {{ form_label(child) }}
                        </label>
                    </div>
                {% else %}
                    {# Regular field styling #}
                    <label class="form-label-modern">{{ form_label(child) }}</label>
                    {{ form_widget(child, {
                        'attr': {
                            'class': 'form-input-modern' ~ (child.vars.errors|length > 0 ? ' input-error' : '')
                        }
                    }) }}
                {% endif %}
                {{ form_errors(child) }}
            </div>
        {% endif %}
    {% endfor %}

    {{ form_widget(form._token) }}
{% endblock %}
