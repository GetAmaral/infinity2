{% extends '_base_modal.html.twig' %}

{# Configure modal appearance and behavior #}
{% set entity_type = 'stepconnection' %}
{% set icon = 'arrow-left-right' %}
{% set form_action = (is_edit and stepConnection.id) ? path('stepconnection_edit', {id: stepConnection.id}) : path('stepconnection_new') %}
{% set title_create = 'stepconnection.modal.create.title'|trans({}, 'stepconnection') %}
{% set title_edit = 'stepconnection.modal.edit.title'|trans({}, 'stepconnection') %}
{% set subtitle_create = 'stepconnection.modal.create.subtitle'|trans({}, 'stepconnection') %}
{% set subtitle_edit = 'stepconnection.modal.edit.subtitle'|trans({}, 'stepconnection') %}
{% set submit_key = is_edit ? 'button.save.changes' : 'stepconnection.button.create.stepconnection' %}
{% set submit_domain = is_edit ? 'messages' : 'stepconnection' %}
{% set entity = stepConnection %}

{# Form fields - auto-render all fields #}
{% block modal_body %}
    {# Render all form fields automatically #}
    {% for child in form.children %}
        {% if child.vars.name not in ['_token'] %}
            <div class="form-group-modern">
                {% if child.vars.block_prefixes[1] == 'checkbox' %}
                    {# Checkbox/Switch styling #}
                    <div class="form-check form-switch">
                        {{ form_widget(child, {
                            'attr': {
                                'class': 'form-check-input',
                                'style': 'cursor: pointer;'
                            }
                        }) }}
                        <label class="form-check-label" for="{{ child.vars.id }}" style="color: var(--luminai-text-primary); cursor: pointer; margin-left: 0.5rem;">
                            {{ form_label(child) }}
                        </label>
                    </div>
                {% else %}
                    {# Regular field styling #}
                    <label class="form-label-modern">{{ form_label(child) }}</label>
                    {{ form_widget(child, {
                        'attr': {
                            'class': 'form-input-modern' ~ (child.vars.errors|length > 0 ? ' input-error' : '')
                        }
                    }) }}
                {% endif %}
                {{ form_errors(child) }}
            </div>
        {% endif %}
    {% endfor %}

    {{ form_widget(form._token) }}
{% endblock %}
