{% extends '_base_modal.html.twig' %}

{# Configure modal appearance and behavior #}
{% set entity_type = 'tag' %}
{% set icon = 'tag-fill' %}
{% set form_action = (is_edit and tag.id) ? path('tag_edit', {id: tag.id}) : path('tag_new') %}
{% set title_create = 'tag.modal.create.title'|trans({}, 'tag') %}
{% set title_edit = 'tag.modal.edit.title'|trans({}, 'tag') %}
{% set subtitle_create = 'tag.modal.create.subtitle'|trans({}, 'tag') %}
{% set subtitle_edit = 'tag.modal.edit.subtitle'|trans({}, 'tag') %}
{% set submit_key = is_edit ? 'button.save.changes' : 'tag.button.create.tag' %}
{% set submit_domain = is_edit ? 'messages' : 'tag' %}
{% set entity = tag %}

{# Form fields - auto-render all fields #}
{% block modal_body %}
    {# Render all form fields automatically #}
    {% for child in form.children %}
        {% if child.vars.name not in ['_token'] %}
            <div class="form-group-modern">
                {% if child.vars.block_prefixes[1] == 'checkbox' %}
                    {# Checkbox/Switch styling #}
                    <div class="form-check form-switch">
                        {{ form_widget(child, {
                            'attr': {
                                'class': 'form-check-input',
                                'style': 'cursor: pointer;'
                            }
                        }) }}
                        <label class="form-check-label" for="{{ child.vars.id }}" style="color: var(--luminai-text-primary); cursor: pointer; margin-left: 0.5rem;">
                            {{ form_label(child) }}
                        </label>
                    </div>
                {% else %}
                    {# Regular field styling #}
                    <label class="form-label-modern">{{ form_label(child) }}</label>
                    {{ form_widget(child, {
                        'attr': {
                            'class': 'form-input-modern' ~ (child.vars.errors|length > 0 ? ' input-error' : '')
                        }
                    }) }}
                {% endif %}
                {{ form_errors(child) }}
            </div>
        {% endif %}
    {% endfor %}

    {{ form_widget(form._token) }}
{% endblock %}
