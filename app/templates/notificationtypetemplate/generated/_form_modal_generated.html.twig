{% extends '_base_modal.html.twig' %}

{# Configure modal appearance and behavior #}
{% set entity_type = 'notificationtypetemplate' %}
{% set icon = 'bell-slash' %}
{% set form_action = (is_edit and notificationTypeTemplate.id) ? path('notificationtypetemplate_edit', {id: notificationTypeTemplate.id}) : path('notificationtypetemplate_new') %}
{% set title_create = 'notificationtypetemplate.modal.create.title'|trans({}, 'notificationtypetemplate') %}
{% set title_edit = 'notificationtypetemplate.modal.edit.title'|trans({}, 'notificationtypetemplate') %}
{% set subtitle_create = 'notificationtypetemplate.modal.create.subtitle'|trans({}, 'notificationtypetemplate') %}
{% set subtitle_edit = 'notificationtypetemplate.modal.edit.subtitle'|trans({}, 'notificationtypetemplate') %}
{% set submit_key = is_edit ? 'button.save.changes' : 'notificationtypetemplate.button.create.notificationtypetemplate' %}
{% set submit_domain = is_edit ? 'messages' : 'notificationtypetemplate' %}
{% set entity = notificationTypeTemplate %}

{# Form fields - auto-render all fields #}
{% block modal_body %}
    {# Render all form fields automatically #}
    {% for child in form.children %}
        {% if child.vars.name not in ['_token'] %}
            <div class="form-group-modern">
                {% if child.vars.block_prefixes[1] == 'checkbox' %}
                    {# Checkbox/Switch styling #}
                    <div class="form-check form-switch">
                        {{ form_widget(child, {
                            'attr': {
                                'class': 'form-check-input',
                                'style': 'cursor: pointer;'
                            }
                        }) }}
                        <label class="form-check-label" for="{{ child.vars.id }}" style="color: var(--luminai-text-primary); cursor: pointer; margin-left: 0.5rem;">
                            {{ form_label(child) }}
                        </label>
                    </div>
                {% else %}
                    {# Regular field styling #}
                    <label class="form-label-modern">{{ form_label(child) }}</label>
                    {{ form_widget(child, {
                        'attr': {
                            'class': 'form-input-modern' ~ (child.vars.errors|length > 0 ? ' input-error' : '')
                        }
                    }) }}
                {% endif %}
                {{ form_errors(child) }}
            </div>
        {% endif %}
    {% endfor %}

    {{ form_widget(form._token) }}
{% endblock %}
