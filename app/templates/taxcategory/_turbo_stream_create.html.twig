<turbo-stream action="prepend" target="taxcategory-list">
    <template>
        <tr id="taxcategory-{{ taxcategory.id }}">

            <td>{{ taxcategory.name }}</td>

            <td>{{ taxcategory.organization }}</td>

            <td>{{ taxcategory.taxRate }}</td>

            <td>{{ taxcategory.description }}</td>

            <td>
                <div class="btn-group" role="group">
                    <a href="{{ path('tax_category_show', {id: taxcategory.id}) }}"
                       class="btn btn-sm btn-info">
                        <i class="bi bi-eye"></i>
                    </a>
                    <a href="{{ path('tax_category_edit', {id: taxcategory.id}) }}"
                       class="btn btn-sm btn-warning">
                        <i class="bi bi-pencil"></i>
                    </a>
                    <form method="post"
                          action="{{ path('tax_category_delete', {id: taxcategory.id}) }}"
                          style="display: inline;"
                          onsubmit="return confirm('{{ 'action.confirm_delete'|trans }}');">
                        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ taxcategory.id) }}">
                        <button type="submit" class="btn btn-sm btn-danger">
                            <i class="bi bi-trash"></i>
                        </button>
                    </form>
                </div>
            </td>
        </tr>
    </template>
</turbo-stream>

<turbo-stream action="update" target="flash-messages">
    <template>
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            <i class="bi bi-check-circle me-2"></i>
            {{ 'TaxCategory'|trans }} created successfully!
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    </template>
</turbo-stream>
