{% extends 'base.html.twig' %}

{% block title %}{{ 'action.create'|trans }} {{ 'city.singular'|trans({}, 'city') }}{% endblock %}

{% block body %}
<div class="container py-4">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="luminai-card p-4">
                <div class="mb-4">
                    <h2>
                        <i class="bi bi-plus-circle me-2"></i>{{ 'action.create'|trans }} {{ 'city.singular'|trans({}, 'city') }}
                    </h2>
                </div>

                {% include 'city/generated/form_generated.html.twig' with {
                    'city': city,
                    'form': form
                } %}
            </div>
        </div>
    </div>
</div>
{% endblock %}
