<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Novo Contato - Website Avelum</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        .header {
            background: linear-gradient(135deg, #4f46e5 0%, #7c3aed 100%);
            color: white;
            padding: 30px;
            text-align: center;
            border-radius: 8px 8px 0 0;
        }
        .content {
            background: #f9fafb;
            padding: 30px;
            border: 1px solid #e5e7eb;
            border-top: none;
            border-radius: 0 0 8px 8px;
        }
        .field {
            margin-bottom: 20px;
            padding: 15px;
            background: white;
            border-radius: 6px;
            border-left: 4px solid #7c3aed;
        }
        .label {
            font-weight: bold;
            color: #4f46e5;
            display: block;
            margin-bottom: 5px;
        }
        .value {
            color: #374151;
        }
        .message-box {
            background: white;
            padding: 20px;
            border-radius: 6px;
            margin-top: 20px;
            border: 1px solid #e5e7eb;
        }
        .footer {
            text-align: center;
            margin-top: 30px;
            padding-top: 20px;
            border-top: 1px solid #e5e7eb;
            color: #6b7280;
            font-size: 14px;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>✉️ Novo Contato do Site</h1>
        <p>Formulário de Contato - avelum.com.br</p>
    </div>

    <div class="content">
        <div class="field">
            <span class="label">Nome:</span>
            <span class="value">{{ name }}</span>
        </div>

        <div class="field">
            <span class="label">Email:</span>
            <span class="value"><a href="mailto:{{ email }}">{{ email }}</a></span>
        </div>

        {% if phone %}
        <div class="field">
            <span class="label">Telefone:</span>
            <span class="value">{{ phone }}</span>
        </div>
        {% endif %}

        <div class="field">
            <span class="label">Mensagem:</span>
            <div class="message-box">
                {{ message|nl2br }}
            </div>
        </div>

        <div class="footer">
            <p><strong>Avelum</strong> - Soluções Inteligentes de IA</p>
            <p>Email enviado automaticamente do website - {{ 'now'|date('d/m/Y H:i') }}</p>
        </div>
    </div>
</body>
</html>
