<turbo-stream action="prepend" target="socialmediatype-list">
    <template>
        <tr id="socialmediatype-{{ socialmediatype.id }}">

            <td>{{ socialmediatype.name }}</td>

            <td>{{ socialmediatype.url }}</td>

            <td>{{ socialmediatype.iconUrl }}</td>

            <td>
                <div class="btn-group" role="group">
                    <a href="{{ path('social_media_type_show', {id: socialmediatype.id}) }}"
                       class="btn btn-sm btn-info">
                        <i class="bi bi-eye"></i>
                    </a>
                    <a href="{{ path('social_media_type_edit', {id: socialmediatype.id}) }}"
                       class="btn btn-sm btn-warning">
                        <i class="bi bi-pencil"></i>
                    </a>
                    <form method="post"
                          action="{{ path('social_media_type_delete', {id: socialmediatype.id}) }}"
                          style="display: inline;"
                          onsubmit="return confirm('{{ 'action.confirm_delete'|trans }}');">
                        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ socialmediatype.id) }}">
                        <button type="submit" class="btn btn-sm btn-danger">
                            <i class="bi bi-trash"></i>
                        </button>
                    </form>
                </div>
            </td>
        </tr>
    </template>
</turbo-stream>

<turbo-stream action="update" target="flash-messages">
    <template>
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            <i class="bi bi-check-circle me-2"></i>
            {{ 'SocialMediaType'|trans }} created successfully!
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    </template>
</turbo-stream>
