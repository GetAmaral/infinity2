{% extends '_base_modal.html.twig' %}

{# Configure modal appearance and behavior #}
{% set entity_type = 'campaign' %}
{% set icon = 'megaphone' %}
{% set form_action = (is_edit and campaign.id) ? path('campaign_edit', {id: campaign.id}) : path('campaign_new') %}
{% set title_create = 'campaign.modal.create.title'|trans({}, 'campaign') %}
{% set title_edit = 'campaign.modal.edit.title'|trans({}, 'campaign') %}
{% set subtitle_create = 'campaign.modal.create.subtitle'|trans({}, 'campaign') %}
{% set subtitle_edit = 'campaign.modal.edit.subtitle'|trans({}, 'campaign') %}
{% set submit_key = is_edit ? 'button.save.changes' : 'campaign.button.create.campaign' %}
{% set submit_domain = is_edit ? 'messages' : 'campaign' %}
{% set entity = campaign %}

{# Form fields - auto-render all fields #}
{% block modal_body %}
    {# Render all form fields automatically #}
    {% for child in form.children %}
        {% if child.vars.name not in ['_token'] %}
            <div class="form-group-modern">
                {% if child.vars.block_prefixes[1] == 'checkbox' %}
                    {# Checkbox/Switch styling #}
                    <div class="form-check form-switch">
                        {{ form_widget(child, {
                            'attr': {
                                'class': 'form-check-input',
                                'style': 'cursor: pointer;'
                            }
                        }) }}
                        <label class="form-check-label" for="{{ child.vars.id }}" style="color: var(--luminai-text-primary); cursor: pointer; margin-left: 0.5rem;">
                            {{ form_label(child) }}
                        </label>
                    </div>
                {% else %}
                    {# Regular field styling #}
                    <label class="form-label-modern">{{ form_label(child) }}</label>
                    {{ form_widget(child, {
                        'attr': {
                            'class': 'form-input-modern' ~ (child.vars.errors|length > 0 ? ' input-error' : '')
                        }
                    }) }}
                {% endif %}
                {{ form_errors(child) }}
            </div>
        {% endif %}
    {% endfor %}

    {{ form_widget(form._token) }}
{% endblock %}
