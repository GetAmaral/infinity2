<turbo-stream action="replace" target="tasktype-{{ tasktype.id }}">
    <template>
        <tr id="tasktype-{{ tasktype.id }}">

            <td>{{ tasktype.name }}</td>

            <td>{{ tasktype.organization }}</td>

            <td>{{ tasktype.taskFunction }}</td>

            <td>
                <div class="btn-group" role="group">
                    <a href="{{ path('task_type_show', {id: tasktype.id}) }}"
                       class="btn btn-sm btn-info">
                        <i class="bi bi-eye"></i>
                    </a>
                    <a href="{{ path('task_type_edit', {id: tasktype.id}) }}"
                       class="btn btn-sm btn-warning">
                        <i class="bi bi-pencil"></i>
                    </a>
                    <form method="post"
                          action="{{ path('task_type_delete', {id: tasktype.id}) }}"
                          style="display: inline;"
                          onsubmit="return confirm('{{ 'action.confirm_delete'|trans }}');">
                        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tasktype.id) }}">
                        <button type="submit" class="btn btn-sm btn-danger">
                            <i class="bi bi-trash"></i>
                        </button>
                    </form>
                </div>
            </td>
        </tr>
    </template>
</turbo-stream>

<turbo-stream action="update" target="flash-messages">
    <template>
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            <i class="bi bi-check-circle me-2"></i>
            {{ 'TaskType'|trans }} updated successfully!
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    </template>
</turbo-stream>
