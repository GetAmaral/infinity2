<turbo-stream action="replace" target="eventresourcetype-{{ eventresourcetype.id }}">
    <template>
        <tr id="eventresourcetype-{{ eventresourcetype.id }}">

            <td>{{ eventresourcetype.name }}</td>

            <td>{{ eventresourcetype.organization }}</td>

            <td>{{ eventresourcetype.description }}</td>

            <td>
                <div class="btn-group" role="group">
                    <a href="{{ path('event_resource_type_show', {id: eventresourcetype.id}) }}"
                       class="btn btn-sm btn-info">
                        <i class="bi bi-eye"></i>
                    </a>
                    <a href="{{ path('event_resource_type_edit', {id: eventresourcetype.id}) }}"
                       class="btn btn-sm btn-warning">
                        <i class="bi bi-pencil"></i>
                    </a>
                    <form method="post"
                          action="{{ path('event_resource_type_delete', {id: eventresourcetype.id}) }}"
                          style="display: inline;"
                          onsubmit="return confirm('{{ 'action.confirm_delete'|trans }}');">
                        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ eventresourcetype.id) }}">
                        <button type="submit" class="btn btn-sm btn-danger">
                            <i class="bi bi-trash"></i>
                        </button>
                    </form>
                </div>
            </td>
        </tr>
    </template>
</turbo-stream>

<turbo-stream action="update" target="flash-messages">
    <template>
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            <i class="bi bi-check-circle me-2"></i>
            {{ 'EventResourceType'|trans }} updated successfully!
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    </template>
</turbo-stream>
