{% extends 'base.html.twig' %}

{% block title %}
    {% if eventresourcebooking.id %}
        {{ 'action.edit'|trans }} {{ 'EventResourceBooking'|trans }}
    {% else %}
        {{ 'action.create'|trans }} {{ 'EventResourceBooking'|trans }}
    {% endif %}
{% endblock %}

{% block body %}
<div class="container py-4">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card">
                <div class="card-header">
                    <h2>
                        {% if eventresourcebooking.id %}
                            <i class="bi bi-pencil me-2"></i>
                            {{ 'action.edit'|trans }} {{ 'EventResourceBooking'|trans }}
                        {% else %}
                            <i class="bi bi-plus-circle me-2"></i>
                            {{ 'action.create'|trans }} {{ 'EventResourceBooking'|trans }}
                        {% endif %}
                    </h2>
                </div>
                <div class="card-body">
                    {{ form_start(form, {'attr': {'data-turbo': 'true'}}) }}

                    {{ form_errors(form) }}


                    <div class="mb-3">
                        {{ form_label(form.quantity) }}
                        {{ form_widget(form.quantity) }}
                        {{ form_errors(form.quantity) }}

                    </div>

                    <div class="mb-3">
                        {{ form_label(form.bookingStatus) }}
                        {{ form_widget(form.bookingStatus) }}
                        {{ form_errors(form.bookingStatus) }}

                    </div>

                    <div class="mb-3">
                        {{ form_label(form.notes) }}
                        {{ form_widget(form.notes) }}
                        {{ form_errors(form.notes) }}

                    </div>


                    <div class="d-flex justify-content-between mt-4">
                        <a href="{{ path('event_resource_booking_index') }}"
                           class="btn btn-secondary">
                            <i class="bi bi-arrow-left me-1"></i>
                            {{ 'action.back'|trans }}
                        </a>
                        <button type="submit" class="btn btn-primary">
                            <i class="bi bi-check-circle me-1"></i>
                            {{ 'action.save'|trans }}
                        </button>
                    </div>

                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
