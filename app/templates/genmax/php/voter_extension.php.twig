<?php

declare(strict_types=1);

namespace {{ namespace }};

use {{ generatedNamespace }}\{{ baseClassName }};

/**
 * {{ entityName }} Voter
 *
 * This class extends the generated base and is SAFE TO EDIT.
 * Add custom authorization logic and override methods here.
 *
 * @generated once by Genmax Code Generator
 */
final class {{ className }} extends {{ baseClassName }}
{
    // Constructor is inherited from base class (RoleHierarchy is automatically injected)

    // Override authorization methods here if needed

    // Example: Custom VIEW logic
    // protected function canView(?{{ entityName }} ${{ entityVariable }}, User $user): bool
    // {
    //     // Add custom logic
    //     if (${{ entityVariable }}->isPublic()) {
    //         return true;
    //     }
    //
    //     return parent::canView(${{ entityVariable }}, $user);
    // }

    // Example: Custom EDIT logic
    // protected function canEdit(?{{ entityName }} ${{ entityVariable }}, User $user): bool
    // {
    //     // Owner can edit their own {{ entityName|lower }}
    //     if (${{ entityVariable }}->getOwner() && $user->getId()->equals(${{ entityVariable }}->getOwner()->getId())) {
    //         return true;
    //     }
    //
    //     return parent::canEdit(${{ entityVariable }}, $user);
    // }

    // Example: Custom DELETE logic
    // protected function canDelete(?{{ entityName }} ${{ entityVariable }}, User $user): bool
    // {
    //     // Only allow deletion if no related data
    //     if (${{ entityVariable }}->hasRelatedData()) {
    //         return false;
    //     }
    //
    //     return parent::canDelete(${{ entityVariable }}, $user);
    // }
}
