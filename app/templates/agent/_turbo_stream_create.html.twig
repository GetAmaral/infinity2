<turbo-stream action="prepend" target="agent-list">
    <template>
        <tr id="agent-{{ agent.id }}">

            <td>{{ agent.name }}</td>

            <td>{{ agent.organization }}</td>

            <td>{{ agent.user }}</td>

            <td>{{ agent.agentType }}</td>

            <td>{{ agent.agentPrompt }}</td>

            <td>
                <div class="btn-group" role="group">
                    <a href="{{ path('agent_show', {id: agent.id}) }}"
                       class="btn btn-sm btn-info">
                        <i class="bi bi-eye"></i>
                    </a>
                    <a href="{{ path('agent_edit', {id: agent.id}) }}"
                       class="btn btn-sm btn-warning">
                        <i class="bi bi-pencil"></i>
                    </a>
                    <form method="post"
                          action="{{ path('agent_delete', {id: agent.id}) }}"
                          style="display: inline;"
                          onsubmit="return confirm('{{ 'action.confirm_delete'|trans }}');">
                        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ agent.id) }}">
                        <button type="submit" class="btn btn-sm btn-danger">
                            <i class="bi bi-trash"></i>
                        </button>
                    </form>
                </div>
            </td>
        </tr>
    </template>
</turbo-stream>

<turbo-stream action="update" target="flash-messages">
    <template>
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            <i class="bi bi-check-circle me-2"></i>
            {{ 'Agent'|trans }} created successfully!
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    </template>
</turbo-stream>
