<turbo-stream action="prepend" target="eventcategory-list">
    <template>
        <tr id="eventcategory-{{ eventcategory.id }}">

            <td>{{ eventcategory.name }}</td>

            <td>{{ eventcategory.organization }}</td>

            <td>{{ eventcategory.description }}</td>

            <td>{{ eventcategory.color }}</td>

            <td>{{ eventcategory.icon }}</td>

            <td>
                <div class="btn-group" role="group">
                    <a href="{{ path('event_category_show', {id: eventcategory.id}) }}"
                       class="btn btn-sm btn-info">
                        <i class="bi bi-eye"></i>
                    </a>
                    <a href="{{ path('event_category_edit', {id: eventcategory.id}) }}"
                       class="btn btn-sm btn-warning">
                        <i class="bi bi-pencil"></i>
                    </a>
                    <form method="post"
                          action="{{ path('event_category_delete', {id: eventcategory.id}) }}"
                          style="display: inline;"
                          onsubmit="return confirm('{{ 'action.confirm_delete'|trans }}');">
                        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ eventcategory.id) }}">
                        <button type="submit" class="btn btn-sm btn-danger">
                            <i class="bi bi-trash"></i>
                        </button>
                    </form>
                </div>
            </td>
        </tr>
    </template>
</turbo-stream>

<turbo-stream action="update" target="flash-messages">
    <template>
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            <i class="bi bi-check-circle me-2"></i>
            {{ 'EventCategory'|trans }} created successfully!
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    </template>
</turbo-stream>
