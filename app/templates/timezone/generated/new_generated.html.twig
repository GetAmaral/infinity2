{% extends 'base.html.twig' %}

{% block title %}{{ 'action.create'|trans }} {{ 'timezone.singular'|trans({}, 'timezone') }}{% endblock %}

{% block body %}
<div class="container py-4">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="luminai-card p-4">
                <div class="mb-4">
                    <h2>
                        <i class="bi bi-plus-circle me-2"></i>{{ 'action.create'|trans }} {{ 'timezone.singular'|trans({}, 'timezone') }}
                    </h2>
                </div>

                {% include 'timezone/generated/form_generated.html.twig' with {
                    'timeZone': timeZone,
                    'form': form
                } %}
            </div>
        </div>
    </div>
</div>
{% endblock %}
