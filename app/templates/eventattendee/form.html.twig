{% extends 'base.html.twig' %}

{% block title %}
    {% if eventattendee.id %}
        {{ 'action.edit'|trans }} {{ 'EventAttendee'|trans }}
    {% else %}
        {{ 'action.create'|trans }} {{ 'EventAttendee'|trans }}
    {% endif %}
{% endblock %}

{% block body %}
<div class="container py-4">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card">
                <div class="card-header">
                    <h2>
                        {% if eventattendee.id %}
                            <i class="bi bi-pencil me-2"></i>
                            {{ 'action.edit'|trans }} {{ 'EventAttendee'|trans }}
                        {% else %}
                            <i class="bi bi-plus-circle me-2"></i>
                            {{ 'action.create'|trans }} {{ 'EventAttendee'|trans }}
                        {% endif %}
                    </h2>
                </div>
                <div class="card-body">
                    {{ form_start(form, {'attr': {'data-turbo': 'true'}}) }}

                    {{ form_errors(form) }}


                    <div class="mb-3">
                        {{ form_label(form.name) }}
                        {{ form_widget(form.name) }}
                        {{ form_errors(form.name) }}

                    </div>

                    <div class="mb-3">
                        {{ form_label(form.email) }}
                        {{ form_widget(form.email) }}
                        {{ form_errors(form.email) }}

                    </div>

                    <div class="mb-3">
                        {{ form_label(form.phone) }}
                        {{ form_widget(form.phone) }}
                        {{ form_errors(form.phone) }}

                    </div>

                    <div class="mb-3">
                        {{ form_label(form.attendeeStatus) }}
                        {{ form_widget(form.attendeeStatus) }}
                        {{ form_errors(form.attendeeStatus) }}

                    </div>

                    <div class="mb-3">
                        {{ form_label(form.comment) }}
                        {{ form_widget(form.comment) }}
                        {{ form_errors(form.comment) }}

                    </div>

                    <div class="mb-3">
                        {{ form_label(form.organizer) }}
                        {{ form_widget(form.organizer) }}
                        {{ form_errors(form.organizer) }}

                    </div>

                    <div class="mb-3">
                        {{ form_label(form.optional) }}
                        {{ form_widget(form.optional) }}
                        {{ form_errors(form.optional) }}

                    </div>


                    <div class="d-flex justify-content-between mt-4">
                        <a href="{{ path('event_attendee_index') }}"
                           class="btn btn-secondary">
                            <i class="bi bi-arrow-left me-1"></i>
                            {{ 'action.back'|trans }}
                        </a>
                        <button type="submit" class="btn btn-primary">
                            <i class="bi bi-check-circle me-1"></i>
                            {{ 'action.save'|trans }}
                        </button>
                    </div>

                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
