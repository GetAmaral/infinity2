{% extends 'base.html.twig' %}

{% block title %}{{ 'action.create'|trans }} {{ 'productbatch.singular'|trans({}, 'productbatch') }}{% endblock %}

{% block body %}
<div class="container py-4">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="luminai-card p-4">
                <div class="mb-4">
                    <h2>
                        <i class="bi bi-plus-circle me-2"></i>{{ 'action.create'|trans }} {{ 'productbatch.singular'|trans({}, 'productbatch') }}
                    </h2>
                </div>

                {% include 'productbatch/generated/form_generated.html.twig' with {
                    'productBatch': productBatch,
                    'form': form
                } %}
            </div>
        </div>
    </div>
</div>
{% endblock %}
