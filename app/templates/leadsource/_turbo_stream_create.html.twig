<turbo-stream action="prepend" target="leadsource-list">
    <template>
        <tr id="leadsource-{{ leadsource.id }}">

            <td>{{ leadsource.name }}</td>

            <td>{{ leadsource.organization }}</td>

            <td>{{ leadsource.description }}</td>

            <td>{{ leadsource.group }}</td>

            <td>{{ leadsource.active }}</td>

            <td>
                <div class="btn-group" role="group">
                    <a href="{{ path('lead_source_show', {id: leadsource.id}) }}"
                       class="btn btn-sm btn-info">
                        <i class="bi bi-eye"></i>
                    </a>
                    <a href="{{ path('lead_source_edit', {id: leadsource.id}) }}"
                       class="btn btn-sm btn-warning">
                        <i class="bi bi-pencil"></i>
                    </a>
                    <form method="post"
                          action="{{ path('lead_source_delete', {id: leadsource.id}) }}"
                          style="display: inline;"
                          onsubmit="return confirm('{{ 'action.confirm_delete'|trans }}');">
                        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ leadsource.id) }}">
                        <button type="submit" class="btn btn-sm btn-danger">
                            <i class="bi bi-trash"></i>
                        </button>
                    </form>
                </div>
            </td>
        </tr>
    </template>
</turbo-stream>

<turbo-stream action="update" target="flash-messages">
    <template>
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            <i class="bi bi-check-circle me-2"></i>
            {{ 'LeadSource'|trans }} created successfully!
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    </template>
</turbo-stream>
