<turbo-stream action="prepend" target="notificationtype-list">
    <template>
        <tr id="notificationtype-{{ notificationtype.id }}">

            <td>{{ notificationtype.name }}</td>

            <td>{{ notificationtype.organization }}</td>

            <td>{{ notificationtype.description }}</td>

            <td>
                <div class="btn-group" role="group">
                    <a href="{{ path('notification_type_show', {id: notificationtype.id}) }}"
                       class="btn btn-sm btn-info">
                        <i class="bi bi-eye"></i>
                    </a>
                    <a href="{{ path('notification_type_edit', {id: notificationtype.id}) }}"
                       class="btn btn-sm btn-warning">
                        <i class="bi bi-pencil"></i>
                    </a>
                    <form method="post"
                          action="{{ path('notification_type_delete', {id: notificationtype.id}) }}"
                          style="display: inline;"
                          onsubmit="return confirm('{{ 'action.confirm_delete'|trans }}');">
                        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ notificationtype.id) }}">
                        <button type="submit" class="btn btn-sm btn-danger">
                            <i class="bi bi-trash"></i>
                        </button>
                    </form>
                </div>
            </td>
        </tr>
    </template>
</turbo-stream>

<turbo-stream action="update" target="flash-messages">
    <template>
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            <i class="bi bi-check-circle me-2"></i>
            {{ 'NotificationType'|trans }} created successfully!
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    </template>
</turbo-stream>
