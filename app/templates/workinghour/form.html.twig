{% extends 'base.html.twig' %}

{% block title %}
    {% if workinghour.id %}
        {{ 'action.edit'|trans }} {{ 'WorkingHour'|trans }}
    {% else %}
        {{ 'action.create'|trans }} {{ 'WorkingHour'|trans }}
    {% endif %}
{% endblock %}

{% block body %}
<div class="container py-4">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card">
                <div class="card-header">
                    <h2>
                        {% if workinghour.id %}
                            <i class="bi bi-pencil me-2"></i>
                            {{ 'action.edit'|trans }} {{ 'WorkingHour'|trans }}
                        {% else %}
                            <i class="bi bi-plus-circle me-2"></i>
                            {{ 'action.create'|trans }} {{ 'WorkingHour'|trans }}
                        {% endif %}
                    </h2>
                </div>
                <div class="card-body">
                    {{ form_start(form, {'attr': {'data-turbo': 'true'}}) }}

                    {{ form_errors(form) }}


                    <div class="mb-3">
                        {{ form_label(form.description) }}
                        {{ form_widget(form.description) }}
                        {{ form_errors(form.description) }}

                    </div>

                    <div class="mb-3">
                        {{ form_label(form.dayOfWeek) }}
                        {{ form_widget(form.dayOfWeek) }}
                        {{ form_errors(form.dayOfWeek) }}

                    </div>

                    <div class="mb-3">
                        {{ form_label(form.startTime) }}
                        {{ form_widget(form.startTime) }}
                        {{ form_errors(form.startTime) }}

                    </div>

                    <div class="mb-3">
                        {{ form_label(form.endTime) }}
                        {{ form_widget(form.endTime) }}
                        {{ form_errors(form.endTime) }}

                    </div>

                    <div class="mb-3">
                        {{ form_label(form.minimalMinutesEventDuration) }}
                        {{ form_widget(form.minimalMinutesEventDuration) }}
                        {{ form_errors(form.minimalMinutesEventDuration) }}

                    </div>

                    <div class="mb-3">
                        {{ form_label(form.notes) }}
                        {{ form_widget(form.notes) }}
                        {{ form_errors(form.notes) }}

                    </div>


                    <div class="d-flex justify-content-between mt-4">
                        <a href="{{ path('working_hour_index') }}"
                           class="btn btn-secondary">
                            <i class="bi bi-arrow-left me-1"></i>
                            {{ 'action.back'|trans }}
                        </a>
                        <button type="submit" class="btn btn-primary">
                            <i class="bi bi-check-circle me-1"></i>
                            {{ 'action.save'|trans }}
                        </button>
                    </div>

                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
