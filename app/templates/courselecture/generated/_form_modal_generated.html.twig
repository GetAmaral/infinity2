{% extends '_base_modal.html.twig' %}

{# Configure modal appearance and behavior #}
{% set entity_type = 'courselecture' %}
{% set icon = 'play-circle' %}
{% set form_action = (is_edit and courseLecture.id) ? path('courselecture_edit', {id: courseLecture.id}) : path('courselecture_new') %}
{% set title_create = 'courselecture.modal.create.title'|trans({}, 'courselecture') %}
{% set title_edit = 'courselecture.modal.edit.title'|trans({}, 'courselecture') %}
{% set subtitle_create = 'courselecture.modal.create.subtitle'|trans({}, 'courselecture') %}
{% set subtitle_edit = 'courselecture.modal.edit.subtitle'|trans({}, 'courselecture') %}
{% set submit_key = is_edit ? 'button.save.changes' : 'courselecture.button.create.courselecture' %}
{% set submit_domain = is_edit ? 'messages' : 'courselecture' %}
{% set entity = courseLecture %}

{# Form fields - auto-render all fields #}
{% block modal_body %}
    {# Render all form fields automatically #}
    {% for child in form.children %}
        {% if child.vars.name not in ['_token'] %}
            <div class="form-group-modern">
                {% if child.vars.block_prefixes[1] == 'checkbox' %}
                    {# Checkbox/Switch styling #}
                    <div class="form-check form-switch">
                        {{ form_widget(child, {
                            'attr': {
                                'class': 'form-check-input',
                                'style': 'cursor: pointer;'
                            }
                        }) }}
                        <label class="form-check-label" for="{{ child.vars.id }}" style="color: var(--luminai-text-primary); cursor: pointer; margin-left: 0.5rem;">
                            {{ form_label(child) }}
                        </label>
                    </div>
                {% else %}
                    {# Regular field styling #}
                    <label class="form-label-modern">{{ form_label(child) }}</label>
                    {{ form_widget(child, {
                        'attr': {
                            'class': 'form-input-modern' ~ (child.vars.errors|length > 0 ? ' input-error' : '')
                        }
                    }) }}
                {% endif %}
                {{ form_errors(child) }}
            </div>
        {% endif %}
    {% endfor %}

    {{ form_widget(form._token) }}
{% endblock %}
