{% extends '_base_modal.html.twig' %}

{# Configure modal appearance and behavior #}
{% set entity_type = 'meetingdata' %}
{% set icon = 'camera-video' %}
{% set form_action = (is_edit and meetingData.id) ? path('meetingdata_edit', {id: meetingData.id}) : path('meetingdata_new') %}
{% set title_create = 'meetingdata.modal.create.title'|trans({}, 'meetingdata') %}
{% set title_edit = 'meetingdata.modal.edit.title'|trans({}, 'meetingdata') %}
{% set subtitle_create = 'meetingdata.modal.create.subtitle'|trans({}, 'meetingdata') %}
{% set subtitle_edit = 'meetingdata.modal.edit.subtitle'|trans({}, 'meetingdata') %}
{% set submit_key = is_edit ? 'button.save.changes' : 'meetingdata.button.create.meetingdata' %}
{% set submit_domain = is_edit ? 'messages' : 'meetingdata' %}
{% set entity = meetingData %}

{# Form fields - auto-render all fields #}
{% block modal_body %}
    {# Render all form fields automatically #}
    {% for child in form.children %}
        {% if child.vars.name not in ['_token'] %}
            <div class="form-group-modern">
                {% if child.vars.block_prefixes[1] == 'checkbox' %}
                    {# Checkbox/Switch styling #}
                    <div class="form-check form-switch">
                        {{ form_widget(child, {
                            'attr': {
                                'class': 'form-check-input',
                                'style': 'cursor: pointer;'
                            }
                        }) }}
                        <label class="form-check-label" for="{{ child.vars.id }}" style="color: var(--luminai-text-primary); cursor: pointer; margin-left: 0.5rem;">
                            {{ form_label(child) }}
                        </label>
                    </div>
                {% else %}
                    {# Regular field styling #}
                    <label class="form-label-modern">{{ form_label(child) }}</label>
                    {{ form_widget(child, {
                        'attr': {
                            'class': 'form-input-modern' ~ (child.vars.errors|length > 0 ? ' input-error' : '')
                        }
                    }) }}
                {% endif %}
                {{ form_errors(child) }}
            </div>
        {% endif %}
    {% endfor %}

    {{ form_widget(form._token) }}
{% endblock %}
