{% extends 'base.html.twig' %}

{% block title %}
    {% if attachment.id %}
        {{ 'action.edit'|trans }} {{ 'Attachment'|trans }}
    {% else %}
        {{ 'action.create'|trans }} {{ 'Attachment'|trans }}
    {% endif %}
{% endblock %}

{% block body %}
<div class="container py-4">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card">
                <div class="card-header">
                    <h2>
                        {% if attachment.id %}
                            <i class="bi bi-pencil me-2"></i>
                            {{ 'action.edit'|trans }} {{ 'Attachment'|trans }}
                        {% else %}
                            <i class="bi bi-plus-circle me-2"></i>
                            {{ 'action.create'|trans }} {{ 'Attachment'|trans }}
                        {% endif %}
                    </h2>
                </div>
                <div class="card-body">
                    {{ form_start(form, {'attr': {'data-turbo': 'true'}}) }}

                    {{ form_errors(form) }}


                    <div class="mb-3">
                        {{ form_label(form.filename) }}
                        {{ form_widget(form.filename) }}
                        {{ form_errors(form.filename) }}

                    </div>

                    <div class="mb-3">
                        {{ form_label(form.fileType) }}
                        {{ form_widget(form.fileType) }}
                        {{ form_errors(form.fileType) }}

                    </div>

                    <div class="mb-3">
                        {{ form_label(form.fileSize) }}
                        {{ form_widget(form.fileSize) }}
                        {{ form_errors(form.fileSize) }}

                    </div>

                    <div class="mb-3">
                        {{ form_label(form.url) }}
                        {{ form_widget(form.url) }}
                        {{ form_errors(form.url) }}

                    </div>


                    <div class="d-flex justify-content-between mt-4">
                        <a href="{{ path('attachment_index') }}"
                           class="btn btn-secondary">
                            <i class="bi bi-arrow-left me-1"></i>
                            {{ 'action.back'|trans }}
                        </a>
                        <button type="submit" class="btn btn-primary">
                            <i class="bi bi-check-circle me-1"></i>
                            {{ 'action.save'|trans }}
                        </button>
                    </div>

                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
