<turbo-stream action="prepend" target="productcategory-list">
    <template>
        <tr id="productcategory-{{ productcategory.id }}">

            <td>{{ productcategory.name }}</td>

            <td>{{ productcategory.organization }}</td>

            <td>{{ productcategory.description }}</td>

            <td>{{ productcategory.parentCategory }}</td>

            <td>
                <div class="btn-group" role="group">
                    <a href="{{ path('product_category_show', {id: productcategory.id}) }}"
                       class="btn btn-sm btn-info">
                        <i class="bi bi-eye"></i>
                    </a>
                    <a href="{{ path('product_category_edit', {id: productcategory.id}) }}"
                       class="btn btn-sm btn-warning">
                        <i class="bi bi-pencil"></i>
                    </a>
                    <form method="post"
                          action="{{ path('product_category_delete', {id: productcategory.id}) }}"
                          style="display: inline;"
                          onsubmit="return confirm('{{ 'action.confirm_delete'|trans }}');">
                        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ productcategory.id) }}">
                        <button type="submit" class="btn btn-sm btn-danger">
                            <i class="bi bi-trash"></i>
                        </button>
                    </form>
                </div>
            </td>
        </tr>
    </template>
</turbo-stream>

<turbo-stream action="update" target="flash-messages">
    <template>
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            <i class="bi bi-check-circle me-2"></i>
            {{ 'ProductCategory'|trans }} created successfully!
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    </template>
</turbo-stream>
