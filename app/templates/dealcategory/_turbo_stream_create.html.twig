<turbo-stream action="prepend" target="dealcategory-list">
    <template>
        <tr id="dealcategory-{{ dealcategory.id }}">

            <td>{{ dealcategory.name }}</td>

            <td>{{ dealcategory.group }}</td>

            <td>{{ dealcategory.description }}</td>

            <td>
                <div class="btn-group" role="group">
                    <a href="{{ path('deal_category_show', {id: dealcategory.id}) }}"
                       class="btn btn-sm btn-info">
                        <i class="bi bi-eye"></i>
                    </a>
                    <a href="{{ path('deal_category_edit', {id: dealcategory.id}) }}"
                       class="btn btn-sm btn-warning">
                        <i class="bi bi-pencil"></i>
                    </a>
                    <form method="post"
                          action="{{ path('deal_category_delete', {id: dealcategory.id}) }}"
                          style="display: inline;"
                          onsubmit="return confirm('{{ 'action.confirm_delete'|trans }}');">
                        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dealcategory.id) }}">
                        <button type="submit" class="btn btn-sm btn-danger">
                            <i class="bi bi-trash"></i>
                        </button>
                    </form>
                </div>
            </td>
        </tr>
    </template>
</turbo-stream>

<turbo-stream action="update" target="flash-messages">
    <template>
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            <i class="bi bi-check-circle me-2"></i>
            {{ 'DealCategory'|trans }} created successfully!
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    </template>
</turbo-stream>
